# LAUNCH — מה עומד בין הענף הזה לאוויר · עודכן 27.7.2026

קובץ אחד שעונה בכל רגע: **מה חסר לעלייה, ומי מחזיק כל פריט.**
מתעדכן בכל סבב. הפריטים מסודרים לפי מי חוסם.

---

## 🔒 אצל OC — חוסמי עלייה שאינם קוד

- [ ] **דומיין.** `og:image` / `og:url` מצביעים עכשיו על
  `ormeirc-ship-it.github.io/shift-website` — מי שישתף את הקישור
  בוואטסאפ/אינסטגרם אחרי מעבר דומיין יקבל תצוגה שבורה. שינוי של
  2 שורות ב-`<head>` ברגע שיש כתובת.
- [ ] **מייל / וואטסאפ אמיתיים.** כפתור "שיחת היכרות" מוסתר עם TODO
  (החלטת 26.7, אחרי שנמצא `mailto:` לתיבה שלא קיימת). אתר לידים בלי
  ערוץ קשר = הכפתור המרכזי חסר. שורה אחת להחזיר כשיש כתובת.
- [ ] **תוכן אירועים.** 9 סימוני `TODO: תוכן מ-OC` בסקשן האירועים —
  תיאורים, תאריכים, מחירים, ותמונות אמיתיות מריטריט/סדנה/אירוע.
  המבנה קיים; אין תוכן מומצא, כמוסכם.
- [ ] **פריט E — בדיקת אייפון אמיתי.** לפתוח את האתר בספארי-iOS ולהסתכל
  על שער הכניסה: לוגו מצויר = שומר הסף עובד; מלבן שחור = לדווח ונחזיר
  את החסימה. אין מכשיר בסביבת העבודה — רק OC יכול לסגור את זה.
- [ ] **הכרעת B3** — סיום הצלילה: גרסה א (הווילון) או ב (המשטח)?
  `delivery/B3.md` + 4 צילומים. אחרי ההכרעה: חיווט לגלילה (קוד, שעה-שתיים).
- [ ] **החלטת Lenis (שארית פריט N).** ברשת איטית המבקר רואה מסך ראשון
  אחרי ~8ש'. הדילול (B2) קיצץ את זנב הטעינה אבל לא את הרגע הזה —
  הוא תלוי ב-175KB של חבילת התנועה לפני הפריים הראשון. ויתור על
  Lenis (~30KB) הוא הצעד הזמין; הגלילה תהיה פחות "צפה". החלטת טעם.

## 🔧 בקוד — פתוח, לא חוסם עלייה

- [ ] **B2-אימות ביד:** OC מרגיש את הגלילה בצלילה על המכשיר אחרי
  הדילול. אם עדיין לא חלקה — הצעד הבא מוכן (createImageBitmap מראש).
- [ ] **ניקוי היסטוריית git** (‎161MB אחרי לילה של node_modules בפנים) —
  אופציונלי, החלטת OC, לא נוגע לאתר החי.
- [ ] מחיקת ה-CSS של גרסת B3 שלא תיבחר.

## ✅ סגור ומאומת (נכון ל-27.7)

- [x] תוכן 21 הימים תואם למקור החי — `npm run verify` 21/21, מוניטור
  סחיפה (`npm run drift`) משווה גם מול Firestore החי.
- [x] favicon קיים (`assets/brand/favicon.png`) · title · description ·
  תגי og מלאים (התוכן נכון; רק ה-URL ממתין לדומיין).
- [x] **workflow הפריסה מסנן לקבצי ריצה בלבד** — בלי node_modules,
  ‏_build, delivery, scripts, tests ומסמכים. פריסה = push ל-main בלבד.
- [x] קריאוּת המסלול בכל גודל חלון — `npm run overlap`, ‏16 גדלים, 21/21.
- [x] חבילת הבדיקות המלאה ירוקה: 23 בדיקות · קונסול נקי · נגישות
  (ממצא יחיד מתועד: קישור הדילוג, בכוונה) · reduced-motion מלא ·
  טיפוגרפיה ב-5 רוחבים · תקציב משקל · תאימות (עם המגבלה המתועדת:
  אין ספארי/פיירפוקס בסביבה) · stress בארבעה תרחישים.
- [x] מסך הפתיחה נסגר מוקדם ברשת איטית, בלי נעילת גלילה יתומה (T9/T11).

---

**כלל האצבע:** אם כל התיבות של OC מסומנות — האתר עולה באותו יום.
הקוד לא מחכה לאף אחד; מה שנשאר בו הוא ליטוש, לא חסימה.
