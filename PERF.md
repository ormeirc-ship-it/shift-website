# PERF — מדידת חלקות התנועה (פריט T1)

**נמדד:** 26.7.2026 · headless Chrome (הכרום המותקן במכשיר) · גלילה מלאה
מראש העמוד עד הסוף, 14px לפריים — קצב של גלילת אצבע רציפה.
**קומיט:** `a56c29e`+ · **הכלי:** `perf.mjs` (בסקראצ'פד; ראה "מה חסר" למטה).

---

## התוצאות

| תצורה | FPS חציון | אחוזון-5 | אחוזון-1 | פריימים >50ms | long tasks | recalc | layout |
|---|---|---|---|---|---|---|---|
| דסקטופ 1440, ללא האטה | 120 | 101 | 94 | 0 / 1564 | 0 | 4872 (690ms) | 722 (138ms) |
| דסקטופ 1440, האטה ×4 | 120 | 94 | 88 | 0 / 1564 | 0 | 4908 (808ms) | 765 (297ms) |
| מובייל 390, ללא האטה | 120 | 101 | 93 | 0 / 1707 | 0 | 5417 (624ms) | 375 (99ms) |
| מובייל 390, האטה ×4 | 120 | 97 | 93 | 0 / 1707 | 0 | 5445 (502ms) | 426 (248ms) |

**המסקנה: לא נמצא jank.** אין ולו long task אחד מעל 50ms בשום תצורה, כולל
בהאטת CPU ×4 — שם יושב הקהל האמיתי מאינסטגרם. אחוזון-1 לא יורד מתחת ל-88
בשום מקום, כלומר גם הפריימים הגרועים ביותר נשארים מעל 60.

---

## מה כן בלט — ומה עשיתי איתו

**~5,000 חישובי סגנון מחדש (recalc) לאורך הגלילה.** זה נשמע הרבה, ולכן בדקתי
מאיפה זה מגיע לפני שהנחתי שזה באג. היחס יציב: כ-3 recalcs לכל צעד גלילה, בכל
ארבע התצורות. זה בדיוק מה שמצופה ממספר ה-ScrollTriggers עם `scrub` שרצים על
העמוד — כל אחד מהם משנה סגנון בכל עדכון. **זו עלות מובנית של הגישה, לא דליפה.**

**שני דברים שכן היו שלי, ותוקנו:**

1. **layout thrash אמיתי בגוון הניווט.** הגרסה הראשונה של `initNavTone` קראה
   `getBoundingClientRect()` פעמיים בתוך מאזין `scroll` — כלומר אילצה חישוב
   פריסה מסונכרן בכל גלילה. הוחלף בשני תצפיתנים (IntersectionObserver
   לנוכחות, MutationObserver למחלקה) ואפס קריאות פריסה בזמן גלילה.
2. **כתיבות סרק ב-`render()` של הצלילה.** הפונקציה רצה בכל פריים וכתבה סגנון
   לשישה אלמנטים גם כשההתקדמות לא זזה. נוסף שער `Math.abs(p - lastP) < 0.0008`.

**כמה זה שיפר את המספרים? כמעט כלום** (recalc: 0%, layout: -1% עד -3%).
אני מדווח את זה כמו שהוא ולא מנפח: התיקונים נכונים בפני עצמם, אבל הם לא היו
צוואר הבקבוק. צוואר הבקבוק פשוט לא קיים כרגע.

---

## מגבלות המדידה — לקרוא לפני שסומכים על המספרים

1. **גלילה מתוזמנת ב-rAF, לא קלט אמיתי.** הלולאה מתקדמת פריים-פריים, ולכן
   ה-FPS "החציוני" של 120 משקף את קצב הלולאה ולא חוויית גלגלת או אצבע.
   מה שכן אמין: **long tasks, recalc, layout** — הם נמדדים מהדפדפן עצמו.
2. **localhost.** אין latency רשת, ולכן זה לא אומר כלום על זמן טעינה.
   פריט H ב-BACKLOG (Lighthouse אמיתי) עדיין פתוח.
3. **headless.** אין הרכבה (compositing) על GPU אמיתי כמו במכשיר.
4. **לא נמדד על מכשיר אמיתי.** האטת CPU ×4 היא קירוב, לא אייפון.

---

## הכלים — עכשיו במאגר (פריט M, 27.7)

`npm run perf` · `npm run weigh` · `npm run audit` · `npm run shots` · `npm run net`

הכלים מגישים את הריפו בעצמם (שרת סטטי זמני על פורט פנוי) ולא תלויים בשרת
תצוגה חיצוני — כל מספר כאן ניתן לשחזור מ-clone נקי בפקודה אחת. כרום מאותר
אוטומטית, עם `CHROME_PATH=` לעקיפה. `npm run audit` יוצא עם קוד שגיאה בכל
כשל, כך שהוא מתאים ל-CI.

---

# מדידה ברשת אמיתית (פריט H, 27.7)

**זה החלק החשוב בקובץ.** כל המספרים למעלה נמדדו על localhost — כלומר בלי
latency ובלי מגבלת רוחב פס. הנה מה שקורה באמת (`npm run net`):

| תצורה | FCP | מסך הפתיחה נסגר | כל הרצף |
|---|---|---|---|
| מובייל 390 · Fast 4G | 636ms | **1.1ש'** | 4.4ש' |
| מובייל 390 · Slow 4G | 3900ms | **8.2ש'** | 35.5ש' |
| דסקטופ 1440 · Slow 4G | 3992ms | **7.7ש'** | 71.6ש' |
| מובייל 390 · Slow 3G | 6464ms | **11.8ש'** | 48.0ש' |

**הפער מול המספרים המקומיים הוא פי 250.** "פריים ראשון תוך 30ms" נכון
רק כשאין רשת בכלל.

## מה תוקן בעקבות המדידה

1. **טעינה בשלבים במקום 97 בקשות במקביל.** הפריים הראשון נטען לבדו; רק
   כשהוא בפנים מתחילים את השאר, לפי הסדר, בחלון של 6. הסדר חשוב כי המבקר
   גולל מלמעלה למטה וצורך את הפריימים בדיוק בסדר הזה.
2. **מסך הפתיחה לא מחכה ל-JS.** הוא נסגר ב-`onload` של תמונת הגיבוי,
   ישירות ב-HTML. קודם הוא חיכה לחבילת התנועה (175KB) — ברשת איטית זה
   שניות של מסך ריק. **Fast 4G: 1.9ש' → 1.1ש'.**
3. רשתות הביטחון קוצרו (4000→2200ms ב-`<head>`, 2500→2000 במנוע).
4. `<link rel="preload" as="image" fetchpriority="high">` לפריים הראשון.

## מה זה לא פתר — וזו ההודאה החשובה

**ב-Slow 4G הזמן כמעט לא זז (8.5ש' → 8.2ש').** הסיבה פשוטה: ב-400kbps
ה-FCP לבדו הוא 3.9 שניות, לפני שנגעו בכלל בתמונות. אחריו יושבת חבילת
התנועה — 175KB של GSAP + ScrollTrigger + Lenis — שתופסת את הצינור.
**אי-אפשר לתקן את זה בסדר טעינה. צריך פחות בייטים.**

שתי הדרכים האמיתיות, שתיהן דורשות החלטה:
- **לדלל את הרצף** (97→60 פריימים חוסך כמגה). זו החלטה של OC.
- **לקצץ בחבילת התנועה** — Lenis (~30KB) הוא הכי קל לוותר עליו; הגלילה
  תהיה פחות חלקה אבל האתר יעלה מהר יותר.

עד שאחד מהם ייעשה, **המבקר בחיבור איטי מחכה 8 שניות.** מי שמגיע
מאינסטגרם על סלולר בינוני-איטי — זה הוא.

---

# B2 — דילול הרצף 97→60 (27.7, החלטת OC)

הדסקטופ דולל מ-97 ל-60 פריימים (בחירה אחידה, קצוות נשמרים — f001
ופריים ההגעה-לאור). המובייל נשאר 65 — הוא כבר בסביבת היעד. המקור
המלא שמור ב-`_build/brain-seq-d97/` ואינו נפרס (ה-workflow מסנן).
משקל הרצף בדסקטופ: 3.1MB → 1.9MB.

## לפני / אחרי — רשת אמיתית (`npm run net`)

| תצורה | מסך פתיחה נסגר | כל הרצף |
|---|---|---|
| מובייל 390 · Slow 4G | 8.2ש' → **8.4ש'** | 35.5ש' → 35.7ש' |
| דסקטופ 1440 · Slow 4G | 7.7ש' → **7.9ש'** | 71.6ש' → **47.8ש'** |
| מובייל 390 · Fast 4G | 1.1ש' → 1.1ש' | 4.4ש' → 4.3ש' |
| מובייל 390 · Slow 3G | 11.8ש' → 11.9ש' | 48.0ש' → 48.1ש' |

**הקריאה הישרה של המספרים:** הדילול קיצץ שליש מזמן טעינת הרצף המלא
בדסקטופ (71.6→47.8ש') — זה הרווח האמיתי, והוא בדיוק מה שדילול יכול
לתת. **את 8.2 השניות עד מסך ראשון הוא לא הזיז**, והתנודות (±0.2ש')
הן רעש מדידה. הסיבה לא השתנתה מאז המדידה הקודמת: הרגע הזה תלוי
ב-FCP (3.9ש' ב-400kbps) ובפריים הראשון — לא באורך הזנב של הרצף.
ההערכה "8.2 → ~5ש'" שליוותה את ההחלטה לא מתקיימת. מה שכן יכול להזיז
את הרגע הראשון ברשת איטית נשאר מה שהיה: פחות בייטים לפני הפריים
הראשון — חבילת התנועה (175KB) היא המועמדת, והחלטת Lenis עוד פתוחה.

## חלקוּת (`npm run perf`, האטת CPU ×4)

לפני ואחרי הדילול — זהה בגבולות הרעש: 0 long tasks, אחוזון-1 של 88
(דסקטופ) / 98 (מובייל), recalc ~4.9K. **המדידה הזו לא מרגישה את מה
ש-OC הרגיש ביד** — headless בלי GPU אמיתי לא מודד decode של WebP על
מכשיר. מה שהדילול כן נותן ביד: החלפת תמונה כל ~8.7vh גלילה במקום
~5.4vh — שליש פחות עבודת decode לאורך הצלילה. **נדרש אימות של OC על
המכשיר האמיתי.** אם עדיין לא חלק — הצעד הבא מוכן: `createImageBitmap`
מראש (decode מחוץ לרגע הציור), במקום לדלל עוד.
