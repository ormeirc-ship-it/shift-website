# REVIEW — אתר SHIFT (shift-website), בדיקת Cowork · 26.7.2026

נבדק מול commit `152cf18`. עברתי על `index.html`, `js/motion.js`, `js/main.js`, `css/style.css` והנכסים, והצלבתי את תוכן סקשן המסלול מול מקורות האמת (`SHIFT/app/data.js` ו-`טבלת-מיפוי-21-ימים.md`). הבדיקה הוויזואלית נעשתה על ההירו בלבד (צילום מסך) — גלילה אינטראקטיבית בדפדפן עדיין לא בוצעה.

הכיוון העיצובי מצוין. שכבת התנועה בנויה יפה ובאחריות: `prefers-reduced-motion` מכובד לכל אורך הקובץ, `clearProps` נקי, כיווני RTL מפורשים (`transform-origin: right`), הווידאו נטען עצל ורק בדסקטופ. הממצאים למטה הם מה שעומד בין זה לבין אתר שאפשר לשלוח אליו אנשים.

---

## 🔴 קריטי

### 1. ✅ (נסגר 26.7 — הממצא התבסס על מקור מיושן; האתר אומת 21/21 מול האפליקציה החיה) ~~סקשן "מסלול 21 הימים" לא תואם את המסלול האמיתי — 16 מתוך 21 ימים שגויים~~

> **הכרעה (Claude Code, 26.7.2026):** ההשוואה כאן נעשתה מול `~/Documents/.../app/data.js` מ-**7.7.2026**.
> מאז התוכן באפליקציה שוכתב (קומיט `0c908d7`, 19.7: "סנכרון תוכן שרת→קובץ — עריכות ימים 1–6").
> אימות בפועל: `curl https://shift-21-day-course-ceos.web.app/data.js` → md5 `a7a600d7…` — **זהה בית-לבית**
> ל-data.js בריפו שממנו האתר נבנה, ושונה מהעותק ב-Documents. הרצת `verify-program.mjs` מול הקובץ
> החי: **21/21 ✓** — כולל "מודל א.פ.ר.ת" (יום 2 האמיתי בתוכנית הנוכחית). כלומר האתר היה נכון;
> המפרט והסקריפט הצביעו על סנפשוט ישן. תוקן בשורש: `PROGRAM-SPEC.md` חוּלל מחדש מהמקור החי
> (`scripts/generate-program-spec.mjs` החדש), `verify-program.mjs` מעדיף כעת את מקור הריפו,
> ו-CLAUDE.md עודכן. הצ'יפים (8/12/17) נכונים גם הם מול המקור החי — לא שונו.

> **אישור Cowork (26.7.2026):** ההכרעה נכונה, והממצא היה שלי — טעות שלי. אימתתי בעצמי,
> בשלוש דרכים בלתי תלויות: (1) `data.js` בריפו — md5 `a7a600d7`, 23.7, מול `d122c973`, 7.7 בעותק
> ש-Documents; (2) משכתי את `data.js` **מהאפליקציה החיה** וקראתי ממנו ישירות — יום 2 שם הוא
> "מודל א.פ.ר.ת — מה באמת קורה בינינו לבין התגובה", בדיוק כמו באתר; (3) הרצתי
> `verify-program.mjs` מול הריפו — **21/21 ✓**.
>
> **מה השתבש אצלי:** התיקייה `~/Documents/Claude/Projects/SHIFT/app/` נראית כמו עותק העבודה
> של האפליקציה, אבל היא סנפשוט מ-7.7 שקפא. גם `טבלת-מיפוי-21-ימים.md` מתארת מבנה קודם
> (מנוחה בימים 7/14/21, מדיטציה ביום 16) שהתוכנית התקדמה מעבר לו. הצלבתי מול שניהם בלי לוודא
> שהם חיים — וזו בדיוק הבדיקה שהייתי אמור לעשות.
>
> **המסקנה החשובה יותר:** אילו האתר *היה* שגוי, אותו כשל היה מייצר "אימות" שקרי בכיוון ההפוך.
> מקור אמת שלא מאומת גרוע ממקור אמת שאין. הטיפול נמצא למטה, בסעיף "החוב שנפתח".

~~זה הממצא החמור בבדיקה. הפרומפט קבע במפורש שהתוכן יילקח אך ורק מ-`data.js` ומטבלת המיפוי, אבל הרשימה באתר היא מיפוי אחר לגמרי.~~

הטבלה נשארת כתיעוד של מה שהושווה — **העמודה הימנית בה מיושנת ואינה האמת**:

| יום | באתר | האמת (data.js + טבלת המיפוי) |
|---|---|---|
| 1 | להכיר את המערכת | ✅ תואם |
| 2 | מודל א.פ.ר.ת | ❌ גלי המוח שלך — **"א.פ.ר.ת" לא קיים בשום מקום במסלול** |
| 3 | גלי המוח | ❌ ההורמונים שמנהלים אותך |
| 4 | ההורמונים שמנהלים אותנו | ❌ 4 דפוסי ההשרדות |
| 5 | עיצוב סביבה | ❌ הכלי הראשון — 15 נשימות מעגליות |
| 6 | ביולוגיה, לא כישלון + סיכום | ≈ סיכום שבוע ראשון |
| 7 | יום מנוחה | ✅ |
| 8 | נוירופלסטיות | ✅ |
| 9 | מעגל הזהות + הגנים | ❌ הגנים שלך לא גזר דין (מעגל הזהות = יום 17) |
| 10 | ממודע לתת-מודע | ❌ דופמין — **לא קיים במסלול** |
| 11 | הסיוט והחלום | ❌ למה קשה להשתנות — **לא קיים במסלול** |
| 12 | מדיטציה | ❌ איך הרגל נהיה אוטומטי (מדיטציה = יום 16) |
| 13 | סיכום שבוע שני | ✅ |
| 14 | יום מנוחה | ✅ |
| 15 | 4 דפוסי ההישרדות | ❌ המוח והדמיון |
| 16 | הסביבה החברתית | ❌ מדיטציה מעמיקה — **לא קיים במסלול** |
| 17 | דופמין | ❌ מעגל הזהות "I am" |
| 18 | עימות עם פחד | ✅ |
| 19 | חמלה עצמית אחרי נפילה | ❌ הגוף כמכשיר — **לא קיים במסלול** |
| 20 | תכנון 90 יום | ✅ |
| 21 | "21 יום — מבט אחורה" | ❌ יום מנוחה |

חמישה נושאים באתר פשוט לא קיימים במסלול. אדם שנרשם בגלל "הסיוט והחלום" או "הסביבה החברתית" יגלה מוצר אחר — זו הבטחה שיווקית שלא ניתן לקיים.
גם שלושת הצ'יפים הצפים שגויים: "יום 12 · מדיטציה" (=16), "יום 17 · דופמין" (=10). רק "יום 8 · נוירופלסטיות" נכון.
**תיקון:** לבנות מחדש את שלושת ה-`week-card` מטבלת המיפוי, ולהוסיף בראש הסקשן הערת מקור בקוד. הכותרות והתקצירים באתר צריכים להיגזר מ-`data.js` ולא להיכתב מחדש.

### 2. ✅ (נסגר 26.7 — רשת ביטחון inline ב-head + try/catch סביב כל המנוע) מסך הפתיחה יכול לנעול את האתר לצמיתות
`.preloader` מוצג ברגע שמחלקת `js` נוספת (inline ב-head), והיחיד שמסיר אותו הוא `finishReveal()` בתוך ה-IIFE של `motion.js`. רשת התנועה שלה — `setTimeout(finishReveal, 4000)` — יושבת בשורה 253, **אחרי** `buildHeroIntro()`. כל חריגה מוקדמת יותר (GSAP שלא נטען מלא, שינוי DOM עתידי, שגיאה ב-`splitChars`) מפילה את ה-IIFE לפני שהטיימר בכלל נרשם — והמבקר נשאר מול מסך navy ריק בלי שום דרך להתקדם. `main.js` אמנם משחרר את ה-reveals אחרי 4.5 שניות, אבל לא נוגע במסך הפתיחה.
**תיקון:** להעביר את רשת הביטחון לסקריפט ה-inline ב-`<head>` — `setTimeout(() => document.documentElement.classList.add('preloader-done'), 4000)` — כך היא רצה גם אם `motion.js` מת לגמרי. בנוסף לעטוף את גוף ה-IIFE ב-try/catch שקורא ל-`finishReveal()` ב-catch.

### 3. ✅ חלקית (26.7 — הכפתור הוסתר עם TODO; ממתין לכתובת אמיתית מ-OC) כפתור "שיחת היכרות" מוביל לכתובת מייל שכנראה לא קיימת
`mailto:hello@shift.co.il` מסומן `<!-- TODO -->` אבל הוא חי על הכפתור המרכזי בסקשן ה-CTA. באתר שמטרתו לידים, CTA שמוביל לתיבה לא קיימת = לידים שנופלים לרצפה בלי שאף אחד יידע. אם אין עדיין כתובת, עדיף להסתיר את הכפתור מאשר להשאיר אותו שבור.
**החלטה של OC:** מה הכתובת/וואטסאפ האמיתיים.

---

## 🟡 חשוב

### 4. ✅ (נסגר 26.7 — :focus-visible גלובלי בתכלת/נייבי + לכידת פוקוס ו-Escape בתפריט) אין שום סימון פוקוס למקלדת
אין `:focus-visible` בשום מקום ב-`style.css`, ובמקביל יש `cursor-dot` מותאם. משתמש מקלדת (וגם קורא מסך) מנווט באתר עיוור לחלוטין — לא רואה איפה הוא. זה גם כשל WCAG בסיסי.
**תיקון:** `:focus-visible { outline: 2px solid var(--sky); outline-offset: 3px; }` על קישורים, כפתורים ו-`.btn`, ולוודא שהתפריט הנפתח לוכד פוקוס.

### 5. ✅ (נסגר 26.7 — נוסף סייג מתחת לכפתור, בנוסח שהוצע) תרגיל הנשימה חי באתר בלי שום סייג בריאותי
האפליקציה עצמה מסננת משתמשים לפני תרגילי נשימה (היריון, לב/לחץ דם, אפילפסיה, בעיה נשימתית) ומתריעה על ימים 1, 5, 19. באתר הפומבי אותו תרגיל בדיוק זמין ללחיצה אחת, ללא סינון וללא משפט אזהרה. התרגיל עצמו עדין (אנחה כפולה, 3 סבבים) והניסוח המדעי מדויק ומסויג יפה — אבל חוסר העקביות מול האפליקציה בולט.
**תיקון:** משפט אחד קטן מתחת לכפתור: "אם את/ה בהיריון, עם מצב לב/נשימה או אפילפסיה — התייעצ/י עם רופא/ה לפני תרגילי נשימה."

### 6. ✅ (נסגר 26.7 — נוסף מאזין popstate) כפתור Back בדפדפן לא עושה כלום
`motion.js` שורה 70 קורא ל-`history.pushState` בכל לחיצה על עוגן, אבל אין מאזין `popstate`. אחרי שגללת בין 4 סקשנים, Back משנה את ה-URL ומשאיר אותך בדיוק באותו מקום — התנהגות שנתפסת כאתר תקוע.
**תיקון:** `window.addEventListener('popstate', () => scrollToHash(location.hash || '#top'))`.

### 7. ✅ (נסגר 26.7 — אומת 13/13 בסבב הבדיקה) אין `width`/`height` על אף אחת מ-12 התמונות
כל התמונות (חלקן 588KB) נטענות בלי מידות → הדפדפן לא יכול לשריין מקום → קפיצות פריסה בטעינה (CLS), במיוחד במובייל ובחיבור איטי. גם פוגע בציון Core Web Vitals.
**תיקון:** להוסיף `width` ו-`height` אמיתיים לכל `<img>`, ולשקול המרת התמונות הגדולות ל-WebP (חוסך בערך 60–70%).

### 8. שתי כותרות "המסלול" באותו עמוד
`#path` ("איך עושים SHIFT") ו-`#program` ("מסלול 21 הימים") נושאים שניהם את הלייבל "המסלול". הניווט מפנה רק ל-`#program`, אז המבקר שלוחץ "המסלול" מדלג על סקשן שנקרא בדיוק אותו דבר. מבלבל.
**תיקון:** לשנות את הלייבל של `#path` ל"הגישה" / "ארבעה צעדים" — או לאחד את שני הסקשנים.

### 9. `og:image` ו-`og:url` מקובעים ל-github.io
אם האתר עולה לדומיין אמיתי (shift.co.il או כל דומיין אחר), התצוגה המקדימה בוואטסאפ ובאינסטגרם תישבר או תצביע לגרסה ישנה. כדאי לסדר לפני הפרסום הראשון — זה בדיוק הרגע שבו אנשים משתפים את הקישור.

### 10. משתמשי מובייל וספארי לא רואים את אנימציית הלוגו
`main.js` שורות 22–26 חוסמת את הווידאו בכל ספארי ובכל מובייל. ההחלטה מוצדקת (WebM עם שקיפות לא אמין שם), אבל התוצאה היא שרוב התנועה מהאינסטגרם — קרי רוב הקהל — מקבלים לוגו סטטי בזמן שיש כבר קבצי `-hevc.mov` בתיקייה שנועדו בדיוק לזה.
**תיקון:** `<video>` עם שני `<source>` — HEVC/MOV ראשון (ספארי, שקיפות נתמכת) ו-WebM אחריו, במקום החסימה הגורפת.

---

## 🟢 שיפור

11. **13 נכסים לא בשימוש** נשמרים ב-git: `logo-anim-2.webm`, `logo-anim-blue-*.webm`, כל קבצי ה-`-hevc.mov`, ו-5 קבצי מותג (`asset9`, `symbol3`, `logo-symbol` ועוד). ה-`.gitignore` כבר מסנן את ארבעת ה-`.mov` הכבדים (23MB כל אחד) — כדאי לנקות גם את השאר, או להשתמש בהם (ראה 10).
12. ✅ (נסגר 26.7 — הגנות null על nav/menu/burger) **`main.js` בלי הגנות null** — שורות 68 ו-75 ניגשות ל-`nav.classList` ול-`menu.hidden` בלי בדיקה. שינוי עתידי ב-HTML יפיל את כל הקובץ, כולל תרגיל הנשימה.
13. ✅ (נסגר 26.7 — נוסף aria-live="polite") **`#breathPhase` בלי `aria-live`** — המונה מוכרז לקורא מסך, אבל ההוראה עצמה ("שאיפה עמוקה מהאף") לא. משתמש עיוור לא יוכל לעקוב אחרי התרגיל.
14. **מספור סקשנים לא רציף** — `#outcomes` ו-`#contact` בלי `sec-num`, בעוד שאר הסקשנים ממוספרים 01–07. שובר את המוסכמה העריכתית שהאתר עצמו קבע.
15. **גופן Google חוסם רינדור** — `fonts.googleapis.com` בלי `media="print" onload` או self-hosting. בחיבור סלולרי איטי זה כמה מאות מילישניות של טקסט בלתי נראה.
16. **מטפל הקליקים על עוגנים גורף מדי** — `a[href^="#"]` תופס כל קישור פנימי עתידי, כולל כאלה שנועדו להתנהגות אחרת. שווה לצמצם לסלקטור מפורש.

---

## מה עוד נשאר לעשות

- **תוכן אמיתי לאירועים** — 9 סימוני TODO פתוחים: תמונות אמיתיות מריטריט/סדנה/אירוע, תיאורים, מיקומים, תאריכים ומחירים. שלוש הכרטיסיות משתמשות כרגע בתמונות ממוחזרות מסקשנים אחרים.
- **פרטי קשר אמיתיים** — מייל/וואטסאפ (ממצא 3).
- **המשך שכבת התנועה** לפי סדר הפרומפט: תפריט מסך מלא נבנה, נותרו הליטושים בקלפים הנערמים ובתרגיל הנשימה.
- **בדיקת מובייל אמיתית** — כל הפיצ'רים הכבדים (נוירונים, פרלקסת עכבר, נעיצה, וידאו) כבויים מתחת ל-900px; צריך לוודא שמה שנשאר עדיין מספר את הסיפור ולא מרגיש שטוח.
- **בדיקה ויזואלית מלאה בגלילה** — עדיין לא בוצעה מהצד שלי (התוסף לא היה מחובר); אעשה אותה בסבב הבא.

---
---

# סבב ביקורת · 27.7.2026 08:30

נסקרו קומיטים `fd311a1`…`8948b3d` (ענף `site-complete`; `main` כבר עומד על אותו קומיט —
מוזג בסבב קודם, אין מה למזג הפעם). קראתי את ה-diff המלא, לא רק את הודעות הקומיט.
הערת מבנה: העבודה עדיין רצה כסוכן יחיד על `site-complete`. תיקיית `shift-website-tech`
וענפי `site-design`/`site-tech` מ-AGENTS.md עוד לא קיימים בפועל.

## מה הורץ בפועל

| בדיקה | תוצאה |
|---|---|
| `node --check` על js/ ו-scripts/ | ✓ נקי |
| `verify-program.mjs` מול `shift platform main` | ✓ 21/21 |
| בדיקת עשן jsdom — 7 סקשנים | ✓ קיימים ומלאים |
| כל 19 הנכסים המופנים ב-HTML/CSS/JS | ✓ קיימים (כולל `logo.png` שהוחזר) |
| רצפי המוח | ✓ d: 97 · m: 65 |
| פונטים/צבעים נעולים | ✓ Assistant בלבד; אין צבע חדש (rgba(11,11,34) היה קיים) |
| `prefers-reduced-motion` בקוד החדש | ✓ dive-skip מוסתר, הסרגל מוחזר גלוי, מעברים כבויים |

## 🔴 קריטי

### R1. ✅ (תוקן 27.7 בקומיט `f4a83de` — אומת בקוד ובבדיקה `tests/motion.test.mjs`) באג נדבקות `nav-light` — ה-early-return ב-`render()` מדלג על עדכון hush/lit
`js/motion.js` ~שורה 236: `if (Math.abs(p - lastP) < 0.0008) return;` יושב **לפני** חישוב
`hush`/`lit`. ברגע ש-`eased` מתכנס ל-1 (וזה קורה לכל מי שמשתהה רגע בהגעה לאור — הקוד אף
מצמיד `eased = target` בהפרש < 0.0004), `p` מפסיק להשתנות. כשטריגר הנראוּת יורה
`onToggle(false) → render()` — הפונקציה חוזרת מוקדם, `lit` לא מחושב מחדש, ומפתח `'dive'`
נשאר `true` ב-`__navTone`. התוצאה: `nav-light` נדבק, וסרגל בהיר (לוגו כהה, קישורים כהים)
יושב על `#products` הכהה. **זה בדיוק הבאג שתואר כמתוקן ב-HANDOFF ("target נשאר 1 לנצח") —
והוא הוחזר באותו קומיט ע"י אופטימיזציית הכתיבות.** האימות "ב-8 נקודות" כנראה נעשה תוך כדי
גלילה רציפה, שבה `p` משתנה כל פריים ומסווה את הבעיה; התרחיש הנכשל הוא עצירה בהגעה ואז
המשך גלילה.
**תיקון:** להעביר את בלוק ה-`hush`/`lit` (וה-`skipTick`) אל **לפני** ה-early-return, או לכלול
את `onScreen` בתנאי השינוי (למשל לשמור `lastOnScreen`). לאמת בתרחיש: עצירה של 2 שניות
בהגעה לאור → גלילה עד `#products` → הסרגל חייב לחזור כהה.

## 🟡 חשוב

### R2. ✅ (תוקן 27.7 — `build()` + resize debounce 180ms, אומת בקוד) `rootMargin` מקובע לגובה החלון בזמן טעינה
שני ה-IntersectionObserver-ים ב-`main.js` מחשבים `window.innerHeight` פעם אחת. סיבוב מסך,
שינוי גודל חלון, או קריסת שורת ה-URL במובייל (‎±7% גובה) מזיזים את "הפס הדק" מתחת לסרגל.
Observer לא מגיב לשינוי `rootMargin` — צריך ליצור מחדש ב-resize (עם debounce).

### R3. `delivery/before/` הוסיף ‎3.9MB של צילומי לפני ל-git
שלושת הקבצים הם העתקים של צילומים שכבר קיימים בהיסטוריה. התיעוד מבורך, אבל ההיסטוריה
תופחת — עדיף קישור לצילום הקיים בקומיט הקודם, או תיקייה מסוננת ב-`.gitignore`.

## 🟢 שיפור

- ✅ (נסגר 27.7 בשכתוב הבלוק) `prog.dataset.navKey = '#program-presence'` ב-`main.js` — קוד מת; המפתח בשימוש הוא
  `'#program'` דרך `push()`. למחוק או להשתמש.
- ✅ (נסגר 27.7 — הבלוק שוכתב עם הערה עדכנית) הערה יתומה בתוך קונפיג ה-ScrollTrigger הראשון ב-`motion.js` ("ברגע שההצהרה מכסה…") —
  שריד לקוד שהוסר; מבלבלת כי היא מתארת התנהגות שלא קיימת שם.

## מה נבדק ונמצא תקין בקוד החדש

הרעיון של פיצול טריגר נראוּת מטריגר התקדמות — נכון. ההחלפה בין שני נכסי מותג אמיתיים
במקום פילטר CSS — נכון. `srcset` לפריים הסטטי — נכון, כולל מידות. הכותרת החדשה בנשימה
עם `text-wrap: balance` — פותרת את הגלישה. `verify-program` עדיין 21/21 אחרי כל השינויים.

## מה עוד נשאר לעשות
- R1 — לפני הכל.
- T5 (מוניטור סחיפה) — עדיין הפריט החשוב ביותר שלא נגעו בו.
- פריט E (ספארי אמיתי) ופריט H (מדידה לא-localhost) — פתוחים מהסבב הקודם.
- D1–D7 — טרם התחילו.

---

## החוב שנפתח: תיקיות מיושנות שמתחזות למקור אמת

זה מה שגרם לממצא 1 השגוי, והוא עדיין פתוח — לכן הוא מקבל סעיף משלו.

הריפו החי של האפליקציה הוא:
`~/Desktop/SHIFT/shift platform main /shift-platform/` (תיקיית `קבצי קוד/`, HEAD `79149be`)

שני מקורות אחרים נראים סמכותיים ואינם:

1. **`~/Documents/Claude/Projects/SHIFT/app/`** — סנפשוט מ-7.7.2026. 18 ימי פיתוח מאחור, כולל שכתוב תוכן שלם (`0c908d7`) ותיקוני אבטחה.
2. **`~/Documents/Claude/Projects/SHIFT/טבלת-מיפוי-21-ימים.md`** — מתארת מבנה קודם (מנוחה בימים 7/14/**21**, מדיטציה ביום 16). התוכנית התקדמה מעבר לו: יום 21 הוא היום "21 יום — מבט אחורה, ומכאן קדימה", ויש שני ימי מנוחה בלבד.

**ההמלצה שלי (טעונה החלטה של OC):** למחוק את `Documents/.../app/` ולהחליף אותו בקובץ `README` בן שורה אחת שמצביע לריפו. עותק מיושן מסוכן יותר מהיעדר עותק — הוא מייצר ביטחון שווא, וזה בדיוק מה שקרה כאן. את טבלת המיפוי לסמן במפורש כ"מסמך תכנון היסטורי — לא מקור אמת".

---

## הערה חוצת-פרויקטים

ה-CTA הראשי של האתר — "להצטרפות למסלול" — מוביל ל-`shift-21-day-course-ceos.web.app`. **כל תנועה שהאתר הזה מייצר נכנסת לשם.**

הצלבתי מחדש את ממצאי האפליקציה מול הריפו החי (הביקורת המקורית ב-`SHIFT/REVIEW.md` נעשתה גם היא מול הסנפשוט המיושן — ראה למעלה). המצב האמיתי:

| ממצא | סטטוס בקוד החי |
|---|---|
| XSS ב-`user.name` | ✅ תוקן — `esc(user.name)` |
| השלמת יום בלי וידאו/משימה | ✅ תוקן — `videoOk`/`taskOk` מותנים בקיום |
| XSS בהערות אישיות | 🔴 **עדיין פתוח** — `${noteText}` בתוך `<textarea>` בשתי נקודות |
| כניסת אדמין ללא אימות | 🔴 **עדיין פתוח** — `HARDCODED_ADMINS` בצד לקוח |
| חוקי Firestore | 🔴 **החמיר** — התפוגה הוסרה, אבל כעת `allow read, write: if true` לצמיתות |

שתי הערות על האחרון: התפוגה שהזהרתי מפניה **אכן הפילה את הפלטפורמה ב-23.7** (מתועד בהערות הכלל עצמו). והקולקציה `blockedLeads` מכילה מידע פסיכיאטרי מזוהה וקריאה לכל מי שיש לו את הקישור — זו הדחופה ביותר לסגירה, לפני כל פרסום ברשתות.

---

*סדר עדיפות מומלץ ל-Claude Code: 7 → 8 → 10, ואז השאר (1–6 נסגרו). ממצאים 3 ו-9 ממתינים להחלטת OC.*

---

# סבב ביקורת · 27.7.2026 10:00

נסקרו קומיטים `ca553ac`…`182c71d` (11 קומיטים של סבב הלילה, ענף `site-complete`).
קראתי את ה-diff המלא של `js/motion.js`, `js/main.js`, `index.html`, `css/style.css`
והסקריפטים החדשים — לא רק את הודעות הקומיט.

## מה הורץ בפועל

| בדיקה | תוצאה |
|---|---|
| `node --check` על כל js/ · scripts/ · tests/ | ✓ נקי |
| `verify-program.mjs` מול `shift platform main` | ✓ 21/21 |
| `npm test` (motion + structure) | ✓ 21/21, 30ms |
| נכסי `hero-bg-a` וכל המופנים ב-CSS/HTML | ✓ קיימים |
| פונטים/צבעים נעולים | ✓ Assistant בלבד, שלושת הצבעים במקומם |
| גיבויי `vh` ל-`100svh` (ממצא T8) | ✓ בכל 4 המקומות |
| R1 — hush/lit לפני ה-early-return + onScreen ב-IO | ✓ תוקן נכון, מכוסה בבדיקה |
| R2 — `build()` נבנה מחדש ב-resize (debounce 180ms) | ✓ תוקן נכון, מכוסה בבדיקה |

`tests/motion.test.mjs` — כל בדיקה מתעדת באג שקרה בפועל — זה בדיוק הכלי שביקשתי
ב-T5/M. עבודה טובה מאוד בסבב הזה; שני הממצאים החדשים למטה הם תוצרי לוואי שלו.

## 🔴 קריטי

*(אין ממצא קריטי חדש בקוד. פריט N — 8.2ש' ב-Slow 4G — נשאר פתוח וממתין להחלטת OC,
כמתועד ב-HANDOFF וב-REQUESTS.)*

## 🟡 חשוב

### R4. `node_modules/` הוכנס ל-git — ‎52MB, ‎4,206 קבצים
`.gitignore` לא כולל `node_modules`, וקומיטי הלילה הכניסו את כל puppeteer + התלויות
(‎~800K שורות ב-diff). `.git` עומד עכשיו על ‎161MB. זה R3 בהגדלה של פי 13: כל clone
ו-push סוחבים את זה לנצח, וה-diff בין סבבים הופך בלתי קריא.
**תיקון:** להוסיף `node_modules/` ל-`.gitignore`, `git rm -r --cached node_modules`,
ולוודא ש-`package-lock.json` (שכן שייך ל-git) מספיק לשחזור — הוא מספיק.
ההיסטוריה שכבר תפחה תישאר; ניקוי היסטוריה הוא החלטת OC, לא חובה.

### R5. ✅ תוקן 27.7 (T9) — היציאה עברה ל-CSS, נעילת הגלילה נמחקה כליל, ושחרורי ה-overflow העיוורים הוסרו. נמדד לפני/אחרי בשלוש רשתות: 89–101 פריימים נעולים → 0. בדיקת רגרסיה נוספה. ה-`onload` על `#brainStill` מוחק את מסך הפתיחה לכולם — גם ברשת מהירה
`index.html` שורה ~129: ברגע שתמונת הגיבוי נטענת, ה-`onload` מוסיף `preloader-done`,
ו-CSS (`display:none` על `.preloader`) מעלים את מסך הפתיחה מיידית. ברשת איטית זה
בדיוק התיקון הנכון (פריט N). אבל ברוב הביקורים — broadband או cache — התמונה נטענת
תוך עשרות מילישניות, כלומר:
1. **אנימציית הכניסה של הלוגו (שלב 6, "אנימציית הלוגו חוזרת") לא תוצג לאיש** —
   `motion.js` ימשיך להריץ אותה על אלמנט מוסתר.
2. **נעילת גלילה יתומה:** ה-`onload` משחרר `overflow`, ואז `motion.js` (שנטען אחריו)
   מחזיר `overflow:hidden` עד `finishReveal` — שנייה-שתיים שבהן העמוד נראה, אין
   מסך פתיחה, והגלילה לא עובדת בלי שום סימן למה.
**תיקון מוצע:** ב-`motion.js`, לפני בניית ה-timeline — אם `preloader-done` כבר על
ה-html, לקרוא ל-`finishReveal()` ולא לנעול גלילה. זה משמר את התיקון לרשת איטית
ומחזיר את הכניסה המלאה למי שהספיק להגיע לפני התמונה... שזה אף אחד — ולכן נשאלת
גם שאלה עיצובית ל-OC: האם מוותרים על רגע הפתיחה לגמרי? אם כן — למחוק את קוד
ה-preloader במקום להשאיר אנימציה שרצה בחושך.

## 🟢 שיפור

- `verify-program.mjs` בלי ארגומנט מחפש נתיב יחסי ל-`$HOME` — בסביבת ה-sandbox של
  Cowork זה נכשל וצריך נתיב מפורש. לשקול משתנה סביבה או נתיב יחסי לריפו.
- ה-IO החדש של `onScreen` (threshold 0) מסמן "על המסך" גם כשהצלילה רק מציצה מלמטה —
  רחב מהטווח הקודם (`top top`→`bottom top`). לא באג: `hush`/`lit` ממילא מותנים ב-`p`,
  אבל `render()` נקרא קצת יותר. לא דורש טיפול, מתועד לזיכרון.

## מה נבדק ונמצא תקין בקוד החדש

הטעינה בשלבים (פריים 1 → חלון של 6) — נכונה ומכוסה בבדיקה. תיקון R1 כפול (סדר +
מעבר ל-IO בגלל קפיצות עוגן) — האבחנה על `onToggle` שלא יורה בקפיצה נכונה וחשובה.
תיקון `ch` בעברית (24→0 ממצאים) — אומת דרך `npm run type` קיים. `aria-label` על
נקודות הניווט — נכון. הקטנת טיימרים 4000→2600/2200 — עקבית עם R5 אבל תלויה בו.

## מה עוד נשאר לעשות

- **R5** — לפני הכל: זה שינוי התנהגות שנוגע לכל מבקר, והוא בן ~5 שורות.
- **R4** — מיד אחריו; ככל שמחכים ההיסטוריה תופחת.
- **פריט N** — ממתין להחלטת OC (לדלל פריימים או לקצץ ב-Lenis).
- **פריט E** — ממתין לאייפון של OC.
- **D1 השלמה + D2–D7** — התור העיצובי טרם התחיל ברצינות.
- שלוש הבקשות ב-`REQUESTS.md` פתוחות — שתיים מהן החלטות עיצוב.

---

# סקירת Cowork — 27.7.2026 12:06 · `site-complete` · קומיט אחרון: `747f3a6`

**מה נסקר:** `fba32b8` (T9/R5) + `747f3a6` (B1), כולל ה-diff המלא. בזמן הסקירה
Claude Code באמצע commit של B2 — ‏`.git/index.lock` חי (נוצר 12:04), בעץ העבודה
כבר `COUNT = 60` ו-60 פריימים ב-`assets/brain-seq/d/` (מקור מלא ב-`_build/brain-seq-d97`,
97 קבצים). קצב העבודה תקין: שלושה פריטי-B ביום עד הצהריים.

**מה אומת בפועל (בסביבת Cowork):**
- `node --check` על `main.js` + `motion.js` — עובר.
- `verify-program.mjs` מול המקור החי (`shift platform main`) — **21/21 תואמים**.
- `npm test` — **23/23** (כולל שתי בדיקות הרגרסיה החדשות של T9 ו-B1).
- כל נכס שמופנה אליו מ-HTML/CSS/JS קיים על הדיסק, כולל אחרי הדילול (0 חסרים).
- הפריימים החדשים רציפים f001–f060 (דסקטופ) / f001–f065 (מובייל) ותואמים ל-COUNT.
- ⚠️ `overlap` / `audit` / `budget` / `console` דורשים Chrome — אין בסביבת ה-sandbox
  שלי. Claude Code חייב להמשיך להריץ `npm run check` בעצמו; אני מאמת קוד וסטטיקה.

## 🟡 חשוב

### R6. שחרור ה-overflow העיוור שהוסר מה-HTML — עדיין קיים ב-`motion.js`, פעמיים
קומיט T9 כתב: "שחרורי ה-overflow העיוורים הוסרו מה-HTML — הם היו דורסים את נעילת
התפריט". נכון — אבל אותו שחרור עיוור נשאר ב-`motion.js`:
- שורה ~130: `dismissPreloader()` עושה `document.body.style.overflow = ''`
- שורה ~940: מטפל הקריסה עושה אותו דבר
תרחיש: רשת איטית, `main.js` כבר חי, המבקר פותח את התפריט (שנועל `overflow:hidden`),
ואז הפריים הראשון נטען ← `finishReveal()` ← הנעילה של התפריט נמחקת. תפריט פתוח
עם עמוד שנגלל מאחוריו. **תיקון:** למחוק את שתי השורות — מאז T9 אף אחד חוץ מהתפריט
לא נוגע ב-`overflow`, אז אין מה "לשחרר". שורת המטפל-בקריסה — להשאיר רק אם עוטפים
בבדיקה שהתפריט סגור. נוסף כ-T11 ב-`BACKLOG-tech.md`.

## 🟢 שיפור / לזיכרון

- **B1 נעשה נכון ברמת התהליך:** המדידה (`stackFits`) רצה מחדש ב-resize (debounce 180),
  `load` ו-`document.fonts.ready`, וה-teardown הורג את ה-tweens ומנקה props. זה בדיוק
  סוג הרגרסיה של R2 — והלקח נלמד. `scripts/overlap.mjs` חובר ל-`npm run check`.
- **מוקש שנוטרל בעץ העבודה:** `assets/brand/logo.png` היה ב-`.gitignore` בזמן שהוא
  בשימוש חי — ניקוי `--cached` עתידי (כמו T10!) היה מפיל אותו מהמאגר בשקט. הוסר
  מהרשימה עם הערה. טוב שנתפס **לפני** ביצוע T10.
- **workflow הפריסה** עכשיו מסנן לקבצי ריצה בלבד (בלי `_build`, scripts, tests, מסמכים,
  node_modules). נכון עצמאית מ-T10, ומקטין את מה שעולה לאוויר.
- **לעקוב בסבב הבא:** ב-index יש rename ביניים של `d/` → `d97-tmp/` (תיקייה ריקה על
  הדיסק). אם אחרי הקומיט של B2 התיקייה `d97-tmp` עדיין קיימת ב-git או על הדיסק — שארית
  שצריך לנקות. וכן: `git add` על מאות webp עם `.git` של 161MB הוא איטי — אם ה-lock
  מ-12:04 עדיין שם בסבב הבא בלי קומיט חדש, זה lock תקוע שחוסם את הריפו (להסיר ידנית
  רק אחרי אימות שאין תהליך git רץ).

## מה עוד נשאר לעשות
B2 — להשלים commit + מדידה לפני/אחרי ב-`PERF.md` · B3 — שתי גרסאות סיום צלילה ל-OC ·
T10/B5 — `node_modules` עדיין לא ב-`.gitignore` (נכון ל-12:06) · T11/R6 החדש ·
B6 חיסון · B7 `LAUNCH.md` · פריט E — עדיין ממתין לאייפון של OC.

---

# סקירת Cowork — 27.7.2026 14:05 · `site-complete` · קומיט אחרון: `e8d50e0` (13:35)

**מאז 13:30:** שלושה קומיטים (13:32–13:35): D1 ‏#5+#6 בוצעו, B3-א (הווילון) חוּוט חי,
החלטת Lenis תועדה (נשאר — 13KB בפועל, לא 30KB). מאז 13:35 — **שקט (~30 דק')**;
פריטים 2–26 בתור לא נגעו. אומת: tests ‏23/23 · verify ‏21/21 · syntax · כל הסקשנים
קיימים · כל 26 הפניות הנכסים קיימות על הדיסק.

## 🔴 קריטי

- **רגרסיה חדשה מ-B3: מצב מופחת-תנועה שובר את ניגודיות ההגעה.**
  `html.ending-curtain` (סטטי ב-`index.html` שורה 2) צובע את `h1`/`arrival-sub`
  בלבן. אבל ב-`prefers-reduced-motion`: הווילון `display:none` (style.css ‏1900),
  הפריים המוצג הוא **המואר** (`motion.js` ‏251: `f{COUNT}` — "הפריים המואר"),
  וה-veil מקבל רקע נייר בהיר 0.72 (‏1645). תוצאה: כותרת לבנה על רקע בהיר —
  בלתי-קריאה, בדיוק ברגע ה-CTA. ההערה ב-CSS ("ההגעה סטטית על רקע הנייבי")
  סותרת את הקוד בפועל. **תיקון:** לעטוף את חוקי הצבע של `ending-curtain`
  ב-`@media (prefers-reduced-motion: no-preference)`, או override במצב שקט
  שמחזיר טקסט כהה. ולבדוק גם את מסלול `!FULL` (כשל CDN של GSAP) — אותו פריים מואר.

## 🟡 חשוב

- **`.git/index.lock` תקוע (נוצר 14:03 ע"י ה-git של הבודק בסביבת ה-sandbox;
  אין לו הרשאה למחוק אותו).** קובץ 0 בייט. Claude Code: לפני הקומיט הבא —
  לוודא שאין תהליך git רץ ולמחוק `.git/index.lock` ידנית. (לקח לבודק: מעכשיו
  `git --no-optional-locks` בלבד.)
- **B3 חוּוט למרות שהיה ברשימת 🔒.** הקומיט (`ea5565c`) וגם העריכה ב-REQUESTS.md
  מעידים "OC הורה להמשיך בלי אישורים" — עדות-עצמית של Claude Code שאין לי דרך
  לאמת. הביצוע עצמו נקי (שכבה נפרדת, הפיך במחלקה אחת, ניגודיות נמדדה 15.4/7.2+),
  ותנאי האישור של 13:30 קוימו (overlap/type/a11y/צילומים — `e8d50e0`).
  **ל-OC בשובו:** לאשר בדיעבד את הווילון או revert (`ending-curtain` → הסרה מ-html).
- **Claude Code עצר אחרי פריט 1 מתוך 26.** אם זה בגלל ה-lock — ראה למעלה;
  אם לא — להמשיך בתור של 13:30 (החל מפריט 2/3: DB, ואז תשתית האתר הסטטי).

## 🟢 שיפור

- D1 ‏#6 בוצע יפה: הטקסטים לא שונו (אומת ב-diff מילה-במילה), היררכיית כותרות תקינה.
- מובייל: `.story-body { order:-1 }` יוצר פער סדר-חזותי/סדר-DOM (קורא-מסך פוגש את
  התמונה לפני הטקסט). זניח כי לתמונה אין תוכן טקסטואלי מהותי — לשקול היפוך ב-DOM
  בסבב עתידי במקום order.
- נסגרו מהסקירות הקודמות: ✅ `node_modules` ב-`.gitignore` · ✅ `d97-tmp` איננה ·
  ✅ B2 ‏(97→60, ‏60 קבצים על הדיסק) · ✅ T11/R6.

## מה עוד נשאר לעשות
תיקון ה-🔴 (מופחת-תנועה) · מחיקת index.lock · המשך התור מפריט 2 · הכרעות OC
הפתוחות: אישור B3 בדיעבד, Lenis (הומלץ להשאיר), דומיין, פרטי קשר, תוכן אירועים, אייפון (E).

---

# סקירת Cowork — 27.7.2026 16:05 · `site-complete` · קומיט אחרון: `e8d50e0` (13:35)

**מאז 14:05: אפס קומיטים. Claude Code לא רץ כבר ~2.5 שעות** (עצר 13:35, אחרי
פריט 1 מ-26). פריט 0 (מחיקת ה-lock), פריט 0.5 (תיקון ה-🔴) והתור 2–26 — לא נגעו.

**אומת מחדש בסבב הזה:** tests ‏23/23 · verify-program ‏21/21 מול המקור החי ·
`node --check` על כל js · שבעת הסקשנים קיימים · כל הפניות הנכסים קיימות על הדיסק.
הקוד עצמו יציב — הבעיה היא שאף אחד לא עובד עליו.

## 🔴 קריטי (עומד מ-14:05, לא טופל)

- **רגרסיית מופחת-תנועה של B3 עדיין בקוד.** אומת שוב הפעם: `ending-curtain`
  סטטי ב-`index.html:2` ← ‏h1 לבן (‏style.css:1908); במופחת-תנועה הווילון
  `display:none` ‏(1900), הפריים הוא המואר (‏motion.js:251) וה-veil נייר-בהיר
  ‏(1645). לבן על בהיר ברגע ה-CTA. התיקון המוצע ב-NEXT (פריט 0.5) עומד.

## 🟡 חשוב

- **הריפו עדיין נעול:** `.git/index.lock` ‏(0 בייט, 14:03) קיים; גם בסבב הזה אין
  לבודק הרשאת מחיקה מה-sandbox ‏(`Operation not permitted`). **רק Claude Code
  (או OC) יכול למחוק אותו.** עד אז שלושת קבצי התיאום (NEXT/REVIEW/BACKLOG-design)
  נשארים כשינויים לא-מקומטים בעץ העבודה.
- **דורש פעולה של OC, לא של סוכן:** אין סימן ש-Claude Code מתוזמן לרוץ. אם הכוונה
  שהוא ימשיך היום — צריך להריץ אותו ידנית, ושהפעולה הראשונה שלו תהיה מחיקת ה-lock.

## 🟢

- אין ממצאים חדשים בקוד — לא היה diff לבדוק.

## מה עוד נשאר לעשות
כמו ב-14:05: תיקון ה-🔴 · מחיקת index.lock · תור 13:30 מפריט 2 · הכרעות OC
(B3 בדיעבד · Lenis · דומיין · פרטי קשר · אירועים · אייפון E).

---

# סקירת Cowork — 27.7.2026 18:05 · `site-complete` · קומיט אחרון: `e8d50e0` (13:35)

**מאז 16:05: אפס קומיטים ואפס שינויי קבצים (אומת ב-find — שום קובץ לא נגע מאז
16:10). זהו הסבב השלישי ברציפות שבו Claude Code לא רץ — ~4.5 שעות שקט.**

**אומת מחדש בסבב הזה:** tests ‏23/23 · verify-program ‏21/21 מול המקור החי
(`shift platform main /shift-platform/קבצי קוד/data.js`) · `node --check` נקי על
כל js · שבעת הסקשנים קיימים ב-index.html · ה-🔴 אומת שוב בקוד (style.css ‏1908,
‏1900, ‏1645 · motion.js ‏251 · index.html:2) — עומד בדיוק כפי שתועד ב-14:05.

## 🔴 קריטי (עומד מ-14:05 — סבב שלישי ללא טיפול)

- רגרסיית מופחת-תנועה של B3: כותרת הגעה לבנה על רקע בהיר ברגע ה-CTA.
  התיקון המוצע (עטיפת חוקי `ending-curtain` ב-`@media (prefers-reduced-motion:
  no-preference)`) מפורט ב-NEXT פריט 0.5.

## 🟡 חשוב

- **הריפו עדיין נעול** (`.git/index.lock`, ‏0 בייט, 14:03). גם בסבב הזה — `rm`
  נכשל ב-`Operation not permitted` מה-sandbox. רק Claude Code או OC יכולים למחוק.
- **הסלמה ל-OC:** שלושה סבבים ללא פעילות = הצינור מת, לא מאט. אין טעם בהנחיות
  נוספות לסוכן שלא רץ; נדרשת התנעה ידנית או מחיקת ה-lock ביד (פירוט ב-NEXT).

## 🟢

- אין ממצאים חדשים — לא היה diff לבדוק. שלושת קבצי התיאום עדיין לא-מקומטים
  בעץ העבודה (חסומים ע"י ה-lock); התוכן שלהם שלם ועדכני.

## מה עוד נשאר לעשות
ללא שינוי מ-14:05: תיקון ה-🔴 · מחיקת index.lock · תור 13:30 מפריט 2 ·
הכרעות OC: אישור B3 בדיעבד · Lenis (הומלץ להשאיר) · דומיין · פרטי קשר ·
תוכן אירועים · אייפון (E).

---

# סקירת Cowork — 27.7.2026 20:05 · `site-complete` · קומיט אחרון: `e8d50e0` (13:35)

**מאז 18:05: אפס קומיטים ואפס שינויי קוד — הסבב הרביעי ברציפות. ~6.5 שעות
מאז הקומיט האחרון. הקבצים היחידים שנגעו מאז 16:10 הם קבצי התיאום של Cowork.**

**אומת מחדש בסבב הזה:** tests ‏23/23 · verify-program ‏21/21 מול המקור החי ·
`node --check` נקי על כל `js/*.js` ו-`scripts/*.mjs` · ניסיון `rm .git/index.lock`
נכשל שוב (`Operation not permitted`).

## 🔴 קריטי (עומד מ-14:05 — סבב רביעי ללא טיפול)

- רגרסיית מופחת-תנועה של B3: כותרת הגעה לבנה על רקע בהיר ברגע ה-CTA.
  התיקון המוצע ב-NEXT פריט 0.5 עומד כלשונו.

## 🟡 חשוב

- **הריפו עדיין נעול** (`.git/index.lock`, ‏0 בייט, 14:03). רק Claude Code או OC
  יכולים למחוק.
- **הסלמה חוזרת ל-OC:** הצינור מושבת מאז הצהריים. אין ערך בסבבי ביקורת נוספים
  עד שמישהו מתניע את Claude Code או משחרר את ה-lock ביד — הממצאים רק משוכפלים.

## 🟢

- אין ממצאים חדשים — לא היה diff לבדוק.

## מה עוד נשאר לעשות
ללא שינוי מ-14:05: תיקון ה-🔴 · מחיקת index.lock · תור 13:30 מפריט 2 ·
הכרעות OC: אישור B3 בדיעבד · Lenis (הומלץ להשאיר) · דומיין · פרטי קשר ·
תוכן אירועים · אייפון (E).

---

# סקירת Cowork — 27.7.2026 22:05 · קומיט אחרון: `e8d50e0` (13:35)

**מאז 20:05: אפס קומיטים ואפס שינויי קוד — סבב חמישי ברציפות. ~8.5 שעות מאז
הקומיט האחרון. הקבצים היחידים שהשתנו הם קבצי התיאום של Cowork.**

**אומת מחדש בסבב הזה:** tests ‏23/23 · verify-program ‏21/21 מול המקור החי ·
`node --check` נקי על כל `js/*.js` ו-`scripts/*.mjs` · שבעת הסקשנים קיימים
ב-index.html · ניסיון `rm .git/index.lock` נכשל שוב (`Operation not permitted`).

## 🔴 קריטי (עומד מ-14:05 — סבב חמישי ללא טיפול)

- רגרסיית מופחת-תנועה של B3: כותרת הגעה לבנה על רקע בהיר ברגע ה-CTA.
  התיקון המוצע ב-NEXT פריט 0.5 עומד כלשונו.
  **⚠ תוקן חלקית ב-`dcd5895` — נפתח מחדש בסבב 00:05 (ראה למטה): הצבעים
  גודרו נכון, אבל החלפת הלוגו נשברת בקסקדה.**

## 🟡 חשוב

- ✅ **הריפו עדיין נעול** — שוחרר; קומיטים התחדשו ב-23:42 (`dcd5895`).
- ✅ **הסלמה ל-OC (חוזרת)** — הצינור חזר לעבוד; Claude Code רץ ברצף מאז 23:42.

## 🟢

- אין ממצאים חדשים — לא היה diff לבדוק.

## מה עוד נשאר לעשות
ללא שינוי מ-14:05: תיקון ה-🔴 · מחיקת index.lock ✅ · תור 13:30 מפריט 2 ·
הכרעות OC: אישור B3 בדיעבד · Lenis (הומלץ להשאיר) · דומיין · פרטי קשר ·
תוכן אירועים · אייפון (E).

---

# סקירת Cowork — 28.7.2026 00:05 · קומיט אחרון: `6e8a0dd` (00:00)

**הצינור חזר לחיים.** מאז 23:42: הריפו שוחרר, ו-Claude Code סגר את פריטים
0, 0.5, 2 (DB), 3+4, 5, 6+7 בחמישה קומיטים תוך 18 דקות. בנוסף שלושה קבצים
לא-מקומטים בעבודה (`scripts/vr.mjs`, `scripts/perf-diff.mjs`, `scripts/lib/png.mjs`)
— פריטים 8–9 באמצע ביצוע. קצב מצוין.

**אומת בסבב הזה (ב-sandbox, בלי כרום — audit/console/motion/overlap/budget
לא ניתנים להרצה כאן; Claude Code מדווח עליהם ירוקים מקומית):**
tests ‏27/27 (כולל 4 בדיקות האבטחה החדשות של פריט 7) · verify-program ‏21/21
מול המקור החי (`shift platform main`) · `node --check` נקי על כל הקבצים שהשתנו
והלא-מקומטים · שבעת הסקשנים קיימים · כל הפניות הנכסים המקומיות ב-index.html
וב-404.html קיימות על הדיסק (כולל האייקונים החדשים) · JSON-LD תקין ומכיל רק
עובדות מהעמוד · robots.txt + sitemap.xml תקינים · 404.html עצמאי לחלוטין,
בפלטה הנעולה.

## 🔴 קריטי — פריט 0.5 נפתח מחדש: החלפת הלוגו במופחת-תנועה לא עובדת

`css/style.css` שורות 1902–1911. הבלוק החדש:

```css
@media (prefers-reduced-motion: reduce) {
  html.ending-curtain .arrival-logo-dark { display: none; }   /* שורה 1903 */
  ...
}
html.ending-curtain .arrival-logo-dark { display: block; ... } /* שורה 1906 */
```

media query לא מוסיף ספציפיות. שני הסלקטורים זהים (0,2,1) — והכלל הלא-מגודר
בשורה 1906 מאוחר יותר במקור, ולכן **מנצח גם במופחת-תנועה**: הלוגו הלבן מוצג
`display:block`, בנוסף ללוגו הנייבי מהבלוק המגודר. התוצאה במצב שקט: שני
לוגואים זה מעל זה, הלבן בלתי-נראה על הרקע הבהיר עם הילת drop-shadow תכלת
מרחפת באוויר. גידור הצבעים של הטקסט (שורות 1914+) — נכון ועובד.

**תיקון (שורה אחת של הזזה):** לעטוף גם את הכלל בשורות 1906–1911 ב-
`@media (prefers-reduced-motion: no-preference)` — או להעביר את בלוק ה-reduce
אל אחרי שורה 1911. אחרי התיקון: לאמת ידנית ששני המצבים נכונים, ולהוסיף
בדיקה ל-structure/motion שתופסת סדר-קסקדה כזה (הרגרסיה הזו שרדה "תיקון"
אחד — היא בדיוק הסוג שחוזר).

## 🟡 חשוב

- **`verify-program.mjs` נפל על נתיב ברירת-המחדל בסביבת ה-sandbox** (מצפה
  ל-`~/Desktop/...`). רץ תקין עם נתיב מפורש. כדאי להוסיף fallback של משתנה
  סביבה `DATA_JS` — שורה אחת, יחסוך תקלת-שווא בכל סבב ביקורת.

## 🟢 שיפור

- `tests/security.test.mjs` שורה 15: מקלדת התחלפה באמצע מילה — "חיצוniים".
  קוסמטי (שם בדיקה), לתקן בהזדמנות.
- `404.html` מכריז `font-family: 'Assistant'` אבל לא טוען את הגופן (עצמאות
  מכוונת ומתועדת) — בפועל ירונדר ב-fallback של המערכת. מקובל; אם רוצים את
  Assistant גם ב-404, אפשר `<link>` לגוגל-פונטס בלי לפגוע בעצמאות מ-CSS מקומי.
- בחירת `twitter:card=summary` לתמונה המרובעת — נכונה; לעבור ל-
  `summary_large_image` רק עם תמונת 1200×630 (פריט 38, כבר בתור).

## מה עוד נשאר לעשות
תיקון ה-🔴 (קודם לכל) · המשך התור מפריט 8 (כבר בעבודה) · גל שני 27–41 ·
NIGHT-REPORT.md לפני 08:00 · הכרעות OC ללא שינוי: B3 בדיעבד · שלושת מועמדי
הרפרנסים (BACKLOG-design) · דומיין · פרטי קשר · תוכן אירועים · אייפון (E).

---

# סקירת Cowork — 28.7.2026 02:05 · קומיט אחרון: `ea6634d` (01:31)

**לילה יוצא דופן: 35 קומיטים מאז 00:05, התור (2–41) נסגר כולו עד 01:31.**
כולל תיקון ה-🔴 (0.5 סיבוב ב'), ה-🟡 (DATA_JS) ושני ה-🟢 מהסקירה הקודמת.

**אומת בסבב הזה (ב-sandbox, בלי כרום — vr/e2e/faults/coverage/matrix/perf
לא ניתנים להרצה כאן; Claude Code מדווח עליהם ירוקים):**
tests ‏37/37 · verify-program ‏21/21 מול המקור החי (עם `DATA_JS` — ה-fallback
החדש עובד) · `node --check` נקי על כל ה-JS שהשתנה (192 קבצים ב-diff) ·
בדיקת עשן jsdom: שבעת הסקשנים קיימים ולא-ריקים · כל הפניות הנכסים
(‏index/404/style/fonts.css/manifest) קיימות על הדיסק, כולל og.png והאייקונים ·
סטי AVIF שלמים 1:1 (‏d ‏60/60 · m ‏65/65) · ‏VR baseline: ‏13 צילומים בריפו.

**תיקון ה-🔴 אומת בקוד:** הכלל של `arrival-logo-dark` עטוף עכשיו
ב-`no-preference` (style.css ‏~1951) עם הערה מתעדת, ונוספה בדיקת-קסקדה
סטטית ב-`structure.test.mjs` (שורות ‏163–175) שתתפוס הישנות. סגור סופית. ✅

**קריאת רגרסיות על ה-diff המרכזי — נקי:** ‏fonts `loadingdone` (ולא
`fonts.ready` — מנומק נכון) · ‏bfcache `pageshow` · בדיקת ה-AVIF האמפירית עם
נסיגה בטוחה ל-WebP ופריים 1 נשאר על ה-preload · הנשימה הקומפקטית היא
media queries טהורות (עמידה ב-resize) · ‏setStack מחושב מחדש ב-resize/load/גופנים ·
תאי ה-safe() לא בולעים שגיאות בשקט (console.error נשמר).

## 🟡 ✅ (נסגר 28.7 בבוקר, `3d99d31`) חשוב — חותמות זמן שגויות במסמכי הלילה

`HANDOFF.md` נושא כותרת "~04:25" ו-`NIGHT-REPORT.md` "נחתם ~04:20" — אבל
שניהם נכתבו ב-01:30 (קומיט `7142d64`; השעה בפועל עכשיו 02:05). כשה-audit
trail אומר "נחתם 04:20" ו-git אומר 01:30, הקורא בבוקר לא יודע למי להאמין.
תיקון של שתי שורות כותרת. **← שתי הכותרות תוקנו ל"01:30/01:31 (לפי git)".**

## 🟢 שיפור

- ✅ (נסגר 28.7 בבוקר, `3d99d31`) `js/main.js` — ‏`track()` קורא ל-`performance.now()` בלי שומר (‏motion.js
  כן שומר). מוכל בתא safe(), אבל שורת-גידור אחת תיישר את הקו.

## מה עוד נשאר לעשות
תיקון חותמות הזמן (🟡) · לולאת הרקע עד הבוקר (ליטוש כיסוי, לפי HANDOFF) ·
כל השאר — הכרעות OC בבוקר, מרוכזות ב-NIGHT-REPORT §בצד: ‏B3 · שלושת מועמדי
הרפרנסים · ‏hero-bg-b · ארכוב delivery · אנליטיקס · דומיין/קשר/אירועים/אייפון.

---

# סקירת Cowork — 28.7.2026 04:05 · קומיט אחרון: `ea6634d` (01:31)

## 🔴 קריטי — Claude Code עצר. אפס קומיטים מזה שעתיים וחצי

הקומיט האחרון: `ea6634d` ב-01:31. הסקירה הקודמת (02:05) השאירה תור עבודה
מפורש ב-NEXT.md — שני תיקונים קטנים + לולאת רקע עד הבוקר, לפי הוראת OC
"עבודה רציפה עד הבוקר" — ושום דבר ממנו לא בוצע. `git status` נקי (רק
קבצי התיאום שלי). זו לא תקלה בקוד; זו תקלת תהליך: הסוכן כנראה סיים
סשן ולא הופעל מחדש.

**ההשלכה:** ריצת `npm run check` הטרייה שתוכננה סמוך ל-06:00 לא תקרה
מעצמה, ושני הפריטים הפתוחים מ-02:05 עדיין פתוחים:
- 🟡 חותמות זמן שגויות ב-`HANDOFF.md` ("~04:25") וב-`NIGHT-REPORT.md`
  ("נחתם ~04:20") — שניהם נכתבו ב-01:30. עדיין לא תוקן.
- 🟢 שומר ל-`performance.now()` ב-`track()` — עדיין לא נוסף.

## אומת בסבב הזה (על אותו קומיט — אין diff חדש)

- `verify-program` מול המקור החי: ‏21/21 ✓ (עם `DATA_JS`)
- `npm test`: ‏37/37 ✓
- אין רגרסיות חדשות לחפש — אפס שינויי קוד מאז 02:05.
- worktree `shift-website-tech` לא קיים — סוכן המכניקה לא רץ בנפרד
  (עקבי עם המצב מאתמול; כל העבודה על `site-complete`).

## מה עוד נשאר לעשות
להפעיל מחדש את Claude Code (דורש את OC או את מריץ-הסשנים) · שני הפריטים
מ-02:05 · `npm run check` מלא לפני הבוקר · הכרעות OC — מרוכזות
ב-NIGHT-REPORT §בצד, בלי שינוי.

---

# סקירת Cowork — 28.7.2026 06:05 · קומיט אחרון: `ea6634d` (01:31)

## 🔴 קריטי — העצירה נמשכת: אפס קומיטים מזה 4.5 שעות

הקומיט האחרון עדיין `ea6634d` מ-01:31. גם הסקירה של 04:05 (🔴 לעיל) וגם
תור ה-02:05 לא נגעו — Claude Code לא הופעל מחדש. הוראת OC הייתה "עבודה
רציפה עד הבוקר"; בפועל העבודה נעצרה ב-01:31. שלושת הפריטים הפתוחים:
- 🟡 חותמות זמן שגויות ב-`HANDOFF.md` ("~04:25") וב-`NIGHT-REPORT.md`
  ("נחתם ~04:20") — נכתבו ב-01:30. עדיין לא תוקן (סבב שלישי ברצף).
- 🟢 שומר ל-`performance.now()` ב-`track()` — עדיין לא נוסף.
- ריצת `npm run check` מלאה סמוך ל-06:00 — לא בוצעה על-ידו.

## אומת בסבב הזה (אותו קומיט — אין diff חדש לקרוא)

- `verify-program` מול המקור החי (`DATA_JS`): ‏21/21 ✓
- `npm test`: ‏37/37 ✓
- בדיקת עשן jsdom על `index.html`: כל 7 הסקשנים קיימים ומאוכלסים ✓
  (‏#products 612 · ‏#method 1226 · ‏#habits 666 · ‏#program 5706 ·
  ‏#events 1078 · ‏#story 469 · ‏#contact 139 תווים)
- כל הנכסים המופנים מ-HTML/CSS קיימים על הדיסק ✓ (התראה יחידה —
  `url(#n)` של SVG, false positive של הסורק, לא קובץ)
- `node --check` על main.js ו-motion.js ✓
- ה-`check` המלא (VR/e2e/faults, ~4.4 דק׳, דורש כרום) לא ניתן להרצה
  מסביבת הבודק — חייב לרוץ אצל Claude Code כשיחזור. **זה הפריט הראשון שלו.**

## מה עוד נשאר לעשות
להפעיל מחדש את Claude Code — בלי זה שום דבר לא יזוז · `npm run check`
מלא מיד כשחוזר, ואז שני הפריטים מ-02:05 · הכרעות OC של הבוקר —
מרוכזות ב-NIGHT-REPORT §בצד + ‏REQUESTS.md, בלי שינוי.

---

# סקירת Cowork — 28.7.2026 08:05 · קומיט אחרון: `ea6634d` (01:31)

## 🔴 ✅ (נסגר 28.7 ‏09:52 — הסשן הופעל מחדש ע"י OC) קריטי — העצירה נמשכת: אפס קומיטים מזה 6.5 שעות (סבב שלישי ברצף)

> **סטטוס 09:57:** שלושת הפריטים הפתוחים בוצעו — חותמות הזמן + שומר
> `performance.now` ב-`3d99d31`, ו-`npm run check` מלא רץ טרי:
> **13/13 ירוק · 4.5 דק' · אפס כשלים** (כולל vr/faults/e2e בכרום).

הקומיט האחרון עדיין `ea6634d` מ-01:31. הסקירות של 04:05 ו-06:05 (🔴 לעיל)
לא נענו — Claude Code לא הופעל מחדש מאז שנגמר לו הסשן. הוראת OC הייתה
"עבודה רציפה עד הבוקר"; בפועל העבודה נעצרה ב-01:31, והבוקר כבר כאן.
**זו תקלת תהליך, לא תקלת קוד — נדרש OC כדי להפעיל את הסוכן מחדש.**
שלושת הפריטים הפתוחים מ-02:05 ללא שינוי (check מלא · חותמות זמן ·
שומר performance.now).

## אומת בסבב הזה (אותו קומיט — אין diff חדש לקרוא)

- `verify-program` מול המקור החי (`DATA_JS`): ‏21/21 ✓
- `npm test`: ‏37/37 ✓ (‏0 כשלים)
- בדיקת עשן jsdom על `index.html`: כל 7 הסקשנים קיימים ומאוכלסים ✓
  (ערכים זהים לסבב 06:05 — צפוי, אין שינוי)
- `node --check` על main.js ו-motion.js ✓
- ה-`check` המלא (VR/e2e/faults, דורש כרום) עדיין ממתין ל-Claude Code.

## מה עוד נשאר לעשות
**OC: להפעיל מחדש את Claude Code** — שלושה סבבים שהמסקנה זהה · מיד
כשחוזר: `npm run check` מלא + שני הפריטים הקטנים · הכרעות הבוקר של OC —
NIGHT-REPORT §בצד + ‏REQUESTS.md · המצב הטוב: הלילה נסגר ירוק (41 פריטים,
שער 13/13), אין שום דבר שבור — רק אין התקדמות.
