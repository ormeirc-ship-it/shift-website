# DELIVERY — אתר SHIFT, סבב "להביא למצב מוגמר"

**ענף:** `site-complete` · 9 קומיטים מעל `main` · לא נדחף לאתר החי, לא מוזג ל-main.
**להרצה מקומית:** `cd "קבצי קוד"`… (כאן: `python3 -m http.server` בתיקיית הפרויקט) ואז הדפדפן.
**צילומי המסך:** `delivery/` — 28 קבצים, 1440px ו-390px.

---

## 1. מה נעשה בכל שלב

### שלב 1 ✅ — הכניסה דרך המוח
ההירו הישן (רקע, אורורה, שדה נוירונים, פרלקסת עכבר, כניסה תו-אחר-תו) הוסר במלואו
והוחלף בצלילה: שער פתיחה → רשת נוירונים עם שלוש שורות שחולפות → פריצה אל האור,
ושם הכותרת "מהישרדות ליצירה".

- ההחלקה מאב-הטיפוס נשמרה בדיוק: `eased += (target - eased) * 0.14`, רצה על
  `gsap.ticker`. הקלט מגיע מ-ScrollTrigger בלבד — **אין מאזין scroll מקביל**.
- מובייל טוען רק `assets/brain-seq/m/` (65 פריימים) — נבדק ואומת ב-360 וב-390.
- מסך הפתיחה נסגר כשהפריים **הראשון** מוכן, לא כשכל הרצף נטען. רשת ביטחון:
  נפתח בכל מקרה אחרי 2.5 שניות, ועוד אחת ב-`<head>` אחרי 4.
- `.dive-still` — הפריים הראשון כ-`<img>` רגיל, כך שגם בלי JS או אם הרצף לא נטען
  יש תמונה ולא מסך ריק.
- דילוג "דלגו לתוכן" למקלדת, ראשון בסדר הטאב.

**שתי החלטות שלקחתי בלי לשאול** (היית בחוץ):
1. שלוש שורות המסע נשארו כפי שכתבת אותן. הפרומפט ביקש שאציע 2–3 חלופות — לא היה
   למי. הן מדויקות מדעית ומתאימות לניסוח המסויג, אז לא נגעתי.
2. המשפט "מהישרדות ליצירה" ירד ממסך הפתיחה ועבר לרגע ההגעה לאור. הוא לא יכול
   להופיע פעמיים באותה דקה בלי לשרוף את הפואנטה.

### שלב 2 ✅ — הסרת ארבע התבניות
- **10 לייבלים → 1.** נשאר "טעימה מתוך יום 1" בלבד, כי הוא היחיד שנושא מידע
  אמיתי: הוא מסמן שהתרגיל ציטוט מהמסלול ולא המצאה שיווקית.
- **מספור 01–07 → הוסר.** במקומו המספרים ב"הגישה — ארבעה צעדים" הוגדלו
  (2.4rem→3.8rem), שם הם רצף אמיתי.
- **`.grad-text` → הוסר.** ההגעה לאור היא על רקע בהיר, ולכן הכותרת שם היא צבע
  מלא ממילא. הכלל נמחק מה-CSS.
- **שתי רשתות קלפים זהות → אחת.** "הרגלים מעצבי תודעה" הפך לטריפטיך עריכתי.
  ההיגיון: סל המוצרים נשאר קלפים כי הם **יעדים ללחיצה**; שלושת ההרגלים הם
  עקרונות, ולקופסה אין שם תפקיד. (הפרומפט ביקש שאציע שתי חלופות — שוב, לא היה
  למי; זו הבחירה וזה הנימוק.)

### שלב 3 ✅ — המעבר מהישרדות ליצירה כמערכת
הצבע והטיפוגרפיה נעולים (פריטים 10–11), ולכן הקצב והמרחב נושאים את ההבחנה לבדם.
שלושה פרמטרים נעים יחד מ-0 ל-1 לאורך 11 סקשנים:

| פרמטר | למעלה | למטה | נמדד בפועל |
|---|---|---|---|
| רוחב עמודה | צר | נפתח | 913px → 1147px |
| מרווח אנכי | מכווץ | נדיב | 74px → 150px |
| משך הכניסות | מהיר | איטי | 0.6ש' → 1.2ש' |

ה-JS רק מסמן לכל סקשן `--j`; הרוחב והמרווח הם CSS טהור, כך שגם בלי מנוע התנועה
ובמצב מופחת-תנועה המרחב עדיין מספר את הסיפור. ההצהרה והסיום נשארים מרוכזים
ב-820px בכוונה — הם רגעי עצירה, לא פתיחה.

### שלב 4 ✅ — תרגיל הנשימה קיבל סקשן משלו
היה קבור מתחת ל-21 כרטיסי ימים. עלה לסקשן עצמאי אחרי "הרגלים מעצבי תודעה" —
מוקדם מספיק שיגיעו אליו, מאוחר מספיק שכבר יודעים מהו הרגל. כותרת חדשה:
"לפני שתקראו עוד משפט — קחו נשימה אחת". הרקע בהיר ולכן הצבעים הופכו.
**התוכן המדעי לא נגעתי בו** — אותן שלוש הוראות, אותו סייג בריאותי, אותו ציטוט.

### שלב 5 ✅ — ביצועים
- 6 תמונות → WebP, הוקטנו ל-1600px רוחב מקסימלי. **1941KB → 219KB בדיסק (-89%).**
  ה-JPG נשמרו כ-fallback דרך `<picture>` ו-`image-set()`.
- כל 13 התמונות קיבלו `width`/`height` אמיתיים → אין יותר קפיצות פריסה.
- הגופן כבר לא חוסם רינדור (`preload` + `media="print"`/`onload` + `<noscript>`).

### שלב 6 ✅ — הלוגו בספארי ובמובייל
החסימה הגורפת הוסרה. שני `<source>` (HEVC/MOV ואז WebM) — **אבל בלי לסמוך על
זיהוי דפדפן.** אחרי הטעינה מציירים פריים לקנבס ובודקים בפועל אם יש שקיפות:
פריים אטום-וחשוך = המלבן השחור שדיווחת עליו בעבר, והווידאו נזרק. גם ניגון שנחסם
מחזיר את הלוגו הסטטי. בדיקה אמפירית במקום ניחוש על מחרוזת UA.

### שלב 7 ✅ — ניקיון
- "איך עושים SHIFT" → **"הגישה — ארבעה צעדים"**. שני סקשנים נשאו את אותו שם
  והניווט הוביל רק לאחד.
- 12 נכסים לא בשימוש הוצאו מהמאגר (נשארו על הדיסק, נוספו ל-`.gitignore`).

### שלב 8 ✅ — מעבר מלא
נבדק ב-headless Chrome על **360 / 390 / 768 / 1440**:

| בדיקה | תוצאה |
|---|---|
| גלישה רוחבית | 0 בכל הרוחבים |
| קונסול (אחרי גלילה מלאה) | 0 שגיאות, 0 אזהרות |
| מעבר מקלדת | כל אלמנט ממוקד מקבל outline; הדילוג ראשון |
| `prefers-reduced-motion` | הצלילה מתכווצת ל-100vh, השער מוסתר, הכותרת גלויה מיד |
| בלי JavaScript כלל | 31 כותרות גלויות + תמונת הגיבוי של הצלילה |
| `verify-program.mjs` | **21/21 ✓** |
| שורות מעל 75 תווים | 0 (למעט 3 גבוליות ב-768px) |

---

## 2. צילומי מסך — `delivery/`

**ההירו בשלוש נקודות:** `hero-1-start` · `hero-2-mid` · `hero-3-light`
**כל סקשן ששונה:** `statement` · `products` · `method` · `habits` · `path` ·
`breath` · `program` · `events` · `story` · `outcomes` · `closing`
כל אחד ב-`-1440.png` וב-`-390.png`. סה"כ 28 קבצים.

---

## 3. מספרים אמיתיים (headless Chrome, מדידה ולא הערכה)

| מדד | ערך |
|---|---|
| פריים ראשון של המוח מוצג | **20–33ms** (חמש מדידות; מקומי) |
| כל הרצף טעון | **61–97ms** (מקומי) |
| משקל עמוד — לפני (main) | **1.49MB** |
| משקל עמוד — אחרי, דסקטופ | **3.33MB** |
| משקל עמוד — אחרי, מובייל | **1.55MB** |
| תמונות שנטענו בפועל | 1182KB → **43KB** (-96%) |
| רצף המוח לבדו | 3.0MB דסקטופ · 1.28MB מובייל |

⚠️ **המספרים מקומיים** (localhost, בלי latency). ברשת אמיתית הפריים הראשון ייקח
עשרות עד מאות מילישניות — אבל הוא לא חוסם כלום, וזו הנקודה.

---

## 4. מה נשאר פתוח — ומה אתה צריך להחליט

**🔒 חסום עליך (לא נגעתי, לפי ההוראה):**
1. **דומיין** — `og:image`/`og:url` עדיין מקובעים ל-github.io. נשבר ברגע ששולחים
   קישור בוואטסאפ מדומיין אחר.
2. **מייל / וואטסאפ אמיתיים** — כפתור "שיחת היכרות" עדיין מוסתר.
3. **תוכן אירועים** — 9 סימוני TODO. שלוש הכרטיסיות משתמשות בתמונות ממוחזרות
   מסקשנים אחרים. זה הפער הכי גלוי לעין באתר כרגע.

**החלטות שכדאי שתאשר בדיעבד:**
4. שלוש שורות המסע בצלילה — נשארו שלך, בלי חלופות.
5. הטריפטיך במקום רשת הקלפים השנייה — בחירה שלי, בלי שתי חלופות להשוואה.
6. "הגישה — ארבעה צעדים" כשם החדש ל-`#path`.
7. הורדת "מהישרדות ליצירה" ממסך הפתיחה.

---

## 5. מה הכי מדאיג אותי בתוצאה

**שהעמוד גדל פי 2.2 בדסקטופ (1.49MB → 3.33MB), והרווח כולו הולך לרצף המוח.**
עשיתי את כל אופטימיזציית התמונות (-96%) ובכל זאת יצאתי כבד יותר — בלעדיה הוא היה
נוחת על ~4.4MB. במובייל זה בסדר (1.55MB, בדיוק כמו העמוד הישן), ושם מגיע רוב
הקהל מאינסטגרם. אבל אם מישהו יפתח את זה בדסקטופ על חיבור איטי, הוא יראה את
הפריים הראשון מהר — ואז יחכה לשאר בזמן שהוא כבר גולל. **לא בדקתי את זה בתנאי
רשת אמיתיים, רק על localhost.** זה הפער הכי גדול בין מה שמדדתי למה שיקרה בפועל.

---

## 6. סבב ביקורת עצמית — מה הייתי מבקר אם מישהו אחר היה כותב את זה

קראתי את כל ה-diff מ-`main` (945 תוספות, 367 מחיקות, 29 קבצים) כאילו הוא לא שלי:

1. **🔴 בדיקת הפיקסלים של הלוגו לא נבדקה על ספארי אמיתי.** כתבתי שומר סף חכם,
   אבל אין לי ספארי כאן — בדקתי אותו רק על Chromium, שם הוא בוחר WebP ועובר.
   ההיגיון נכון והכשל בטוח (נופל חזרה ללוגו סטטי), אבל **הטענה "עכשיו זה עובד
   בספארי" לא אומתה.** צריך בדיקה על אייפון אמיתי לפני שסומכים עליה.

2. **🟡 `520vh` לצלילה זה הרבה גלילה.** במובייל צמצמתי ל-420vh, אבל גם זה כארבעה
   מסכים לפני שרואים משפט תוכן אחד. אם מישהו נכנס מאינסטגרם לבדוק "מה זה SHIFT"
   ולא אוהב לגלול — הוא יוצא לפני הכותרת. הדילוג פותר את זה למקלדת בלבד; אין
   דילוג גלוי לעכבר ולמגע. **הייתי שוקל כפתור "דלג" נראה אחרי 2–3 שניות.**

3. **🟡 `--j` נקבע לפי סדר קשיח ב-JS.** מערך `JOURNEY` ב-`motion.js` מונה סקשנים
   לפי סלקטור. מי שיוסיף סקשן ל-HTML ולא יזכור לעדכן את המערך — יקבל סקשן בלי
   מרווח ובלי רוחב, כלומר שבור למראה. זה חוב שיתפוצץ בשקט.

4. **🟡 מדדתי רק על localhost.** כל מספרי הביצועים כאן חסרי משמעות לרשת אמיתית.
   פריט B ב-BACKLOG (Lighthouse מקומי) היה בדיוק בשביל זה ולא בוצע.

5. **🟢 `.dive-still` מציג `d/f001.webp` גם במובייל.** מי שאין לו JS מוריד את
   הפריים הדסקטופי (31KB במקום 20KB). זניח, אבל לא נכון.

6. **🟢 הסרתי 22 בלוקי CSS מתים** אחרי שהצלבתי כל מחלקה מול ה-HTML — אבל עשיתי
   את זה בסקריפט אוטומטי. עברתי על התוצאה ובדקתי מחדש את כל ארבעת הרוחבים
   (נקי), ובכל זאת: מחיקה אוטומטית של CSS היא הדבר שהכי קל לפספס בו רגרסיה
   ויזואלית שלא מופיעה בשום בדיקה אוטומטית.

**מה שהייתי משבח:** ההפרדה בין הצלילה (JS) למערכת המרחב (CSS טהור) — היא אומרת
שגם כשהכול נכשל, האתר עדיין קריא ועדיין מספר את הסיפור. וההחלטה לא לסמוך על
`navigator.userAgent` בשלב 6 מונעת בדיוק את הרגרסיה שכבר קרתה פעם אחת.

---

## 7. מכאן — האתר עובר לשני סוכנים

מ-26.7 העבודה מתחלקת לפי `AGENTS.md`: **חזות** (`index.html`, `css/style.css`,
אבות-הטיפוס) ו**מכניקה** (`js/`, `scripts/`, `assets/`, בדיקות). אף סוכן לא נוגע
בקבצים של השני; בקשה חוצה-גבול נכתבת ב-`REQUESTS.md`. Cowork ממזג — לא הסוכנים.

הסבב הזה נעשה כשעוד לא הייתה חלוקה, ולכן הוא חוצה את שני התחומים. הוא הבסיס
ששני התורים (`BACKLOG-design.md` / `BACKLOG-tech.md`) יוצאים ממנו.

**מה שכל צד צריך לדעת מהסבב הזה:**

- **לחזות:** מערכת ה-`--j` (שלב 3) היא CSS טהור פרט לשורה אחת ב-`motion.js`
  שמסמנת לכל סקשן את מיקומו. שינוי הקצב או המרווחים נעשה כולו ב-CSS.
  שינוי *סדר* הסקשנים דורש בקשה למכניקה (מערך `JOURNEY`) — ראה פריט G.
- **למכניקה:** הצלילה (שלב 1) חושפת `window.__dive` עם `firstFrameMs`,
  `allFramesMs`, `render()` ו-`state()` — נועד בדיוק למדידה ולבדיקות
  אוטומטיות בסביבה שבה ה-rAF קפוא. כל מספרי הביצועים כאן הם localhost.

### ממצא ש-Cowork תפס ואני פספסתי

בצילום `delivery/hero-3-light-1440.png` — הרגע הכי פתוח באתר — **סרגל הניווט
הכהה יושב כפס נייבי אטום על הרקע הבהיר וחותך אותו.** זה בתמונה שאני עצמי צילמתי
והסתכלתי עליה, ובכל זאת לא ראיתי: בדקתי אם הכותרת מופיעה, לא אם *הרגע עובד*.
זו בדיוק המגבלה של לבדוק את מה שכתבת מול מה שהתכוונת שיקרה. הטיפול בזה הוא
D1 אצל סוכן החזות.
