# shift-website — פרוטוקול עבודה משותפת (Claude Code + Cowork)

שני סוכנים עובדים על האתר הזה:
- **Claude Code** — מפתח: כותב ומתקן את קוד האתר
- **Cowork (הבודק)** — עובר על הקוד ועל האתר אחרי כל סבב, מאתר באגים, ומאמת תוכן

## חובה ל-Claude Code — בסיום כל סבב עבודה

1. עשה commit עם הודעה ברורה (מה שונה ולמה).
2. עדכן את `HANDOFF.md` בפורמט:
   - **מה עשיתי** — רשימת שינויים בסבב הזה
   - **מה לא הושלם / ידוע כשבור**
   - **שאלות פתוחות** (אם יש)
3. אם קיים `REVIEW.md` — קרא אותו **בתחילת** הסבב וטפל בממצאים לפי סדר העדיפות, לפני משימות חדשות. סמן בו ✅ ליד מה שתוקן.

## 🧠 כיווץ הקשר - מה לשמר (הוגדר 2.8.2026 בהוראת OC)

הכיווץ מופעל אוטומטית ב-**85%** מחלון ההקשר (‏`.claude/settings.json`).
‏`PreCompact` שומר מצב ל-`.claude/SESSION-STATE.md` ועושה נקודת
שמירה בגיט; ‏`SessionStart` מזריק אותו חזרה. **בתקציר הכיווץ
עצמו - לשמר בעדיפות זו:**

1. הפריט הנוכחי מ-`NEXT.md` ולמה הוא נבחר.
2. הכרעות OC שהתקבלו בסשן הזה ועדיין לא נכתבו לקובץ - **אלה
   הדבר היקר ביותר. אם משהו נאמר בצ'אט ולא תועד, לתעד לפני
   הכיווץ.**
3. באגים שאובחנו ועדיין לא תוקנו, עם מספרי שורות.
4. מה נוסה ונכשל (כדי לא לחזור על אותו מסלול).

**לזרוק בלי היסוס:** תוכן קבצים שנקרא ואפשר לקרוא שוב · פלט
בדיקות שעבר · ‏diff-ים שכבר קומטו · חקירות שהסתיימו במסקנה
שכבר נרשמה.

## 💸 חיסכון בקריאה — מה לקרוא ומה לא (29.7.2026)

**לקרוא בתחילת סבב:** `NEXT.md` (התור המחייב) · `REVIEW.md` (סקירות פתוחות בלבד) · `HANDOFF.md` · `BRAND-DESIGN.md` לפני עבודה עיצובית · ה-BACKLOG הרלוונטי לתחום שלך.

**לא לקרוא אלא אם צריך תשובה היסטורית ספציפית:** `REVIEW-ARCHIVE.md` · `BACKLOG-ARCHIVE.md` · `NIGHT-REPORT.md` · `DELIVERY.md` · `PROMPTS.md` · `delivery/*.md`. אלה ארכיונים — יקרים לקריאה ולא משנים את מה שצריך לעשות עכשיו.

**כלל:** ‏NEXT גובר על BACKLOG. אם פריט מופיע בשניהם — לעבוד לפי NEXT ולא לקרוא את השני.

## מקורות אמת לתוכן — חובה

כל תוכן על מסלול 21 הימים באתר נגזר **אך ורק** מאלה, אף פעם לא נכתב מחדש מהזיכרון:
- `PROGRAM-SPEC.md` (בתיקייה הזו) — המפרט המחייב. מחולל אוטומטית: `node scripts/generate-program-spec.mjs`. לא לערוך ביד.
- `~/Desktop/SHIFT/shift platform main /shift-platform/קבצי קוד/data.js` — **המקור החי**: `DEFAULT_DAYS` ו-`WEEK_INTROS`. אומת ב-26.7.2026 (md5 זהה) מול ה-data.js שהאפליקציה הפרוסה מגישה בפועל ב-shift-21-day-course-ceos.web.app.

> ⚠️ עדכון 26.7.2026: `~/Documents/Claude/Projects/SHIFT/app/data.js` הוא סנפשוט ישן (7.7.2026)
> ו`טבלת-מיפוי-21-ימים.md` ישנה אף היא — **לא להשתמש בהם כמקור**. בכל סתירה, ה-data.js של הריפו
> (= מה שהאפליקציה החיה מגישה) גובר. אם מזיזים את תיקיית הריפו — לעדכן את הנתיב גם
> ב-`scripts/verify-program.mjs` וגם ב-`scripts/generate-program-spec.mjs`.

**חובה לפני כל commit שנוגע בסקשן המסלול:** `node scripts/verify-program.mjs` — חייב לצאת 21/21.
אם `data.js` השתנה, יש להריץ מחדש את `scripts/generate-program-spec.mjs`.

על אירועים אין מקור אמת. **אסור להמציא** תאריכים, מחירים, מיקומים או פרטי אירוע — רק מבנה ועיצוב, עם `<!-- TODO: תוכן מ-OC -->`.

ניסוח מדעי: תמיד מסויג ("נראה כי", "מחקרים מצביעים על כך"), לפי `דוח-אימות-מדעי.md`.

## חובה ל-Cowork (הבודק) — אחרי כל סבב

1. קרא את `HANDOFF.md` ואת ה-diff מאז הבדיקה הקודמת (`git log` / `git diff`).
2. עבור על הקוד: באגים, נגישות, ביצועים, RTL/עברית, קוד כפול.
3. **הצלב את תוכן המסלול מול `data.js` וטבלת המיפוי** — יום אחר יום.
4. פתח את האתר בדפדפן: גלילה מלאה, קונסול, מובייל, `prefers-reduced-motion`.
5. כתוב ממצאים ל-`REVIEW.md` בפורמט: 🔴 קריטי / 🟡 חשוב / 🟢 שיפור, ובסוף — **מה עוד נשאר לעשות**.

## קבצי התיאום

- `HANDOFF.md` — Claude Code כותב, Cowork קורא
- `REVIEW.md` — Cowork כותב, Claude Code קורא ומטפל

## ⚙️ כלל התור — אף פעם לא להישאר בלי משימה

`BACKLOG.md` הוא תור העבודה. הכלל:

1. **בסיום כל פריט** — לסמן ✅ עם תאריך ב-`BACKLOG.md`, לעדכן `HANDOFF.md`, לעשות commit,
   **ומיד למשוך את הפריט הפתוח הבא מלמעלה ולהתחיל בו.** בלי לעצור ובלי לשאול.
2. **פריט 🔒** חסום בהחלטת OC — לדלג עליו ולקחת את הבא. לא לחכות.
3. **אם התור נגמר** — לעבור לפרויקטי הרקע בתחתית ה-BACKLOG. הם תמיד זמינים ותמיד יש בהם עבודה.
4. **תובנות שמתגלות תוך כדי** — להוסיף כפריט חדש ל-`BACKLOG.md` במקום הנכון לפי עדיפות,
   ולהמשיך במשימה הנוכחית. לא לקפוץ באמצע.
5. **מה שדורש החלטה של OC** — לרשום כ-🔒 עם שאלה מנוסחת היטב ב-`HANDOFF.md`, ולהמשיך הלאה.

חריג יחיד: אם פעולה עלולה למחוק או להשחית נתונים אמיתיים של משתמשים — לעצור ולשאול.
