# A11Y — מפת נגישות (פריט T7)

**נמדד:** 27.7.2026 · `npm run a11y` · headless Chrome, 1440×900, אחרי גלילה מלאה
(כדי שתוכן שנחשף בטריגרים ייכלל).

הכלי מודד בפועל ולא מסתמך על הצהרות: הוא דוגם צבע מחושב, מרכיב שקיפויות
על הרקע האמיתי, ומחשב יחס ניגודיות לפי WCAG.

---

## התוצאה

| בדיקה | תוצאה |
|---|---|
| ניגודיות טקסט (184 אלמנטים) | **0 כשלים** |
| תמונות בלי `alt` | **0 מתוך 13** |
| אלמנטים אינטראקטיביים בלי שם נגיש | **0 מתוך 36** |
| יעדי מגע מתחת ל-24×24 (WCAG 2.2) | **0** |
| קפיצות בהיררכיית הכותרות | **0** |
| מעבר מקלדת (40 עצירות) | כולן עם טבעת פוקוס |
| `lang` / `dir` | `he` / `rtl` ✓ |
| `h1` בעמוד | 1 ✓ |
| `aria-live` | `breathPhase`, `breathCounter` ✓ |

---

## מה תוקן בסבב הזה

**1. קישורי ניווט הנקודות היו חסרי שם נגיש.** שמונה קישורים ריקים
(`<a href="#top" data-label="פתיחה"></a>`) — התווית הייתה ב-`::after` של ה-CSS,
כלומר גלויה לעין וחסרת קיום לקורא מסך. נוסף `aria-label` לכל אחד.

**2. יעדי המגע של אותן נקודות היו 10×10.** מתחת למינימום של WCAG 2.2
(24×24). שטח הלחיצה הורחב ל-26×26 דרך `::before` שקוף — המראה לא השתנה.

**3. הסייג הבריאותי בתרגיל הנשימה היה בניגודיות 2.9:1.** זה הממצא החשוב
מבין השלושה: `--ink-faint` (שקיפות 0.45) על רקע לבן. הטקסט הזה מזהיר אנשים
בהיריון, עם מצב לב/נשימה או אפילפסיה — **אזהרה רפואית שנועדה להיקרא, לא
להיות "עדינה".** הועבר ל-`--ink-soft` (5.9:1) וקיבל קו הדגשה בתכלת.
אותו תיקון הוחל גם על הערת המחקר לצדו.

---

## מגבלות — מה הכלי לא יכול לבדוק

1. **טקסט מעל הקנבס של הצלילה** (הכותרת בהגעה לאור, תת-הכותרת, הכפתורים)
   מוחרג מבדיקת הניגודיות: הרקע הוא פריים מצויר שמשתנה לאורך הגלילה, ואין
   ממנו ערך יחיד לדגום. **הוערך ידנית מהצילומים:** נייבי כהה על פריצת אור
   בהירה — ניגודיות גבוהה מאוד. אבל זו הערכה, לא מדידה.
2. **`.love-card`, `.cta` ו-`.closing`** מוחרגים מאותה סיבה (רקע גרדיאנט
   או תמונה). טקסט לבן על נייבי/גרדיאנט כהה — נראה בטוח, לא נמדד.
3. **קורא מסך אמיתי לא הופעל.** נבדק מבנה (שמות, תפקידים, `aria-live`),
   לא חוויית הקראה בפועל ב-VoiceOver או NVDA.
4. **עצירת מקלדת אחת מחוץ למסך** — זהו קישור הדילוג, שמעוצב לקפוץ פנימה
   בפוקוס. התנהגות תקינה, לא ממצא.

---

## מה נשאר פתוח

- בדיקה עם VoiceOver על מכשיר אמיתי, במיוחד על תרגיל הנשימה: ה-`aria-live`
  מכריז את שלבי הנשימה, וכדאי לוודא שהקצב לא מציף.
- ניגודיות הטקסט מעל הצלילה — למדוד בדגימת פיקסלים מהפריים עצמו במקום
  להעריך מהצילום.
