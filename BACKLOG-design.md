# BACKLOG — 🎨 סוכן חזות

**בעלות:** `index.html` · `css/style.css` · `prototype-*.html`
לפני כל פריט: לקרוא את `NEXT.md`. חוקי העבודה ב-`AGENTS.md`.

**נעול — לא לשנות:** פונטים (Assistant) · פלטת הצבעים (#18163B · #5CBBF0 · #F6F5F2).
הוכרע ע"י OC ב-26.7.

---

## עכשיו

### D0. 🔴 רגרסיה מ-B3: מופחת-תנועה — כותרת הגעה לבנה על רקע בהיר
`html.ending-curtain` צובע את טקסט ההגעה בלבן, אבל במצב `prefers-reduced-motion`
הווילון `display:none`, הפריים המוצג הוא המואר וה-veil בהיר (style.css ‏1645) —
טקסט לבן על בהיר ברגע ה-CTA. תיקון: לעטוף את חוקי הצבע (‏1902–1912)
ב-`@media (prefers-reduced-motion: no-preference)`; לבדוק גם מסלול בלי-GSAP.
פירוט: `REVIEW.md` ‏27.7 ‏14:05. נוסף 27.7 ‏14:05 ע"י Cowork.

### D1. ✅ מעבר עיניים — **המיפוי הושלם** (27.7 אחה"צ). מיפוי, לא תיקון.
הסבב הראשון כיסה את הצלילה→מוצרים→שיטה→הרגלים→נשימה→מסלול; ההשלמה
כיסתה את אירועים · הסיפור · מה נפתח · סצנת הסיום (צילומי `delivery/`
העדכניים, 1440 + 390; המסלול נבחן לעומק ב-B1 על 16 גדלי חלון).
**התיקונים עצמם מוקפאים עם D2–D7** — זו רשימת הממצאים, לפי חומרה.

**מה שראיתי:**

1. ~~הסרגל ברגע האור נקרא כרצועה~~ **תוקן 27.7 (REQUESTS):** הסרגל
   שקט לכל אורך הצלילה כולל רגע האור, וחוזר עם ההצהרה — בדיוק הכיוון
   שהוצע כאן. אומת בארבעה תרחישים כולל קפיצת עוגן.
2. ~~הקצב במובייל בסל המוצרים איטי מדי~~ **נסגר במדידה (27.7 D4):**
   קלף נמדד ב-221px ב-390 — שלושת הקלפים + הכותרת ≈ 1.2 מסכים,
   קצב סביר. הרושם "כמעט מסך שלם" נבע מקריאת צילום ‎@2x כאילו הוא ‎@1x.
3. ~~הצלילה ארוכה מהתוכן שאחריה~~ **טופל 27.7 (REQUESTS):** קוצר
   ‎520→440vh — פשרה שמקצרת את ההשקעה בלי להחזיר את תדירות החלפות
   הפריימים שהדילול (B2) בא להוריד (‎~7.3vh לפריים).
4. **הטיפוגרפיה נקייה עכשיו בכל חמשת הרוחבים** (`npm run type`) —
   אחרי שהתגלה ש-`ch` בעברית משקר ב-~29%. זו כבר לא נקודת חולשה.

**מהשלמת המיפוי (27.7 אחה"צ):**

5. ✅ **תוקן 27.7** (הוראת OC לפעול בלי אישורים): הכותרת פוגשת את העין במסך הראשון; במובייל המילים לפני התמונה. ~~הסיפור נפתח במסך ריק.~~ ב-1440 המסך
   הראשון של הסקשן הוא ~55% נייר ריק משמאל וקצה כרטיס-תמונה מימין;
   ב-390 זה חמור יותר — **מסך שלם ויותר של גרדיאנט כחול בלי מילה
   אחת.** מי שגולל בקצב רגיל פוגש סקשן שמתחיל בהיעדרות. כיוון אפשרי:
   להקדים את הכותרת/משפט הפתיחה אל מעל התמונה, או לקצר את התמונה
   במובייל. (המסלול: `delivery/story-1440.png`, `story-390.png`)
6. ✅ **תוקן 27.7**: קובץ לשלושה אשכולות (פנימה/קדימה/ביומיום) בשלוש עמודות — הטקסטים ללא שינוי. ~~ענן תגיות בלי עוגן.~~ ב-1440 עשר גלולות שוות-משקל
   צפופות בפינה הימנית-עליונה; חצי שמאל והחצי התחתון ריקים. אין
   היררכיה ואין קיבוץ (תוצאות-זהות ותוצאות-יכולת מעורבבות), והסקשן
   שכולו הבטחה הוא החלש ויזואלית. במובייל הזיגזג דווקא עובד. כיוון:
   קיבוץ ל-2–3 אשכולות עם כותרות-משנה, או עוגן ויזואלי בצד השמאלי.
7. **סצנת הסיום עומדת יפה** — כותרת, שורה, אינסטגרם, ומילת הענק.
   עקב אכילס אחד: הקופי מבטיח "השאירו פרטים" אבל אין על העמוד דרך
   להשאיר פרטים (כפתור השיחה מוסתר עד כתובת אמיתית — `LAUNCH.md`).
   הבטחה שהעמוד לא מקיים. לתקן קופי או להחזיר כפתור, לפי מה שיקרה קודם.
8. **אירועים עומד** — קומפוזיציה קוהרנטית; מה שחסר שם הוא תוכן (🔒),
   לא עיצוב.

### D1-מקורי
הפרויקט הגדול נסגר — אף אחד עוד לא הסתכל על התוצאה **כחוויה אחת רציפה**.
לגלול לאט מלמעלה למטה ב-1440 וב-390, ולרשום כל מקום שבו:
- הקצב נשבר (סקשן שמרגיש מהיר מדי או איטי מדי ביחס לשכניו)
- העין לא יודעת לאן ללכת
- שני דברים סמוכים נראים כאילו נכתבו ע"י שני אנשים
לתעד ב-`REVIEW-design.md` עם צילומי מסך. **לא לתקן עדיין** — קודם למפות.

### D2. ✅ המעבר מהישרדות ליצירה — נמדד, והציר החסר הושלם (27.7)
**מה נמדד:** הסולם עובד ומצטבר יפה — עמודה 910→1150 (+26%), אוויר אנכי
74→150px (+103%), מרווח כותרת +76%. כל צעד בודד ~3% — מתחת לסף התפיסה
**בכוונה** (מערכת, לא אפקט). ההצהרה נשארת צרה/מהירה (הישרדות) והסגירה
צרה גם היא — התרחבות הסיום נישאת ע"י מילת הענק, לא ע"י הפסקה. הוחלט: לא נוגעים.
**מה חסר והושלם:** מהמפרט המקורי — שלושה צירים (רוחב, קצב אנכי, משך
אנימציה). הציר השלישי היה מיושם רק על כותרות (jDur); גופי הסקשנים רצו
במשך קבוע. עכשיו כל הכניסות נמתחות עם המסע: ימי המסלול 0.6→0.79ש',
הסיפור 0.8→1.02ש', הצ'יפים 0.55→0.86ש' (נמדד בפועל). הישרדות נכנסת
מהר ועצבני; יצירה נושמת. זה הערוץ שהגוף מרגיש בלי שהעין תצטרך לזכור רוחב.

### D3. ✅ תרגיל הנשימה — שלוש התשובות ניתנו (27.7)
לפני: המונה מציג "שלושה סבבים · פחות מדקה" עוד לפני הלחיצה. באמצע:
הטבעת נושמת עם ההנחיה — עוגן לעין בנשיפה הארוכה (כבוי ב-reduced-motion).
אחרי: "איך זה מרגיש עכשיו?" מוביל עכשיו לשורת המשך — "זה היה רגע אחד
מתוך יום 1... אל המסלול ←". אומת בהאצת זמן על שלושת המצבים.

### D4. ✅ מובייל כחוויה — נבדק 27.7 בשלושת הרגעים הקריטיים
שער הכניסה: המוח ממלא את המסך, לוגו ורמז גלילה במקום ✓. ההגעה לאור:
נקייה וקריאה (העיצוב הסופי ממתין להכרעת B3) ✓. המעבר כהה→בהיר במסלול:
הקלפים אטומים וקריאים באמצע המורף, המספרים בצד הנכון (RTL) ✓.
סל המוצרים נמדד — 221px לקלף, הקצב סביר (ראו תיקון ממצא D1 #2).
הפער האמיתי שנשאר במובייל: פתיחת הסיפור הריקה — ממצא D1 #5, בתור.

### D5. ✅ מיקרו-קופי — הצעות ב-`delivery/COPY.md` (27.7), ממתין ל-OC
שלושה כיוונים לשורות הצלילה (ישיר / מדעי / מינימלי) + המלצה; טבלת
כפתורים; סריקת מצבי-ריק. יושם רק תיקון-אמת אחד: שורת הסיום הבטיחה
"השאירו פרטים" בלי שיש דרך להשאיר — ההבטחה הוסרה עד שיש ערוץ (TODO
בקוד להחזרה). שאר ההצעות לא יושמו — הכרעת OC.

---

## אחר כך

### D6. ✅ ההצהרה והסיפור — מכוסים ומאומתים (27.7)
אורך שורה: `npm run type` מודד שורות ויזואליות בפועל ב-5 רוחבים — נקי.
יתומים: `text-wrap: pretty` על p/הצהרה/אינטרו; `balance` על כותרות.
ציטוט הסיפור ב-logical properties (RTL נכון). קצב קריאה: ציר המשך של
D2 מאט את כניסות הסיפור (0.8→1.02ש').

### D7. ✅ סוף העמוד — מסיים, לא סתם נגמר (27.7)
נשפט ב-D1 #7: כותרת → שורה → אינסטגרם → מילת הענק → פוטר = סגירה
מכוונת. פער האמת בקופי תוקן (הבטחת "השאירו פרטים"). נשאר תלוי-B3:
אחרי ההכרעה לוודא שהסיום קוהרנטי עם סיום הצלילה שייבחר.

### D8. 🔒 סקשן האירועים — ממתין לתוכן אמיתי מ-OC
9 סימוני TODO. אפשר לשפר את המבנה והקצב בלי להמציא פרטים.

---

## פרויקטי רקע — כשהתור ריק

### DA. ✅ מפת הקצב — `RHYTHM.md` (27.7), נמדד מהדף החי
כל 11 סקשני המסע: גובה, ריווח, עמודה, H2, גוף, ומשך כניסה לפי --j.
השורה התחתונה: המערכת מונוטונית ועקבית — אין סקשן ששובר את הסולם;
הטיפוגרפיה אחידה (הזהות) וההדרגה כולה באוויר ובקצב. החריג המוצדק
היחיד: המסלול, ארוך פי ~3 — הוא המוצר.

### DB. ✅ השוואת רפרנסים (28.7, לילה) — `delivery/DB.md`
בוצע headless עם wheel אמיתי (איזאנמי דרש קליק-כניסה וגלילה וירטואלית).
הצילומים לא בריפו (מותגים של אחרים); הממצאים המלאים ב-delivery.
השורה התחתונה: השער, הצלילה והאיפוק שלנו — באותה ליגה; הווילון (B3)
מאושש כמהלך מהליגה הזו. שלושה פערים → מועמדים למטה.

### מרפרנסים (DB) — מועמדים להכרעת OC, לא יושמו

1. **פרט חי בשוליים** (איזאנמי: שעוני דובאי/טוקיו רצים בפוטר) — מקבילה
   שלנו: נקודה נושמת ליד הלוגו בפוטר, או "יום N מתוך 21" חי. זול, נותן דופק.
2. **פוטר כרגע-מותג** (אלת'יה: wordmark ענק על וידאו בסוף) — SHIFT גדול
   לפני שורת הזכויות; סוגר את המעגל שהשער פותח.
3. **מיקרו-תוויות בצלילה** (אלת'יה: תגיות דאטה מוצמדות ל-3D) — 2–3 תוויות
   אנטומיות זעירות שחולפות עם הפריימים. הכבד מהשלושה; רק אם OC ירצה.
