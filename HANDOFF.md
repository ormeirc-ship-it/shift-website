# HANDOFF — עדכון אחרון מ-Claude Code

**סבב:** פרומפט 1 (היסוד: אמת בתוכן ומה ששבור) · 26.7.2026 · מול REVIEW.md מ-26.7

## מה עשיתי

### ממצא 1 — תוכן המסלול: הוכרע אחרת ממה שהפרומפט הניח
- **האתר לא נבנה מחדש — כי הוא היה נכון.** הממצא "16/21 שגויים" נמדד מול
  `~/Documents/.../app/data.js` מ-7.7.2026. התוכן באפליקציה שוכתב מאז
  (קומיט `0c908d7` מ-19.7, "סנכרון תוכן שרת→קובץ").
- אימות עצמאי: משכתי את `data.js` **מהאפליקציה החיה** (`shift-21-day-course-ceos.web.app/data.js`)
  — md5 `a7a600d7…`, זהה בית-לבית לעותק הריפו שממנו האתר נבנה, ושונה מהעותק ב-Documents.
- `verify-program.mjs` מול הקובץ החי: **21/21 ✓** (הפלט המלא נשלח ל-OC בצ'אט).
- תיקון בשורש, לא בסימפטום:
  - נכתב `scripts/generate-program-spec.mjs` — מחולל את `PROGRAM-SPEC.md` מהמקור; הורץ,
    והמפרט עכשיו משקף את התוכנית האמיתית (כולל "מודל א.פ.ר.ת" ביום 2, מנוחה רק ב-7 ו-14).
  - `scripts/verify-program.mjs` — סדר המקורות עודכן: עותק הריפו (=החי) לפני הסנפשוט הישן.
  - `CLAUDE.md` — סעיף מקורות האמת תוקן + אזהרה מתוארכת על העותק המיושן.
  - נוספה הערת מקור בסוף סקשן המסלול ב-`index.html` (כפי שביקש הפרומפט).
- הצ'יפים הצפים (8 · נוירופלסטיות, 12 · מדיטציה, 17 · דופמין) **נכונים מול המקור החי** — לא שונו.

### ממצאים 2–6 + 12–13 — בוצעו כלשונם
- **2:** רשת ביטחון ב-`<head>` (inline, 4 שניות) שמורידה את מסך הפתיחה גם אם motion.js מת;
  כל גוף ה-IIFE של motion.js עטוף try/catch שמשחרר את המסך ומשדר `shift:reveal` בקריסה.
- **3:** כפתור "שיחת היכרות" (mailto לא קיים) הוסתר בהערת TODO עם הוראת החזרה. ממתין לכתובת מ-OC.
- **4:** `:focus-visible` גלובלי — תכלת על רקע כהה, נייבי על בהיר, טבעת עגולה על כפתורי גלולה;
  התפריט: פוקוס נכנס לקישור הראשון, Tab נלכד בפנים, Escape סוגר, הפוקוס חוזר לכפתור.
- **5:** סייג בריאותי מתחת לכפתור הנשימה, בנוסח שהוצע (כולל התאמת צבע למצב המואר של הסקשן).
- **6:** מאזין `popstate` — Back/Forward מזיזים באמת בין עוגנים.
- **12:** הגנות null על `nav`/`menu`/`burger` ב-main.js.
- **13:** `aria-live="polite"` על `#breathPhase` (היה רק על המונה).
- **בונוס (התגלה תוך כדי):** נוסף פרמטר גרסה ל-CSS/JS (`?v=20260726b`) — בלעדיו דפדפנים
  (וגם GitHub Pages) המשיכו להגיש JS ישן מה-cache אחרי פריסה. לעדכן את הפרמטר בכל שינוי.

## מה לא הושלם / ידוע כשבור
- ממצאים 7–11, 14–16 מ-REVIEW.md — לא בסבב הזה (שייכים לפרומפטים 4–5 לפי PROMPTS.md).
- 9 סימוני TODO באירועים + כתובת קשר — ממתינים לתוכן מ-OC.
- האתר החי (github.io) עדיין מאחור — הדחיפה רק באישור מפורש של OC.

## שאלות פתוחות
- **ל-Cowork:** העותק ב-`~/Documents/Claude/Projects/SHIFT/app/` וטבלת המיפוי מיושנים.
  שווה לעדכן אותם מהריפו (או למחוק את העותק) כדי שהבדיקה הבאה תוצלב מול המקור הנכון.
  הריפו: `~/Desktop/SHIFT/shift platform main /shift-platform/` (git pull לפני הצלבה).
- הערת "16 האי-התאמות" בטבלה של ממצא 1 נשארה בקובץ כפי שהיא (מסומנת כנסגרה) — לא מחקתי
  תוכן של Cowork, רק הוספתי את ההכרעה מעליו.
