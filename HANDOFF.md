# HANDOFF — עדכון אחרון מ-Claude Code · 26.7.2026

**סבב:** "להביא את אתר SHIFT למצב מוגמר" — 8 שלבים, עבודה ללא השגחה. **הסתיים.**
**ענף:** `site-complete`, 11 קומיטים מעל `main`. עץ העבודה נקי.
**לא מוזג ל-main. לא נדחף לאתר החי.** זה של Cowork ושל OC בלבד.
**הדוח המלא:** `DELIVERY.md` — מספרים, 28 צילומי מסך וסקירה עצמית.

> מ-26.7 האתר עובר לשני סוכנים לפי `AGENTS.md`. הסבב הזה נעשה לפני החלוקה
> ולכן הוא חוצה את שני התחומים — הוא הבסיס ששני התורים יוצאים ממנו.

## מה עשיתי

שמונת השלבים, כל אחד ב-commit נפרד:

1. **הכניסה דרך המוח** — ההירו הישן הוחלף בצלילה מונעת-גלילה. ההחלקה
   מאב-הטיפוס נשמרה, הקלט מ-ScrollTrigger בלבד, מובייל טוען רק 65 פריימים.
2. **הסרת ארבע תבניות ה-AI** — 10 לייבלים → 1, מספור 01–07 הוסר,
   `.grad-text` נמחק, ורשת הקלפים הכפולה הפכה לטריפטיך עריכתי.
3. **המעבר כמערכת** — רוחב עמודה (913→1147px), מרווח אנכי (74→150px) ומשך
   הכניסות (0.6→1.2ש') נעים יחד לאורך 11 סקשנים.
4. **תרגיל הנשימה** — סקשן משלו, מוקדם יותר בעמוד.
5. **ביצועים** — WebP (-89% בדיסק), מידות לכל 13 התמונות, גופן לא-חוסם.
6. **לוגו בספארי ובמובייל** — שני `<source>` עם בדיקת פיקסלים במקום זיהוי UA.
7. **ניקיון** — הכותרת הכפולה, 12 נכסים לא בשימוש.
8. **מעבר מלא** — 4 רוחבי מסך, מקלדת, reduced-motion, בלי JS, 21/21 בתוכן.

בנוסף: ניקוי 22 בלוקי CSS מתים, ותיקון אורך שורה (עשרות פסקאות חצו 75 תווים).

`css/style.css` ו-`delivery/` (28 צילומים) נכנסו ב-`4250d0c`; `DELIVERY.md`
נסגר ב-`7d2edab`. אין שום דבר שנשאר לא מקומט.

## מה שהסוכנים צריכים לדעת מהסבב הזה

**🎨 לחזות** — מערכת ה-`--j` (שלב 3) היא CSS טהור, פרט לשורה אחת ב-`motion.js`
שמסמנת לכל סקשן את מיקומו. כוונון קצב ומרווחים נעשה כולו ב-`css/style.css`.
**שינוי סדר הסקשנים דורש בקשה למכניקה** (מערך `JOURNEY`) — ראה פריט G ב-BACKLOG.

**🔧 למכניקה** — הצלילה חושפת `window.__dive` עם `firstFrameMs`, `allFramesMs`,
`render()` ו-`state()`. זה נבנה בדיוק בשביל מדידה ובדיקות אוטומטיות בסביבה שבה
ה-rAF קפוא. סקריפטי המדידה שכתבתי (headless Chrome, מדידת משקל, audit של 4
רוחבים) יושבים ב-scratchpad ולא בריפו — שווה לשחזר אותם תחת `scripts/`.

## מה לא הושלם / ידוע כשבור

- **🔒 שלושת הפריטים החסומים — לא נגעתי**, לפי ההוראה: דומיין (`og:*` עדיין
  github.io), מייל/וואטסאפ (הכפתור מוסתר), ותוכן אירועים (9 TODO, תמונות
  ממוחזרות). **תוכן האירועים הוא הפער הכי גלוי לעין באתר כרגע.**
- **שומר הסף של הלוגו (שלב 6) לא נבדק על ספארי אמיתי** — רק על Chromium.
  הכשל בטוח (נפילה חזרה ללוגו סטטי) אבל הטענה לא אומתה. פריט E.
- **כל מדידות הביצועים הן על localhost.** פריט H, ומשיק ל-T1 של המכניקה.
- **סרגל הניווט ברגע ההגעה לאור** — Cowork תפס בצילום שלי מה שאני פספסתי:
  פס נייבי אטום חותך את הרגע הכי פתוח באתר. עובר ל-D1.
- שום שלב לא נשבר ולא נדרש `git revert`.

## שאלות פתוחות ל-OC

1. **ארבע החלטות שלקחתי לבד** (היית בחוץ, ההוראה הייתה לא לחכות): שלוש שורות
   המסע נשארו כפי שכתבת; הטריפטיך נבחר בלי שתי חלופות; `#path` נקרא עכשיו
   "הגישה — ארבעה צעדים"; והמשפט "מהישרדות ליצירה" ירד ממסך הפתיחה.
2. **הדאגה המרכזית:** העמוד גדל מ-1.49MB ל-3.33MB בדסקטופ — כולו רצף המוח.
   במובייל 1.55MB, כמו קודם. דילול ל-60–70 פריימים חוסך כמגה. **החלטה שלך.**
3. חמישה פריטים (E–I) נוספו ל-`BACKLOG.md` מהביקורת העצמית — כדאי ש-Cowork
   יחלק אותם בין `BACKLOG-design.md` ל-`BACKLOG-tech.md`.
