# HANDOFF — עדכון אחרון מ-Claude Code · 27.7.2026

**סבב:** לילה ארוך, עבודה ללא השגחה עד שהתור נגמר.
**ענף:** `site-complete`, 11 קומיטים בסבב הזה. עץ עבודה נקי.
**לא מוזג ל-main. לא נדחף לאתר החי.**

## התור

**התור הטכני ריק — 0 פריטים פתוחים.** גם שלושת פרויקטי הרקע הושלמו.
נשאר פתוח: התור העיצובי (D1 חלקי, D2–D7), ופריטים חסומים ב-🔒.

| פריט | סטטוס |
|---|---|
| T0 / R1 🔴 הסרגל הבהיר נדבק | ✅ |
| T0.5 / R2 `rootMargin` מקובע | ✅ |
| M כלי המדידה ל-`scripts/` | ✅ |
| H מדידה ברשת אמיתית | ✅ |
| T2 הצלילה תחת לחץ | ✅ |
| T3 קונסול נקי | ✅ |
| T4 `prefers-reduced-motion` | ✅ |
| T5 מוניטור סחיפת תוכן | ✅ |
| T6 מדידת ביצועים אוטומטית | ✅ |
| T7 נגישות מלאה → `A11Y.md` | ✅ |
| T8 תאימות דפדפנים | ✅ |
| E שומר הסף בספארי | ⏳ תועד כהנחה לא מאומתת |
| TA חבילת בדיקות · TB טיפוגרפיה · TC תקציב | ✅ |
| D1 מעבר עיניים | 🟡 **חלקי** |

## ארבעה באגים אמיתיים שנמצאו הלילה

1. **R1 — הסרגל הבהיר נדבק על סקשן כהה.** ה-early-return של "כתיבה רק
   בשינוי" ישב לפני חישוב המצב. **וזו הייתה רק חצי הסיבה:** `onScreen`
   הגיע מ-ScrollTrigger, שלא יורה `onToggle` בקפיצת עוגן. עבר ל-IO.
   זה גם מסביר למה האימות הקודם "ב-8 נקודות" לא תפס — הוא נעשה בקפיצות,
   וכל קפיצה איפסה את המצב במקרה.
2. **`100svh` בלי גיבוי `vh` בארבעה מקומות.** בספארי <15.4 ההצהרה
   נפסלת כולה והצלילה מאבדת גובה. נמצא בפריט T8.
3. **`ch` בעברית משקר ב-~29%.** הגבלתי טקסט ל-68ch בסבב הקודם ודיווחתי
   "0 שורות ארוכות" — מדידה אמיתית של שורות ויזואליות הראתה 87–91 תווים.
   24 ממצאים → 0 (`npm run type`).
4. **הסייג הבריאותי בתרגיל הנשימה בניגודיות 2.9:1.** טקסט שמזהיר אנשים
   בהיריון או עם מצב לב — נועד להיקרא, לא להיות עדין. עכשיו 5.9:1.

## הממצא החשוב ביותר — פריט N 🔴

**מדידה ברשת אמיתית (`npm run net`) הראתה פער של פי 250 מול localhost.**
ב-Slow 4G המבקר מחכה **8.2 שניות** עד שהוא רואה משהו. שיפרתי מה שאפשר
(טעינה בשלבים, מסך פתיחה שלא מחכה ל-JS — Fast 4G ירד מ-1.9ש' ל-1.1ש'),
אבל **ב-Slow 4G הזמן כמעט לא זז.** ב-400kbps ה-FCP לבדו הוא 3.9ש',
ואחריו 175KB של GSAP+ScrollTrigger+Lenis. סדר טעינה לא יפתור את זה.

**שתי הדרכים, שתיהן דורשות החלטה של OC:** לדלל את הרצף (97→60 פריימים
≈ מגה) או לקצץ בחבילת התנועה (Lenis ~30KB הוא הוויתור הקל). מי שמגיע
מאינסטגרם על סלולר בינוני — זה בדיוק הוא.

## כלים חדשים במאגר

`npm run check` מריץ את הכול. בנפרד: `test` (21 בדיקות, 40ms) ·
`audit` · `perf` · `net` · `weigh` · `budget` · `a11y` · `type` ·
`compat` · `stress` · `console` · `motion` · `drift` · `shots`.

הכלים מגישים את הריפו בעצמם, כך שכל מספר ניתן לשחזור מ-clone נקי.
`tests/motion.test.mjs` מיוחד: **כל בדיקה שם מתעדת באג שקרה בפועל** —
הוא הזיכרון של מה שכבר נשבר, כדי שלא יחזור בשקט.

## מה לא הושלם

- **D1 חלקי** — לא עברתי על כל 24 הצילומים. מה שנרשם הוא מה שראיתי.
  D2–D7 לא נגעתי.
- **פריט E** — אין ספארי/אייפון. ההנחה מתועדת ב-`DELIVERY.md` עם טבלת
  מה-נבדק/מה-לא. הרעה אינה אפשרית; שיפור אינו מובטח.
- **🔒 דומיין · מייל/וואטסאפ · תוכן אירועים** — לא נגעתי.
- שום פריט לא נשבר; לא נדרש `git revert`.

## שאלות פתוחות ל-OC

1. **פריט N** — לדלל את רצף המוח או לקצץ בחבילת התנועה? עד אז 8 שניות
   ב-Slow 4G.
2. **פריט E** — לפתוח את האתר באייפון ולהסתכל על השער. אם הלוגו מצויר,
   שומר הסף עובד; אם מלבן שחור — צריך להחזיר את החסימה.
3. שלוש בקשות חוצות-גבול נרשמו ב-`REQUESTS.md`.
