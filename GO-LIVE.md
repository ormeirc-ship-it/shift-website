# GO-LIVE - העלאת האתר לאוויר · הוראה מ-OC ‏30.7.2026

**זו המשימה הבאה, אחרי החמישה הקריטיים ב-`NEXT.md`. לא לפניהם.**

---

## הרקע - למה זה קרה

‏OC ראה שהכתובת החיה מציגה גרסה מ-9.7. הסיבה:

| ענף | קומיט אחרון | איפה |
|---|---|---|
| `origin/main` (= מה שבאוויר) | `5bc2c9e` · **9.7 09:26** | הענן |
| `main` (מקומי) | `397024a` · 27.7 23:46 | המחשב |
| `site-complete` | ‏30.7, עדכני | המחשב בלבד |

‏**80 קומיטים** ב-`site-complete` שלא הגיעו ל-main. אומת:
`git log site-complete..main` = **0** - אין ב-main שום דבר שחסר
ב-site-complete, לכן המיזוג אמור להיות fast-forward נקי.

---

## שלב 1 - התיקונים הקריטיים (חובה לפני העלייה)

‏`NEXT.md` §🔴, פריטים **A1-A5**. במיוחד **A1**: בלי GSAP כל ההירו
נעלם. מקומית זה תיאורטי כי הספרייה תמיד נטענת; **באוויר, מול חוסמי
פרסומות ורשתות חלשות, זה יקרה.** אחרי כל תיקון: `npm run check` מלא.

---

## שלב 2 - בדיקות לפני מיזוג

1. `npm run check` מלא **ירוק** על `site-complete`. אם לא - עוצרים.
2. `git status` נקי (אפס שינויים לא-מקומטים).
3. **סריקת סודות:** ‏`git grep -nEi "api[_-]?key|secret|token|password|firebase.*apiKey|BEGIN .*PRIVATE"` על כל ההיסטוריה שתיחשף.
   הריפו הופך לציבורי - כל קומיט קריא לכל אדם. אם יש ממצא: **לעצור
   ולדווח ל-OC**, לא "לתקן בשקט" (מפתח שדלף בהיסטוריה נשאר שם).
4. **משקל הפריסה:** ‏`git ls-files assets | xargs du -ch | tail -1` -
   אמור להיות ‏~18MB. אם קפץ לעשרות MB, נכנס נכס כבד בטעות
   (‏`_build/brain-dive-4k.mp4` הוא 26MB ו**חייב** להישאר ב-gitignore).
5. `node scripts/verify-program.mjs` - ‏21/21.

## שלב 3 - המיזוג והדחיפה

```
git checkout main
git merge site-complete        # אמור להיות fast-forward
npm run check                  # שוב, על main עצמו
git push origin main
```

אם המיזוג **לא** fast-forward - לעצור ולדווח, לא לכפות.
**לא למחוק את `site-complete`** אחרי המיזוג.

## שלב 4 - הגדרת GitHub Pages

אם Pages כבר מוגדר על `main` / `root` - אין מה לעשות, הפריסה
אוטומטית תוך ~2 דקות. אם לא, זו פעולה בממשק של GitHub שדורשת את
‏OC: ‏Settings → Pages → Source: Deploy from a branch → `main` → `/ (root)`.
**לכתוב ב-HANDOFF בדיוק מה נדרש ממנו**, אם נדרש.

## שלב 5 - אימות על האתר החי (לא על localhost)

הכתובת: `https://ormeirc-ship-it.github.io/shift-website/`
לאמת בפועל, ולתעד תוצאה לכל שורה:

- [ ] העמוד עולה, **הקונסול נקי** (בלי 404 על נכסים)
- [ ] **מסלול הכשל של A1**: לחסום את הדומיין של GSAP ב-DevTools
      ולוודא שהכותרת ושני ה-CTA עדיין נראים
- [ ] רצף המוח נטען (‏4 סטים - webp+avif, דסקטופ ומובייל)
- [ ] הגופנים ה-self-hosted עולים (לא נפילה ל-fallback)
- [ ] הצלילה, הנשימה, התפריט, ה-scrub של העיניים - עובדים
- [ ] מובייל אמיתי: 390×844 **וגם 844×390 (לרוחב)** - ראה A2/A3
- [ ] ‏`/sitemap.xml`, `/robots.txt`, `/404.html`, האייקונים - ‏200
- [ ] תצוגת השיתוף: להדביק את הקישור בוואטסאפ ולוודא ש-`og.png`
      והכותרת מופיעים נכון
- [ ] זמן טעינה ברשת אמיתית (‏Slow 4G ב-DevTools) - להשוות ל-`PERF.md`

## שלב 6 - סגירה

‏`HANDOFF.md` עם: הכתובת החיה · תוצאות שלב 5 שורה-שורה · מה
נשאר פתוח. ‏`RUNBOOK.md` - לעדכן שהפריסה בוצעה ומה השגרה מכאן.

---

## גבולות - לא לחרוג

- **לא לגעת ב-`og:url`/`canonical`.** הם כבר מצביעים נכון
  ל-github.io. שינוי = יום הדומיין, החלטת OC.
- **לא למחוק קבצים** (‏hero-bg-b, ארכוב delivery, ‏T12) - עדיין 🔒.
- **לא להפוך את הריפו לציבורי בעצמך** - זו פעולת OC בממשק.
- אם משהו בשלב 2 או 5 נכשל - **לעצור, לתעד, לדווח.** אתר שבור
  באוויר גרוע מאתר ישן באוויר.

---

## למה זה בטוח

הגרסה מ-9.7 נשארת בהיסטוריה. אם משהו משתבש אחרי הפרסום,
`git revert` או החזרת Pages לקומיט הקודם מחזירים את המצב.
שום דבר כאן אינו בלתי-הפיך.
