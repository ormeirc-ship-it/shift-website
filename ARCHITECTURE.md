# ARCHITECTURE — איך מנוע התנועה בנוי ולמה · 28.7.2026

מסמך כניסה למי שנוגע ב-`js/motion.js` (‏~950 שורות) בפעם הראשונה.
רבע שעה כאן חוסכת יום של הנדסה-לאחור.

## התמונה הגדולה

שלושה קבצים, שלוש אחריות:

- **`js/motion.js`** — כל מה שקשור בגלילה: הצלילה, ה-reveal, הערימה,
  הסרגל. ‏IIFE אחד גדול עטוף try/catch שמשחרר את מסך הפתיחה בקריסה.
- **`js/main.js`** — אינטראקציות בלי גלילה: תפריט, נשימה, וידאו-לוגו
  בשער (עם בדיקת השקיפות האמפירית), nav-tone observers, אנליטיקס-מוכנות.
- **`css/style.css`** — כל המצבים. ‏JS מדליק מחלקות; ‏CSS מחליט איך זה
  נראה. אין כתיבת סגנון ישירה מלבד מה שמתועד למטה.

ספריות: GSAP + ScrollTrigger + Lenis, כולן vendored ב-`assets/vendor/`
(אין CDN). ‏Lenis נשאר אחרי הכרעה מדודה (13KB, ‏LAUNCH 27.7).

## מכונת מסך הפתיחה (הקריטית ביותר)

שלוש רשתות ביטחון עצמאיות סוגרות את השער — כולן מוסיפות
`preloader-done` על `<html>`; הראשונה שמגיעה מנצחת:

1. **טיימר ב-head** (‏2.2ש' מרגע ריצת הסקריפט) — עובד גם אם כל שאר
   ה-JS מת. שים לב: הוא יושב *אחרי* ה-stylesheet, ולכן ברשת איטית
   הוא רץ רק אחרי ש-style.css הגיע (זה בסדר — לפני זה אין ציור בכלל).
2. **onload של תמונת המוח** (`#brainStill`, בגוף העמוד) — הרגע שבאמת
   יש מה להראות.
3. **motion.js עצמו** — `closeNow` על still.complete / load / טיימר,
   ובקריסה — ה-catch העליון מוסיף את המחלקה ומסיר את השכבה.

היציאה עצמה — אנימציית CSS טהורה (`plOut`), לא GSAP: היא חייבת לעבוד
גם כשמנוע התנועה לא הגיע. **אסור לגעת ב-overflow בשום שלב** (לקח R5/T11
— נעילה יתומה חסמה גלילה); היחיד שנוגע ב-overflow הוא התפריט (main.js).

## הצלילה (dive) — הלב

- **DOM:** ‏`.dive` בגובה 440vh (מובייל 420) → בפנים `.dive-sticky`
  (‏position:sticky, מסך מלא) → קנבס + שכבות (שער, שורות חולפות,
  `.dive-curtain`, ‏`.arrival`).
- **קלט:** ‏ScrollTrigger אחד על `.dive` נותן `p` ‏(0→1). אין מאזין
  scroll מקביל (נאכף בבדיקה — R1).
- **החלקה:** ‏`eased += (target - eased) * 0.14` בכל פריים rAF —
  זה מה שגורם לצלילה להרגיש כמו נשימה ולא כמו גלגל עכבר. הערך 0.14
  נעול בבדיקה.
- **פריימים:** ‏60 דסקטופ / 65 מובייל (‏WebP, ‏`assets/brain-seq/d|m/`),
  נטענים אחרי הפריים הראשון; ‏`window.__dive` חושף מדדים לכלי המדידה.
- **שלבי p:** שער נסוג עד 0.18 · שורות חולפות באמצע · ‏`aIn` (הגעה)
  נפתח ב-0.86→1: הווילון (`#diveCurtain`, אטימות=aIn) מחליף את ה-veil,
  הסרגל שקט (`dive-hush`) לכל האורך וחוזר רק עם ההצהרה (p>1).
- **מצב שקט:** ‏prefers-reduced-motion — אין קנבס ואין וילון; הגעה
  סטטית על נייבי, לוגו בהיר. הכול נאכף בסוללת `npm run motion`.

## מערכת ה-reveal — שלוש שכבות נסיגה

1. ‏`html.gsap-on` (ברירת מחדל): GSAP מנהל כניסות עם `jDur`.
2. ‏`html.js` בלי gsap-on (ספרייה לא נטענה): ‏IntersectionObserver מוסיף
   `.visible`; ‏CSS עושה את המעבר.
3. בלי JS בכלל: שום דבר לא מוסתר (נאכף בבדיקה — פריט 13).

## ציר המסע `--j`

בעליית העמוד, motion.js סורק את סקשני המסע לפי סדר ה-DOM (לא רשימה
קשיחה — נאכף בבדיקה) ונותן לכל אחד `--j` בין 0 ל-1. ‏CSS גוזר ממנו
ריווח, רוחב עמודה ומרווח כותרת; ‏JS גוזר ממנו משכי כניסה
(`jDur(el, min, max)`). התוצאה: העמוד "נפתח ונושם" ככל שמעמיקים,
בלי לגעת בטיפוגרפיה (הזהות). המפה המדודה — `RHYTHM.md`.

## ערימת הקלפים (program)

‏sticky-stack רק כשזה באמת נכנס: `stackFits()` מודד את הקלף הגבוה +
סרגל + שוליים מול גובה החלון, ורק אז מדליק `html.stack-on` ובונה
טריגרים. עמעום קלף מכוסה = אטימות על *הילדים* בלבד — הקלף עצמו נשאר
אטום (לקח B1: קריאוּת מעל הכול, נאכף ב-`npm run overlap`).

## גוון הסרגל (nav-tone)

- ‏main.js מחזיק observers על רצועה מתחת לסרגל; כל מקור (סקשן בהיר,
  צלילה, תפריט) כותב דרך ערוץ אחד — `window.__navTone.set(source, on)`.
- ‏motion.js מדווח מהצלילה רק בשינוי (`lit !== litOn`) — בלי ספאם פריימים.
- הלקח ההיסטורי (R1): המצב מחושב *לפני* כל early-return ב-render.

## חוזים שנאכפים אוטומטית

כל הלקחים שלמעלה חיים ב-`tests/` וב-`scripts/` (‏`npm run check`):
מי ששובר אחד — נתפס לפני commit, לא אחרי פריסה. הרחבה: `README.md`.
