# COVERAGE — פירוק הלא-מכוסה, שורה-שורה · 28.7.2026 ‏10:25

התוצר שביקש Cowork (‏NEXT ‏10:05 פריט 1): רשימה מפורשת של כל פער כיסוי
שנשאר אחרי הרחבת המסע, עם שורת-קוד, גודל וסיווג — כדי שאפשר לאמת אותה
סטטית, בלי כרום. המספרים מריצת `npm run coverage` על `2e20f55`.

## השורה התחתונה

| קובץ | לפני | אחרי | נשאר לא-מכוסה |
|---|---|---|---|
| ‏motion.js | ‏85.3% | **‏95.1%** | ‏~2.0KB — הכול מסווג למטה |
| ‏main.js | ‏86.1% | **‏93.4%** | ‏~0.9KB — הכול מסווג למטה |

הסף הועלה ‏60→80 (מרווח ~14 נק' מתחת לנמדד — תופס ריקון-מסע, סופג רעש).

## מה נוסף למסע (כל תוספת סגרה פער שנמדד לפני)

1. עכבר אמיתי: ‏cursor-dot (תנועה/עזיבה) + הכפתור המגנטי — הכפתור הראשון
   ב-DOM יושב בתפריט הסגור (רוחב 0), נבחר הראשון ה*נראה* + דיספץ' גיבוי.
2. קליק-עוגן + ‏Back — ‏pushState / ‏scrollToHash / ‏popstate.
3. נשימה בתנועה מלאה בדסקטופ (בניית ה-timeline) + נשימה **מלאה** במובייל
   עד "סיימתם" (~26ש' — שלושת הסבבים, כל שלבי ההנחיה, האיפוס).
4. לכידת הפוקוס בתפריט: ‏Tab בקצה + ‏Shift+Tab בקצה (שתי העטיפות).
5. קליק-אינסטגרם (מעקב ‏outbound; הניווט נבלם).
6. חציית ‏breakpoint‏ 900 — שני אגפי ‏matchMedia של ‏worlds (הבמה הנעוצה
   וה-cleanup שלה · גרסת המובייל).
7. ערימת הקלפים (‏B1): נדלקת רק כש-‏tallest+~156 ≤ גובה החלון — ‏1300 לא
   הספיק בפועל, ‏1800 כן. הדלקה, גלילה דרך הערימה, וכיבוי בהצרה.
8. כפתור הדילוג בתזמון אמיתי: טיימר ה-2.5ש' נדרך באתחול ומציג רק אם
   ‏eased≤0.25 — לכן עמוד נפרד, שהות בראש הצלילה, מעבר מלא (ההיעלמות
   ב-‎p>0.8), חזרה לאמצע ולחיצה.
9. טעינת מובייל עם ‎#hash (גלילה-לעוגן שבטעינה) + "לראש העמוד" בלי Lenis.

**ממצא תשתיתי בדרך:** ניווט בתוך עמוד מאפס את צבירת ה-Coverage של
puppeteer גם עם `resetOnNavigation: false` (נמדד: ‏90.7→86.9 כשהדילוג רץ
כ-reload באותו עמוד). לכן מסע-הדילוג הוא עמוד נפרד עם סשן כיסוי משלו.

## היתרה ב-motion.js — ‏6 פערים >60B, כולם מסווגים

| שורה | גודל | מה זה | למה לא מכוסה |
|---|---|---|---|
| ‏~982 | ‏569B | ‏catch של קריסת המנוע (משחרר את השער) | מסלול קריסה — מאומת ב-`npm run faults` (תרחיש gsap-killed); במסע תקין לא קורס |
| ‏~106 | ‏432B | **`splitChars` — קוד מת** | אין לו אף קורא בריפו (grep) — שריד מהאינטרו הישן. ‏T12 🔒 |
| ‏~389 | ‏189B | ‏fallback: ‏ScrollTrigger במקום ‏IntersectionObserver | רץ רק כש-IO חסר — לא קיים דפדפן כזה בסביבה (כרום מודרני) |
| ‏~560 | ‏165B | **ענף `.statement .label` — קוד מת** | האלמנט לא קיים ב-HTML — קוד מתגונן ל-DOM שאיננו. ‏T12 🔒 |
| ‏~428 | ‏157B | מאזיני ‏load/error על תמונת-הגיבוי לסגירת השער | מרוץ רשת: ב-localhost התמונה תמיד ‏complete לפני שה-JS רץ; הענף חי רק ברשת איטית (המסלול שנמדד ב-T9) |
| ‏~399 | ‏72B | ‏getter דיבוג של הצלילה (‏target/eased/frame) | כלי פיתוח — נקרא רק ידנית מהקונסול |

## היתרה ב-main.js — ‏5 פערים >60B, כולם מסווגים

| שורה | גודל | מה זה | למה לא מכוסה |
|---|---|---|---|
| ‏~28 | ‏169B | הענף הממתין של `afterReveal` (מאזין + טיימר 4.5ש') | מרוץ רשת: ב-localhost ‏`preloader-done` תמיד קודם ל-JS הנדחה (המדידה מ-27.7), אז הענף המהיר רץ |
| ‏~105 | ‏148B | ‏`play()` נדחה — החזרת הלוגו הסטטי | חסימת ניגון (חיסכון סוללה וכו') — לא משוחזרת ב-headless |
| ‏~45 | ‏115B | ה-catch של תא `safe()` | מסלול קריסה — מאומת ב-`npm run faults` (הזרקה פר-פיצ'ר); במסע תקין לא קורס |
| ‏~93 | ‏69B | ‏catch של בדיקת-השקיפות (‏framePasses) | ‏getImageData לא נזרק על וידאו same-origin — הענף חי רק בסביבות חריגות |
| ‏~132 | ‏69B | ‏fallback של ה-reveals כש-IO חסר | כמו ‏~389 — אין דפדפן כזה בסביבה |

מתחת ל-60B נשארים רסיסים (סוגריים, ענפי-ברירה של שורה) — רעש מדידה.

## איך מאמתים בלי כרום

כל שורה בטבלאות ניתנת לאימות סטטי: לפתוח את הקובץ בשורה, לוודא שהסיווג
נכון (קורא-אין / ‏fallback סביבתי / מסלול קריסה), ולוודא ששני פריטי
הקוד-המת רשומים ב-‏BACKLOG-tech ‏T12 🔒. הריצה עצמה (‏95.1/93.4) ניתנת
לשחזור רק עם כרום: `npm run coverage`.
