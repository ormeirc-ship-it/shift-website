# BACKLOG — 🔧 סוכן מכניקה

**בעלות:** `js/*` · `scripts/*` · `assets/*` · `package.json` · בדיקות
לפני כל פריט: לקרוא את `NEXT.md`. חוקי העבודה ב-`AGENTS.md`.

צריך שינוי ב-`index.html` או ב-`style.css`? **לא לגעת** — לכתוב ב-`REQUESTS.md`.

---

## עכשיו

### T22. 🟡→ המכניקה מוכנה במלואה (3.8); הכדור אצל OC לצעדים 1-2
> ‏Claude Code ‏3.8: **כל מה שלא דורש את OC - נבנה ומחכה:**
> ‏firebase.json (‏_deploy, ‏cleanUrls, ‏cache-headers) · ‏.firebaserc
> (‏placeholder ל-id) · ‏workflow חדש ‏deploy-firebase.yml -
> ‏workflow_dispatch בלבד עד ההפעלה, ‏rsync זהה-אחד-לאחד למלכודת-
> ההחרגה, **וצעד-אימות מובנה שנכשל אם ‏HANDOFF/אב-טיפוס/BACKLOG
> מוגשים** · ההחרגות של ‏Pages הורחבו (‏firebase.json/.firebaserc/
> ‏_deploy לא עולים בינתיים) · **‏scripts/host-swap.mjs** - הכלי
> שביקשת ("לרכז למקום אחד"): ‏dry-run מונה, החלפה בפקודה אחת,
> ומשרת גם את יום-הדומיין. **גילוי בדרך: הרשימה היא 16, לא 11** -
> יש גם משפחת-נתיב (‏404.html ×4 ו-start_url במניפסט) שמניחה את
> ‏/shift-website/ ונשברת באירוח-שורש; הכלי מחליף את שתיהן.
> **כשהפרויקט וה-Secret קיימים:** ‏id ל-.firebaserc → הרצת-בדיקה
> ידנית → ‏host-swap → ‏trigger ל-push → אימות מלא → שלבים 5-6.

#### הפריט המקורי:
🔴 האחסון עובר מ-GitHub Pages ל-Firebase Hosting — הכרעת OC ‏3.8
**המטרה של OC:** שהמאגר יהיה **פרטי** ובארגון המשותף `shiftceos`,
בלי שהאתר החי יירד.

**למה זה לא היה אפשרי כמו שהוא:** ‏GitHub Pages **לא פועל מריפו
פרטי** בתוכנית החינמית (דורש Pro/Team). כלומר "להפוך לפרטי" היה
מוריד את האתר. מעבר ל-Firebase מנתק את הקשר הזה לגמרי, ובדרך
לדומיין אמיתי זה הצעד הנכון בלאו הכי.

**מצב היום:** `.github/workflows/deploy-pages.yml` פורס בכל דחיפה
ל-main אל `https://ormeirc-ship-it.github.io/shift-website/`.
המאגר **ציבורי**.

---

#### 🚨 המלכודת הכי חשובה בפריט הזה
ה-workflow הנוכחי מריץ `rsync` שמחריג במפורש:
`.git · .github · node_modules · _build · _old-version · delivery ·
scripts · tests · *.md · package*.json · prototype-*.html`

**רשימת ההחרגה הזו היא כל מה שמונע מה-`HANDOFF`, ה-`BACKLOG`,
אבות-הטיפוס וכלי הבדיקה לעלות לאוויר.** אם מישהו יכתוב
`firebase.json` עם `"public": "."` — כל זה מתפרסם.
**להעביר את שלב ה-rsync כמות שהוא** ולפרוס מ-`/tmp/site`, או
לשכפל את הרשימה במדויק ל-`hosting.ignore`. **לאמת אחרי הפריסה
הראשונה:** ‏`curl -sI <site>/HANDOFF.md` ו-`/prototype-arrival.html`
חייבים להחזיר **404**. לדווח את שתי התוצאות.

---

#### הסדר קריטי — אין להפוך לפרטי לפני שלב 5
1. **[OC]** ליצור **פרויקט Firebase נפרד לאתר.**
   ⚠️ **לא** להוסיף אתר לפרויקט `shift-21-day-course-ceos` — זה
   הפרויקט של איתי עם נתוני אנשים אמיתיים. גבול נקי.
2. **[OC]** ליצור Service Account עם הרשאת פריסה ולשמור את המפתח
   כ-Secret במאגר (`FIREBASE_SERVICE_ACCOUNT`).
3. **[מכניקה]** `firebase.json` + workflow חדש: אותו rsync → פריסה
   מ-`/tmp/site`. להשאיר את `deploy-pages.yml` **פעיל בינתיים**.
4. **[מכניקה]** לאמת שהאתר החדש חי ותקין: כל הנכסים נטענים, הצלילה
   רצה, קונסול נקי, ושלושת ה-404 מהמלכודת למעלה.
5. **[OC]** רק עכשיו: להעביר את המאגר ל-`shiftceos` ולהפוך לפרטי.
6. **[מכניקה]** למחוק את `deploy-pages.yml` ולהסיר את פריסת ה-Pages.

#### 11 הכתובות המוחלטות
`index.html` מכיל 11 מופעים של `ormeirc-ship-it.github.io/shift-website`
— ‏`og:image` · `og:url` · `canonical` · חמישה ב-JSON-LD — ועוד
ב-`sitemap.xml` ו-`GO-LIVE.md`. כולם יצביעו לכתובת מתה.
⚠️ **בעלות חזות על `index.html`** — שורה ב-`REQUESTS.md`.
**להחליף אותם באותו סבב של שלב 3, לא אחריו.**

⚠️ **הם ישתנו שוב** כשיגיע דומיין (פריט T9/6). זה בסדר — אבל
לרכז אותם למקום אחד או לתעד את הרשימה, כדי שהסבב הבא לא יחפש.

#### שתי אמיתות שלא לטשטש
- **ריפו פרטי לא הופך את האתר לפרטי.** הוא מסתיר את הקוד. האתר
  נשאר פומבי, וזו הכוונה.
- **מה שהיה ציבורי כבר היה ציבורי.** הפיכה לפרטי לא מוחקת עותקים
  שנלקחו. הסיכון המעשי נמוך (איש לא ידע על המאגר), אבל אסור
  להציג את זה כאילו נמחק. **התיקון האמיתי לאזהרת ה-Auth הוא
  פריט 6 בפלטפורמה, לא דגל הנראות כאן.**

נוסף 3.8 ע"י Cowork · **ההכרעה של OC**.

### T21. ✅ 🟡 drift משווה עכשיו גם שבועות (3.8) — הוכח שנכשל על מוטציה של תו
**איך זה התגלה:** ‏3.8 נמצא ש-`שבוע שלישי — תרגול והטמעה` באתר בזמן
שבמסד החי כתוב `שבוע שלישי — מהגוף אל החיים, וכיוון קדימה` (‏D16).
‏`npm run drift` **לא תפס את זה** — הוא משווה 21 כותרות ימים בלבד.

**זה בדיוק סוג הכשל שהכלי נבנה למנוע.** מוניטור שמדווח "אין סחיפה"
בזמן שיש סחיפה גרוע ממוניטור שלא קיים, כי הוא קונה שקט שווא.

**להוסיף ל-`scripts/drift-check.mjs` השוואה של קולקציית `weekIntros`:**
`.../databases/(default)/documents/weekIntros` — לשלוף `title` ו-
`description` לשלושת השבועות, ולהשוות מול `.week-head h3` ו-
`.week-desc` ב-`index.html`.

**⚠️ שתי מלכודות בהשוואת ה-`description`:**
1. במסד הוא **טקסט אחד עם `\n\n`**, באתר הוא **כמה `<p>`**. לנרמל את
   שני הצדדים לאותה צורה, אחרת זה ידווח סחיפה תמידית.
2. **תיאור שבוע 3 באתר הוא נוסח OC ‏2.8 שגובר במכוון על המסד** (מתועד
   בהערה מעל ה-`div` ב-`index.html`). זה **לא** סחיפה. להוציא אותו
   מההשוואה במפורש, עם הערה שמסבירה למה — לא בשקט.

**אימות:** לשנות זמנית תו אחד בכותרת שבוע 1 ולוודא שהכלי **נכשל**.
מוניטור שלא הוכח שהוא מזהה הוא הנחה, לא בדיקה. נוסף 3.8 ע"י Cowork.

### T19. ✅ 🔴 השכבה החיה ב-50 — בוצע 3.8; ‏linkLock: ברירת-המחדל ("50 על הכל") אושרה בהכרזת-הגורף של OC ‏4.8 — סגור
**הכרעת OC ‏3.8:** "תחזיר אותם כמו שהיו, רק שיהיו כמותית יותר —
שכבה חיה ב-50."

**שינוי אחד בלבד:** הקבוע `ALIVE` ב-`js/motion.js` (סביב שורה 444)
מ-`0.25` ל-`0.50`. שום דבר אחר במנוע לא נוגעים בו — הרשת, הקווים,
ההבהוב, ההתפזרות החוצה, כולם נשארים בדיוק כפי שהם.

**⚠️ אבל הסליידר הזה מזיז שלושה דברים בבת-אחת, וזה לא לינארי.**
נמדד בסימולציה (1440×900, 600 פריימים):

| | ‏ALIVE 0.25 | ‏ALIVE 0.50 | יחס |
|---|---|---|---|
| נקודות | 26 | 37 | ‏×1.42 |
| רדיוס קישור | 138 | 148 | ‏×1.07 |
| קווים גלויים/פריים | 68 | 190 | **×2.8** |
| "דיו" הקווים (אטימות×אורך) | 62 | 377 | **×6.1** |
| בדיקות מרחק/פריים | 325 | 666 | ‏×2.05 |

כלומר: ‏OC ביקש **יותר נקודות** (×1.42), אבל מה שגדל בפועל הוא
**הרשת** (×6). זה עדיין בגדר תקציב — 666 בדיקות לפריים זה כלום —
אבל ויזואלית ההבדל הוא בין "יותר כוכבים" ל"מארג צפוף".

**לכן באב-הטיפוס נוסף מתג `קווים כמו ב-25`** (‏`linkLock`): מנתק את
רדיוס-הקישור ואת אטימות-הקו מהסליידר ומשאיר אותם ברמת 0.25, כך
שהנקודות מתרבות אבל הרשת נשארת אותה רשת. **אם OC יבחר בו** — הקבוע
מתפצל לשניים ב-`motion.js`: `ALIVE = 0.50` לנקודות, `LINK_ALIVE =
0.25` לקווים. ברירת המחדל עד להכרעה: 50 על הכל, כפי שנאמר.

**אימות:**
- ‏`prefers-reduced-motion`: המנוע לא רץ, כמו היום.
- ‏VR: `d-dive-end` — הקנבס מוקפא. בייסליין חדש (יותר נקודות),
  אחרי אישור חזותי. לוודא שתי ריצות זהות.
- ‏`npm run perf` + `console`: לאשר שה-×2 בבדיקות המרחק לא נראה.
- קשירה ל-`onScreen` ולנראות-הטאב — לשמר.
- מובייל נשאר בלי קנבס, כמו היום.

**המנוע החלופי (תנועה בפרספקטיבה) — נדחה, נשמר להשוואה.**
קיים ב-`prototype-arrival.html` מאחורי המתג `→ תנועה קדימה`.
לא להעביר ל-`motion.js`. הערכים 20 (אור) ו-70 (בליטת טקסט)
**לא משתנים**. נוסף 3.8 ע"י Cowork · **ההכרעה של OC**.

### T20. ⏸ 🔴 חיווט `/join` — ממתין למפתח שירות-הטפסים מ-OC (הדרך המומלצת אושרה עקרונית; הרצף המלא מוכן לריצה ברגע שיש מפתח)
**הכרעות OC ‏3.8:** ‏(א) הליד הולך למייל של OC · ‏(ב) **סליקה בצד** —
`FREE_MODE` נשאר `true`, לא לגעת · ‏(ג) התקנון נכתב (ראה למטה).

⚠️ **הפער שצריך לפתור לפני שנוגעים בקוד:** אתר סטטי לא יכול לשלוח
מייל בעצמו. `submitLead` מופשט וזה טוב, אבל צריך ממסר. שלוש דרכים,
ו**זו הכרעת OC** כי הראשונה דורשת ממנו פעולה:
1. **שירות טפסים חינמי** (Web3Forms / Formspree) — `POST` לנקודת-קצה,
   הם שולחים את המייל. אפס תשתית, אפס תחזוקה. דורש מ-OC לפתוח חשבון
   ולמסור מפתח גישה. **המומלץ בנפח הנוכחי.**
2. **פונקציית ענן** בפרויקט של האתר — שליטה מלאה, אבל דורשת פרויקט
   וחשבון-חיוב. לא מוצדק לעשרות לידים.
3. ‏`mailto:` — פותח את תוכנת הדואר של המבקר. **לא.** מאבד לידים
   ונראה שבור בנייד.

**כשהמפתח קיים — הרצף המלא:**
- `join.js`: מימוש `submitLead` מול נקודת-הקצה + `LEAD_WIRED = true`.
  ⚠️ שגיאת רשת חייבת להיתפס ולהציג נפילה-רכה עם הקישור הישיר —
  לא להשאיר את המבקר מול כפתור שלא קרה בו כלום.
- ‏`join/index.html`: להסיר `<meta name="robots" content="noindex">`.
- ‏`sitemap.xml`: להוסיף את `/join/`.
- ‏`index.html` שורה ~683: ה-CTA `להצטרפות למסלול` עובר מ-
  `https://shift-21-day-course-ceos.web.app` ל-`/join/`.
  ⚠️ **בעלות חזות** — שורה ב-`REQUESTS.md`, לא לגעת.
- ‏`terms.html`: להסיר `noindex` **רק** אחרי אישור משפטי של OC.

**אימות:** הגשה אמיתית → המייל הגיע ל-OC עם כל השדות · שדה ריק
נחסם · כשל רשת מדומה → נפילה-רכה · `join_submit`/`join_success`
נורים · מובייל 390 · מקלדת בלבד.

**⚠️ ולא לשכוח מה השער הזה לא עושה:** בפלטפורמה אין אימות. מי שיודע
את הכתובת הישירה נכנס בלי לעבור בטופס. השער הזה **מסנן ומודד** —
הוא לא נועל. זה תקף ומתועד, לא באג חדש.

נוסף 3.8 ע"י Cowork · **ההכרעות של OC**.

### T18. ✅ 🔴 ההגעה נחשפת בגלילה — בוצע 3.8 (כל התחנות, כל האימותים; דוח ב-HANDOFF)
OC: "מגיע למסך של מהישרדות ליצירה ומיד נזרק לסקשן הבא."

**האבחנה (Cowork, נמדד בקוד):**
`.dive` = 440vh · ההגעה חיה רק ב-`aIn = (p - 0.86) / 0.14` = **~62vh,
פחות ממסך אחד** — בזמן שהכניסה המדורגת אורכת **4.35ש'**. כלומר האנימציה
ארוכה מהחלון שבו היא חיה. המבקר נזרק לפני שהכפתורים הופיעו.

**הפתרון — הצעת OC, והיא עדיפה על "להאריך את ההחזקה":**
במקום כניסה מבוססת-זמן שצריך לתת לה מקום, **הגלילה עצמה חושפת**:
עוד גלילה → הלוגו · עוד → הכותרת · עוד → הפסקה · עוד → הכפתורים.
זה מבטל את אי-ההתאמה מהשורש (החשיפה לא יכולה להקדים או לפגר),
מתגמל כל צעד גלילה, ועקבי עם שאר הצלילה שכולה scrub.

**היברידי — זה החלק שמונע תחושת סליידר:**
הגלילה קובעת **מה** נחשף; כל אלמנט, ברגע שנחשף, נכנס באנימציה קצרה
משלו (0.6–0.9ש', `cubic-bezier(.22,1,.36,1)`). לא scrub על האטימות.

**תוספת OC (3.8): הסצנה כולה מתעוררת עם הלוגו.**
לא רק הלוגו נחשף בתחנה הראשונה — **כל שכבות ההגעה נדלקות שם יחד**:
פתיחת הווילון, הזוהר הנושם, קרן האור, הסקרים, והרשת החיה. זהו רגע
ה"הגעתי" — ומשם הטקסט נבנה בתוך סצנה שכבר חיה, במקום להופיע על רקע
שעדיין מתגבש.

⚠️ **אבל לא כמתג.** הדלקה חדה תרגיש כמו קפיצה. השכבות נכנסות
בחפיפה על **0.55 → 0.62** ומגיעות למלוא העוצמה בדיוק כשהלוגו נוחת.
הקנבס מתחיל לרוץ ב-0.55 (עדיין שקוף) כדי שהרשת כבר תהיה בתנועה
ולא תתחיל מקפוא ברגע שרואים אותה.

**תחנות מוצעות (לכוונון):**
| p | מה נחשף |
|---|---|
| ~0.55–0.62 | **הסצנה מתעוררת:** וילון · זוהר · קרן · סקרים · רשת חיה |
| ~0.60 | רצף הפריימים מסתיים: `draw(round(min(p/0.60,1) * (COUNT-1)))` |
| 0.62 | לוגו — נוחת על סצנה שכבר חיה |
| 0.70 | "מהישרדות ליצירה" — `condense` מטשטוש, כמו היום |
| 0.80 | הפסקה — **הפער הרחב כאן הוא "פעימת הדממה" בגרסת מרחק** |
| 0.88 | הכפתורים |
| 0.88→1.00 | החזקה: הכול גלוי, הזוהר נושם, הרשת חיה |

`.dive` 440vh → **520vh** (מובייל 420 → 480) כדי שהתחנות לא יהיו צפופות.

**חובה — נעילה בכיוון אחד:** מה שנחשף לא מוסתר בגלילה למעלה.
העיקרון הזה כבר קיים בקוד (`staged` חד-פעמי) — לשמר אותו לכל תחנה
בנפרד. `latched[i] = true` ולא לחזור אחורה.

**מה שמוחלף:** מנגנון ה-`staged` הגלובלי + ה-`animation-delay` המדורג
ב-CSS. הזמנים 0/0.45/2.5/3.35 מתבטלים — הם הופכים לתחנות p.
אב-הטיפוס `prototype-arrival.html` נשאר כמקור לערכי הוויזואל
(20/25/70) — רק התזמון משתנה.

**⚠️ הפשרה, ו-OC מודע לה:** "פעימת הדממה" הופכת ממרחק-זמן למרחק-גלילה.
מי שגולל מהר יעבור אותה מהר. זה המחיר של "אף אחד לא מפספס".
אם בבדיקה זה ירגיש מכני — מרחיבים את הפער 0.70→0.80, לא חוזרים לזמן.

**אימות:**
- לגלול בשלושה קצבים (איטי / רגיל / מהיר מאוד) — בכל אחד כל ארבעת
  האלמנטים חייבים להיחשף לפני שההגעה יוצאת. לדווח מה קרה בכל קצב.
- גלילה למעלה ובחזרה: כלום לא נעלם ולא מהבהב.
- מובייל 390 באותה בדיקה.
- `prefers-reduced-motion`: הכול גלוי מיידית, בלי תחנות.
- כפתור הדילוג (F) עדיין עובד; לדווח אם 520vh מרגיש ארוך מדי.
- VR: `d-dive-end`/`m-dive-end` — נקודת הדגימה זזה, בייסליין חדש
  אחרי בדיקה חזותית. לוודא דטרמיניזם.

**בעלות:** `js/motion.js` + `css/style.css` (הסרת ה-delays, גובה `.dive`)
— אם הסוכנים מופרדים, שורה ב-`REQUESTS.md`.
נוסף 3.8 ע"י Cowork · **הרעיון של OC**.

### T12. 🔒 שני ממצאי קוד-מת מליטוש הכיסוי (28.7) — מחיקה = הכרעת OC
ניתוח הפערים של `npm run coverage` (מסע מורחב, ‏95.1/93.4%) איתר שני
גושים שלא ירוצו לעולם ב-`js/motion.js`:
1. **`splitChars` (~שורה 106, ‏432B)** — שריד מהאינטרו הישן; אין לו אף קורא
   (אומת ב-grep על כל הריפו).
2. **ענף `.statement .label` (~שורה 560, ‏165B)** — קוד מתגונן לאלמנט
   שלא קיים ב-HTML.
שניהם לא מזיקים; מחיקתם זולה והפיכה, אבל מדיניות הלילה הייתה "קוד מת
מדווח, לא נמחק". נוסף 28.7 ע"י Claude Code.

### T8-מקורי
ספארי (דסקטופ ומובייל), פיירפוקס, כרום. במיוחד: `<video>` עם HEVC שנוסף
בשלב 6, `backdrop-filter`, ו-WebP.

### T9. 🔒 `og:image` / `og:url` — ממתין לדומיין מ-OC

---

### T17. 🟢 קוד מת מ-D12 — ענף pathLine ב-motion.js
‏D12 (29.7) הסיר את `#pathLine` מה-HTML; נשאר ענף `if (line)` ב-`motion.js`
‏718–727 (מוגן, לא קורס — ניקיון בלבד). VR חייב 0.00% על d-path/m-path.
נוסף 29.7 ע"י Cowork.

## פרויקטי רקע — כשהתור ריק

### TA-מקורי
אין באתר שום בדיקה. `node:test` מספיק. להתחיל מהפונקציות הטהורות
ב-`motion.js` ו-`main.js`, ואז בדיקת עשן עם jsdom שמרנדרת את העמוד
ומוודאת שכל סקשן קיים ולא ריק.

### TB-מקורי
צילומי מסך של כל הכותרות ב-5 רוחבים (360 / 414 / 768 / 1024 / 1440),
וזיהוי גלישות, יתומים, ושורות מעל 75 תווים. RTL ועברית שוברים הנחות
שהרבה CSS מניח בשקט. ממצאים → `REQUESTS.md` לסוכן החזות.

### TC-מקורי
לקבוע תקציב מפורש (למשל: 2MB עד ל-first paint) ולכתוב סקריפט שנכשל
כשחורגים. אחרת המשקל רק יגדל.

---

## סגורים (תקציר; הפירוט המלא ב-`BACKLOG-ARCHIVE.md`)

- T16. ✅ הציור מלווה את הגלילה — sticky בגובה-אפס (29.7)
- T13. ✅ חיווט "שיחת היכרות" — בוצע 28.7 ‏11:52 (`3db5e7e`)
- T14. ✅ הצלילה עברה למקור ה-4K (גרסה B של OC) — הושלם 29.7
- T15. ✅ ה-scrub חי — ארבעת התנאים קוימו במלואם (28.7)
- T11. ✅ 🟡 R6 — נסגר 27.7 (שתי השורות נמחקו); הפריט כאן לא עודכן — סומן 28.7
- T9. ✅ R5 — תוקן 27.7, עמוק מההצעה: היציאה עברה ל-CSS ונעילת הגלילה נמחקה
- T10. ✅ 🟡 R4 — נסגר 27.7; הפריט כאן לא עודכן — סומן 28.7
- T0. ✅ 🔴 R1 — תוקן 27.7. הבלוק הועבר מעל ה-early-return, ו-onScreen עבר ל-IO
- T0.5 ✅ 🟡 R2 — תוקן 27.7. התצפיתנים נבנים מחדש ב-resize (debounce 180ms). ~~R2 — `rootMargin` מקובע ל-`innerHeight` של הטעינה
- T1. ✅ (26.7 — `PERF.md` נכתב; אין jank. אבל כלי המדידה בסקראצ'פד — ראה פריט M ב-`BACKLOG.md`) חלקות התנועה — מדידה, לא תחושה
- T2. ✅ הצלילה תחת לחץ — הושלם 27.7 (`npm run stress`). ארבעה תרחישים, כולם עברו. ~~
- T3. ✅ קונסול נקי — הושלם 27.7 (`npm run console`). אפס הודעות, כולל תחת אינטראקציה. ~~ ומעקב שגיאות
- T4. ✅ prefers-reduced-motion — הושלם 27.7 (`npm run motion`). 20 אפקטים, כולם עם חלופה שקטה. ~~ — לאמת, לא להניח
- T5. ✅ מוניטור סחיפת תוכן — הושלם 27.7 (`npm run drift`). אומת שהוא גם מזהה. ~~ — `scripts/drift-check.mjs`
- T6. ✅ מדידת ביצועים אוטומטית — הושלם 27.7
- T7. ✅ נגישות מלאה — הושלם 27.7 (`npm run a11y` → `A11Y.md`). שלושה ממצאים תוקנו. ~~
- T8. ✅ תאימות דפדפנים — הושלם 27.7 (`npm run compat`)
- TA. ✅ חבילת בדיקות ראשונה — הושלם 27.7 (`npm test`)
- TB. ✅ טיפוגרפיה עברית ב-5 רוחבים — הושלם 27.7 (`npm run type`)
- TC. ✅ תקציב משקל — הושלם 27.7 (`npm run budget`)
