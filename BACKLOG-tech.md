# BACKLOG — 🔧 סוכן מכניקה

**בעלות:** `js/*` · `scripts/*` · `assets/*` · `package.json` · בדיקות
לפני כל פריט: לקרוא את `NEXT.md`. חוקי העבודה ב-`AGENTS.md`.

צריך שינוי ב-`index.html` או ב-`style.css`? **לא לגעת** — לכתוב ב-`REQUESTS.md`.

---

## עכשיו

### T16. ✅ הציור מלווה את הגלילה — sticky בגובה-אפס (29.7)
נבחר sticky (המסלול שהותר במפורש) ולא pin: מעטפת בגובה-0 שנדבקת מתחת
לסרגל ומלווה עד קצה הסקשן — **אפס שינוי בגובה המסע** (בלי pin-spacer,
בלי הזזת עוגנים/RHYTHM). נמדד: הציור נעוץ ב-‏72px לאורך ‏55%→85% מהסקשן
עם ‏scrub מתקדם (‏3.0→4.7ש') — רגע העין-אל-המוח נצפה במלואו, בדיוק
הבקשה. **ממצא-דרך שתוקן בשורש:** ‏overflow:hidden על הסקשן הופך אותו
לקופסת-גלילה ושובר sticky פנימי — הוחלף ב-‏overflow:clip עם hidden
כגיבוי (דפדפן ישן: ניוון חינני לעוגן העליון של טרום-T16).
‏reduced-motion/מובייל: הווידאו מוסתר כמו קודם — המסילה ריקה.
‏VR: ‏d-events בלבד (7.24%, מכוון) · שער מלא ‏13/13.
~~הסקשן גולל החוצה לפני סוף הרצף — לנעוץ בזמן ה-scrub.~~
פירוט: `NEXT.md` פריט 1. נוסף 28.7 ע"י Cowork.

### T13. ✅ חיווט "שיחת היכרות" — בוצע 28.7 ‏11:52 (`3db5e7e`)
וואטסאפ primary (טקסט פתיחה מוכן) · אינסטגרם ghost · מייל כקישור במשפט.
אומת קצה-לקצה (היעד נפתח על הפרופיל עם הטקסט); ‏outbound תופס גם mailto
(הדלגייט הורחב) וגם wa.me (נוסף לרשימה הסגורה בבדיקת האבטחה).
~~`mailto:ormeirc@gmail.com` · `https://wa.me/972528972050`. לוודא
ש-`track('outbound')` נתפס.~~ פירוט: `NEXT.md` פריט 1. נוסף 28.7 ע"י Cowork.

### T14. 🔓 איכות רצף המוח — **נפתח 28.7 ‏17:15**: מקור 4K חדש נוצר ונבחר
OC הורה "ליצור קובץ איכותי" → Cowork ייצר שתי גרסאות 4K (Seedance 2.0
Pro, ‏3840×2160, ‏8ש') בשפת המותג; **OC בחר גרסה B**. קישורי ההורדה,
צעדי החיווט המלאים והתנאים (משקל/VR/B3/מיקרו-תוויות): `NEXT.md`
§T14-מתקדם. זה עוקף את ההמתנה למקור הישן.
**תגלית 28.7 ‏12:05 (בפיקסלים, לא בהנחות):** ‏`brain-intro-source.mp4`
שבריפו **אינו** המקור של הצלילה — הוא שוט אמביינטי אחד של המוח המואר;
לצלילה החיה (חושך→wireframe→אור) אין רנדר-מקור בריפו. העדות:
`delivery/brain-source-mismatch.png`. לכן "רינדור מחדש במלוא המקור"
חסום על נכס שרק OC יכול לספק.
**מה כן בוצע:** צינור חילוץ מלא (`scripts/brain-frames.mjs`, כרום-קנבס,
בלי ffmpeg) + מדידת העלות על תוכן מאותה משפחה: כפול-רזולוציה = ‏×1.9
משקל (‏d-avif ‏1.6→3.0MB); פריים-1/הרגע-הראשון לא נפגעים. דוח ההחלטה
המלא ל-OC: **`delivery/BRAIN-QUALITY.md`** (3 אופציות + דמו חדות).
~~בקשת OC ‏28.7 ("8K") — המקור 1920×1080 בלבד...~~ פירוט: `NEXT.md`
פריט 3. נוסף 28.7 ע"י Cowork.

### T15. ✅ ה-scrub חי — ארבעת התנאים קוימו במלואם (28.7)
‏currentTime-scrub על הווידאו הקיים, דגל הסרה `data-scrub="off"`.
**התנאים:** (1) שער מלא לפני ‏13/13 ואחרי ‏13/13 · (2) חלקות נמדדה לפני
commit: קצב-קריאה ריאלי (‏12ש' לסקשן) — **אפס תקיעות**; מעבר מהיר (‏4.2ש')
— ‏19fps יציב + תקיעה בודדת ‏1.3ש' ב-GOP דל-keyframes (‏media ‏2.45→4.2,
עקבי; ‏snap מחזיר מיד). לא קרטוע בקצב אמיתי — שוּפט כעביר, מתועד ·
(3) דטרמיניזם VR אומת ולא הונח: שתי ריצות עצמאיות תחת scrub — ‏13×0.00%
בשתיהן (‏__videoFreeze גובר על הלולאה) · (4) ‏e2e ‏19/19 + קונסול נקי.
**שני ממצאי-דרך שתוקנו בשורש:** ‏preload="none" בלע את ה-scrub (בלולאה
הישנה play() משך את הקובץ; עכשיו העלאה ל-auto בפתיחת הטריגר) · ‏serveRepo
לא תמך ב-Range/206 — כרום לא מסוגל לבצע seek בכלל (‏currentTime ננעל על 0);
נוסף לרִיג, שמשקף עכשיו את GitHub Pages.
**ל-OC (זול):** ייצוא-מחדש של events-loop.mp4 עם keyframe כל ≤0.5ש'
ימחק גם את התקיעה של הגלילה המהירה. נוסף 28.7 ע"י Cowork.

### T12. 🔒 שני ממצאי קוד-מת מליטוש הכיסוי (28.7) — מחיקה = הכרעת OC
ניתוח הפערים של `npm run coverage` (מסע מורחב, ‏95.1/93.4%) איתר שני
גושים שלא ירוצו לעולם ב-`js/motion.js`:
1. **`splitChars` (~שורה 106, ‏432B)** — שריד מהאינטרו הישן; אין לו אף קורא
   (אומת ב-grep על כל הריפו).
2. **ענף `.statement .label` (~שורה 560, ‏165B)** — קוד מתגונן לאלמנט
   שלא קיים ב-HTML.
שניהם לא מזיקים; מחיקתם זולה והפיכה, אבל מדיניות הלילה הייתה "קוד מת
מדווח, לא נמחק". נוסף 28.7 ע"י Claude Code.

### T11. ✅ 🟡 R6 — נסגר 27.7 (שתי השורות נמחקו); הפריט כאן לא עודכן — סומן 28.7
Cowork אישר ✅ בסקירת 27.7 ‏14:05. אומת מחדש 28.7 בבוקר: `body.style.overflow`
— 0 מופעים ב-`motion.js`, ובדיקת הרגרסיה ב-`tests/motion.test.mjs`
("גם שחרור עיוור אסור") שומרת שלא יחזור.
~~T9 הסיר את השחרורים מה-HTML אבל `dismissPreloader()` ומטפל-הקריסה עדיין עושים
`document.body.style.overflow = ''`. אם התפריט פתוח כשהחשיפה מסתיימת (רשת איטית) —
הנעילה שלו נמחקת. תיקון: למחוק את שתי השורות; מאז T9 רק התפריט נוגע ב-overflow.~~
פירוט: `REVIEW.md` 27.7 12:06. נוסף 27.7 ע"י Cowork.

### T9. ✅ R5 — תוקן 27.7, עמוק מההצעה: היציאה עברה ל-CSS ונעילת הגלילה נמחקה
מדידה בשלוש מהירויות רשת אישרה את הממצא והוסיפה עליו: הטקס של GSAP **לא הוצג
לאף אחד באף רשת** (ה-onload תמיד הקדים את חבילת התנועה), ונעילת הגלילה היתומה
נמדדה ב-89–101 פריימים (~0.8ש') בכל תצורה — ב-Slow 4G היא נחתה בשנייה השביעית.
לכן במקום `finishReveal()` מוקדם: יציאת מסך הפתיחה היא עכשיו אנימציית CSS על
`preloader-done` (רצה גם כשחבילת התנועה עוד בדרך), motion.js לא נועל overflow
בכלל, ושחרורי ה-overflow העיוורים הוסרו מה-HTML (הם דרסו את נעילת התפריט).
אחרי: 0 פריימים נעולים בכל שלוש הרשתות; קונסול נקי; מכוסה בבדיקת רגרסיה
ב-`tests/motion.test.mjs` (22/22). שאלת העיצוב הוכרעה (NEXT ~11:00): ה-preloader
נשאר — והתיקון הזה הוא מה שגורם לו סוף-סוף להיות מוצג באמת ולא לרוץ בחושך.
~~ה-onload מעלים את מסך הפתיחה; motion.js נועל גלילה אחרי שהשחרור כבר רץ.~~

### T10. ✅ 🟡 R4 — נסגר 27.7; הפריט כאן לא עודכן — סומן 28.7
`node_modules/` ב-`.gitignore` (שורה 26) והוסר מהמעקב — אומת מחדש 28.7:
`git ls-files node_modules/` ריק. Cowork אישר ✅ בסקירת 27.7 ‏14:05.
**עדיין עומד להכרעת OC:** ניקוי ההיסטוריה שכבר תפחה (‏.git ~161MB).
~~להוסיף `node_modules/` ל-`.gitignore` + `git rm -r --cached node_modules`.~~
נוסף 27.7 ע"י Cowork.

### T0. ✅ 🔴 R1 — תוקן 27.7. הבלוק הועבר מעל ה-early-return, ו-onScreen עבר ל-IO
(ScrollTrigger לא יורה onToggle בקפיצת עוגן). אומת בשני התרחישים. ~~R1 — `nav-light` נדבק אחרי הצלילה (רגרסיה מ-`fd311a1`)
ה-early-return ב-`render()` (`js/motion.js`) מדלג על חישוב `hush`/`lit` כש-`p` התכנס.
`onToggle(false)` אחרי שהייה בהגעה לאור → מפתח `'dive'` נשאר דלוק → סרגל בהיר על
`#products` הכהה. תיקון: בלוק `hush`/`lit`/`skipTick` לפני ה-early-return, או
`lastOnScreen` בתנאי. פירוט ותרחיש אימות: `REVIEW.md` סעיף 27.7 + `NEXT.md`.
נוסף 27.7 ע"י Cowork.

### T0.5 ✅ 🟡 R2 — תוקן 27.7. התצפיתנים נבנים מחדש ב-resize (debounce 180ms). ~~R2 — `rootMargin` מקובע ל-`innerHeight` של הטעינה
שני ה-observers של גוון הניווט ב-`main.js`. ליצור מחדש ב-resize (debounce).
נוסף 27.7 ע"י Cowork.

### T1. ✅ (26.7 — `PERF.md` נכתב; אין jank. אבל כלי המדידה בסקראצ'פד — ראה פריט M ב-`BACKLOG.md`) חלקות התנועה — מדידה, לא תחושה
נוספו לעמוד: צלילה של 97 פריימים, מערכת פרמטרים לאורך הגלילה, ופרלקסות.
אף אחד לא מדד מה זה עושה. לפתוח DevTools → Performance, להקליט גלילה מלאה
בדסקטופ ובהאטת CPU ×4 (סימולציית מובייל), ולדווח:
- FPS מינימלי, ואיפה בדיוק הוא צונח
- long tasks מעל 50ms
- layout thrash — קריאות DOM שמכריחות reflow באמצע אנימציה
לתעד ב-`PERF.md` עם המספרים. אחר כך לתקן את הגרוע ביותר.

### T2. ✅ הצלילה תחת לחץ — הושלם 27.7 (`npm run stress`). ארבעה תרחישים, כולם עברו. ~~
97 פריימים × ~45KB. לבדוק במצבים אמיתיים:
- **Slow 3G** — מה המבקר רואה ב-5 השניות הראשונות? האם המסך תקוע?
- **גלילה מהירה** לפני שהרצף נטען — האם הוא קופץ, נתקע, או מדלג יפה?
- **טאב מוסתר** ואז חזרה — האם הכל עדיין מסונכרן?
- **רענון באמצע הצלילה** — נוחת במקום הנכון?

### T3. ✅ קונסול נקי — הושלם 27.7 (`npm run console`). אפס הודעות, כולל תחת אינטראקציה. ~~ ומעקב שגיאות
לעבור על העמוד המלא ולוודא אפס שגיאות ואפס אזהרות. כל אזהרה שנשארת —
לתעד למה היא לגיטימית. לשקול `window.onerror` שמדווח לקונסולה בפורמט אחיד.

### T4. ✅ prefers-reduced-motion — הושלם 27.7 (`npm run motion`). 20 אפקטים, כולם עם חלופה שקטה. ~~ — לאמת, לא להניח
נוספו הרבה אפקטים חדשים. להריץ את כל העמוד עם ההגדרה דלוקה ולוודא שלכל אחד
יש חלופה שקטה שבאמת עובדת. אפקט שפשוט "לא רץ" ומשאיר תוכן נסתר = באג חמור.

### T5. ✅ מוניטור סחיפת תוכן — הושלם 27.7 (`npm run drift`). אומת שהוא גם מזהה. ~~ — `scripts/drift-check.mjs`
**הפרויקט שהיה מונע את הטעות הגדולה שלנו.** משווה שלושה מקורות ומדווח:
1. Firestore החי (`firestore.googleapis.com/v1/projects/shift-21-day-course-ceos/databases/(default)/documents/days`)
2. `data.js` בריפו הפלטפורמה
3. סקשן 21 הימים ב-`index.html`
פלט: מה תואם, מה נסחף, ומתי כל מקור עודכן. קוד יציאה ≠0 בכל סחיפה.
`--json` ל-CI עתידי, ו-`npm run drift`.

### T6. ✅ מדידת ביצועים אוטומטית — הושלם 27.7
`npm run perf` (FPS/long tasks/recalc, כולל האטת CPU ×4) · `npm run net`
(throttling רשת אמיתי) · `npm run budget` (תקציב שנכשל בחריגה). התוצאות
ב-`PERF.md`. לא Lighthouse — מדידה ישירה דרך CDP, שנותנת בדיוק את המדדים
שביקשנו בלי תלות חיצונית. ~~מקורי:
Lighthouse מקומי על `index.html`, שומר תוצאה מתוארכת ל-`perf/`, כדי לראות
רגרסיה בין סבבים. במיוחד עכשיו — נוספו 4.5MB נכסים.

---

## אחר כך

### T7. ✅ נגישות מלאה — הושלם 27.7 (`npm run a11y` → `A11Y.md`). שלושה ממצאים תוקנו. ~~
מעבר מקלדת על כל העמוד · ניגודיות בפועל על כל צמד צבעים · קורא מסך על
הכניסה דרך המוח (מה הוא שומע כשגוללים דרך קנבס?). לתעד ב-`A11Y.md`.

### T8. ✅ תאימות דפדפנים — הושלם 27.7 (`npm run compat`)
14 פיצ'רים בסיכון נסקרו, וכולם מכוסים בגיבוי. **הכלי מצא באג אמיתי:**
`100svh` הופיע בארבעה מקומות **בלי הצהרת `vh` לפניה** — בספארי מתחת
ל-15.4 ההצהרה נפסלת כולה והצלילה מאבדת גובה. תוקן בכל הארבעה.
(והתיקון השני היה בכלי עצמו: הבדיקה חיפשה `100vh` בכל הקובץ ומצאה אותו
במקום אחר, כלומר דיווחה "יש גיבוי" כשלא היה. עכשיו היא פר-הצהרה.)
**מה שהכלי לא עושה:** להריץ ספארי או פיירפוקס — אין כאלה בסביבה.
הוא מוודא שקיים מסלול נסיגה, לא שהמסלול נבדק. ראה גם פריט E.

### T8-מקורי
ספארי (דסקטופ ומובייל), פיירפוקס, כרום. במיוחד: `<video>` עם HEVC שנוסף
בשלב 6, `backdrop-filter`, ו-WebP.

### T9. 🔒 `og:image` / `og:url` — ממתין לדומיין מ-OC

---

## פרויקטי רקע — כשהתור ריק

### TA. ✅ חבילת בדיקות ראשונה — הושלם 27.7 (`npm test`)
21 בדיקות ב-`node:test`, בלי תלויות חדשות, רצות ב-40ms. שני קבצים:
`structure.test.mjs` (מבנה, נכסים, עוגנים, שמות נגישים, נעילת פלטה/גופן)
ו-`motion.test.mjs` — **כל בדיקה שם מתעדת באג אמיתי שקרה**, כדי שלא יחזור
בשקט: סדר החישוב ב-render (R1), מקור onScreen (R1), טעינה בשלבים,
גזירת המסע מה-DOM, layout thrash בגוון הניווט, ו-UA בשומר הסף.

### TA-מקורי
אין באתר שום בדיקה. `node:test` מספיק. להתחיל מהפונקציות הטהורות
ב-`motion.js` ו-`main.js`, ואז בדיקת עשן עם jsdom שמרנדרת את העמוד
ומוודאת שכל סקשן קיים ולא ריק.

### TB. ✅ טיפוגרפיה עברית ב-5 רוחבים — הושלם 27.7 (`npm run type`)
**מדידת שורות ויזואליות אמיתיות** (Range על צומתי טקסט), לא הערכה לפי
רוחב חלקי גודל גופן. וזה חשף שהתיקון מהסבב הקודם היה שגוי:
**יחידת `ch` נגזרת מרוחב הספרה "0", שרחבה מהתו העברי הממוצע** — 68ch
בעברית הם ~88 תווים בפועל. היחס שנמדד: ~1.29 תווים עבריים ל-ch.
זו בדיוק ההנחה השקטה ש-RTL שובר, כפי שהפריט ניבא.
24 ממצאים → 0 בכל חמשת הרוחבים (360/414/768/1024/1440).

### TB-מקורי
צילומי מסך של כל הכותרות ב-5 רוחבים (360 / 414 / 768 / 1024 / 1440),
וזיהוי גלישות, יתומים, ושורות מעל 75 תווים. RTL ועברית שוברים הנחות
שהרבה CSS מניח בשקט. ממצאים → `REQUESTS.md` לסוכן החזות.

### TC. ✅ תקציב משקל — הושלם 27.7 (`npm run budget`)
חמישה תקציבים לכל רוחב: מסלול קריטי, סה"כ, JS, CSS+גופנים, תמונות.
המסלול הקריטי נמדד לפי זהות המשאב ולא לפי מרוץ תזמונים — על localhost
כל מדידה מבוססת-זמן מחזירה הכול או כלום. התקציבים נגזרו מהמצב בפועל
+ ~12%, כי תקציב שנקבע גבוה מדי לא שומר על כלום. אומת שהוא נכשל בחריגה.

### TC-מקורי
לקבוע תקציב מפורש (למשל: 2MB עד ל-first paint) ולכתוב סקריפט שנכשל
כשחורגים. אחרת המשקל רק יגדל.
