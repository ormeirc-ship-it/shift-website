# NEXT — תיבת הדואר מ-Cowork

**סקירה אחרונה: 27.07.2026 13:30 · קומיט: site-complete `fc427c9` · תור מורחב — OC לא זמין לשעות הקרובות**

OC יצא. **כל מה שדורש הכרעת OC — לא נוגעים** (רשימה בתחתית). כל השאר —
תור ארוך ואוטונומי. עבוד פריט-פריט, commit נפרד לכל פריט, `npm run check` בסוף כל סבב.

---

## אישורים שביקשת — ניתנים

**שני השינויים המבניים מ-D1 מאושרים על ידי (במסגרת סמכות ההכרעה שלי):**
- ✅ **D1 #5** — פתיחת הסיפור: כותרת+משפט מעל התמונה; במובייל התמונה ~60vh.
- ✅ **D1 #6** — "מה נפתח": אשכולות עם כותרות-משנה, בלי לשנות את טקסט הגלולות.
אחרי שניהם: להריץ מחדש overlap + type-audit + a11y (סדר פוקוס השתנה), ולצלם ל-`delivery/`.

---

## התור — לפי סדר

### חלק א: השלמות פתוחות
1. **D1 #5 + #6** (מאושרים, למעלה).
2. **DB** — השוואת רפרנסים (izanami-official.com · alethia.earth). אם פאנל הדפדפן
   עדיין תקוע — צילומי viewport דרך shots.mjs על ה-URLs החיצוניים, והשוואה מהצילומים.

### חלק ב: תשתית אתר סטטי — מה שכל אתר חי צריך
3. **404.html** — בעברית, ברוח העיצוב, קישור חזרה. GitHub Pages מגיש אותו אוטומטית.
4. **robots.txt + sitemap.xml** — עמוד יחיד; ה-URL כרגע github.io, לעדכן עם הדומיין (להוסיף שורה ל-LAUNCH.md).
5. **מטא שחסר ב-`<head>`:** `rel=canonical` · `theme-color` (מהפלטה הנעולה) ·
   `apple-touch-icon` (מ-favicon.png בגודל 180) · `og:image:width/height` · `og:locale=he_IL` ·
   `twitter:card`. לא לגעת ב-og:url/og:image עד דומיין.
6. **JSON-LD** — `Organization` + `WebSite` בלבד, אך ורק ממידע שכבר כתוב בעמוד. שום עובדה חדשה.
7. **SRI** — hashes `integrity` + `crossorigin` לכל סקריפט CDN (gsap, lenis וכו'),
   ו-`rel="noopener"` לכל קישור חיצוני עם target.

### חלק ג: רשת ביטחון אוטומטית — שהאתר יגן על עצמו
8. **`perf/` מתוארך** — `npm run perf` ו-`npm run net` שומרים JSON מתוארך ל-`perf/`;
   סקריפט `npm run perf:diff` שמשווה לריצה הקודמת ומתריע על רגרסיה >10%.
9. **רגרסיה ויזואלית** — baseline מ-shots.mjs + pixelmatch; `npm run vr` נכשל על
   שינוי לא צפוי מעל סף. baseline מתעדכן במפורש בלבד (`npm run vr:accept`).
10. **`npm run check` מאחד הכל** — tests, verify, overlap, budget, a11y, console,
    reduced-motion, vr, drift. סדר: מהמהיר לאיטי. פלט סיכום אחד. זה השער של כל סבב.
11. **hook `pre-commit`** קל (tests+verify בלבד, <10ש') דרך `scripts/install-hooks.mjs`.
12. **drift-check משודרג** — פלט `--json`, ותיעוד ב-README מתי מריצים (לפני כל פריסה).

### חלק ד: עמידות חוויה
13. **חוויית בלי-JS** — לעבור על העמוד עם JS כבוי: כל הכותרות, התמונות, העוגנים.
    לתקן כל מה שנשען על JS בלי גיבוי. בדיקה אוטומטית ב-structure.test.
14. **bfcache** — לוודא שאין `unload`/`beforeunload`; חזרה עם Back מחזירה עמוד חי
    (טיימרים, ScrollTrigger). לתקן עם `pageshow` אם צריך.
15. **תקינות מסך-מגע** — כל אינטראקציית hover (קלפים, כפתורים) חייבת מקבילת מגע;
    `:focus-visible` לא נדלק על טאפ. לעבור רכיב-רכיב.
16. **feature-guards** — audit של כל שימוש ב-backdrop-filter, image-set, text-wrap,
    scrollIntoView-smooth, HEVC: מה קורה בדפדפן שלא תומך? fallback מפורש לכל אחד. לתעד ב-`COMPAT.md`.
17. **טיפוגרפיה עברית אחרי המבנה החדש** — type-audit על 5 רוחבים; יתומים, גלישות,
    גרשיים עקביים (״ ולא "), מקפים עקביים. תיקוני CSS בלבד.

### חלק ה: ביצועים — בלי לגעת בהחלטות של OC
18. **גופנים** — Assistant נשאר (נעול), אבל: subset עברית+לטינית בסיסית, self-host
    עם `font-display: swap`, preload ל-woff2 המרכזי. למדוד לפני/אחרי ב-net.mjs.
    (זו אופטימיזציית אספקה, לא החלפת גופן.)
19. **audit של preload/priority אחרי 440vh** — האם עדיין נטענים פריימים שלא בשימוש?
    fetchpriority נכון? lazy נכון על כל מה שמתחת לקפל?
20. **דיאטת נכסים — רשימה בלבד, בלי מחיקה:** כל קובץ ב-repo שלא מופנה מהקוד,
    גודלו, והמלצה. לשים ב-`delivery/CLEANUP.md` להכרעת OC.

### חלק ו: מוכנות לעלייה
21. **אירועים (D8)** — לשפר מבנה/קצב של שלוש הכרטיסיות עם placeholder-ים מסומנים
    בבירור. שום תוכן מומצא.
22. **אנליטיקס-מוכנות** — שכבת אירועים פנימית (`track('breath_done')` וכו') שכותבת
    ל-console/dataLayer בלבד. חיבור לכלי אמיתי — החלטת OC.
23. **README.md + RUNBOOK** — מבנה הריפו, כל פקודות ה-npm, ומדריך פריסה צעד-צעד
    ליום שבו יש דומיין (כולל עדכון og/sitemap/canonical במקום אחד).

### חלק ז: רקע — אם כל זה נגמר
24. הרחבת בדיקות: תרגיל הנשימה (מכונת המצבים), התפריט, nav-tone edge cases.
25. `npm run doctor` — סקריפט שבודק שסביבת הפיתוח שלמה (node גרסה, תלויות, פורט פנוי).
26. מעבר שני על RHYTHM.md — כל חריגה שסומנה "לבדוק" → להכריע ולתעד.

---

## 🔒 לא לגעת בהיעדר OC
חיווט B3 · הסרת Lenis · og:url/og:image (דומיין) · פרטי קשר · תוכן אירועים אמיתי ·
מחיקת קבצים · פונטים (המשפחה) וצבעים · דחיפה לאתר החי · מיזוג ל-main (שלי).

## כללים
commit לכל פריט · `npm run check` בסוף סבב · HANDOFF.md מתעדכן כל 5 פריטים ·
נתקעת על פריט (סביבה/החלטה)? דלג, רשום למה, המשך. הסבב שלי הבא: ~14:00, ואז כל שעתיים.

---

**סקירה אחרונה: 27.07.2026 13:30 · קומיט: `fc427c9`**
