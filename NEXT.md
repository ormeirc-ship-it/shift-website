# NEXT — תיבת הדואר מ-Cowork

**סקירה אחרונה:** 27.07.2026 08:30 · קומיט שנסקר: site-complete `8948b3d` · main `8948b3d` (כבר מוזג — אין מיזוג נדרש)

---

## הסבב עבר את הבדיקות — אבל נמצא באג 🔴 אחד בקוד החדש

אימתתי בפועל: `node --check` ✓ · `verify-program.mjs` מול `shift platform main` ✓ 21/21 ·
jsdom 7 סקשנים ✓ מלאים · כל הנכסים המופנים קיימים (כולל `logo.png` שהוחזר) ✓ ·
14 תמונות עם מידות ✓ · פונטים/צבעים נעולים לא הופרו ✓ · reduced-motion בקוד החדש תקין ✓.

הממצאים 1–2 מהסבב הקודם טופלו יפה: הסרגל קורא את הרקע, הכותרת בנשימה מאוזנת,
כפתור הדילוג קיים בשתי צורות, `--j` נגזר מה-DOM.

---

## 🔧 מכניקה — לפי סדר

### 1. R1 🔴 — `nav-light` נדבק אחרי הצלילה (רגרסיה בקומיט `fd311a1`)
ב-`js/motion.js`, ה-early-return — `if (Math.abs(p - lastP) < 0.0008) return;` — יושב
**לפני** חישוב `hush`/`lit`. כש-`eased` מתכנס ל-1 (לכל מי שמשתהה רגע בהגעה לאור; הקוד
אף מצמיד `eased = target`) ואז טריגר הנראוּת יורה `onToggle(false) → render()` — העדכון
מדולג, מפתח `'dive'` נשאר דלוק ב-`__navTone`, וסרגל בהיר יושב על `#products` הכהה.
**זה בדיוק הבאג שתואר ב-HANDOFF כמתוקן ("target נשאר 1 לנצח") — הוחזר באותו קומיט
ע"י אופטימיזציית הכתיבות.** האימות "ב-8 נקודות" נעשה כנראה בגלילה רציפה, שמסווה את זה.
**תיקון:** להעביר את בלוק `hush`/`lit` (ו-`skipTick`) לפני ה-early-return, או לכלול את
`onScreen` בתנאי השינוי (`lastOnScreen`).
**אימות חובה:** עצירה של 2 שניות בהגעה לאור → גלילה ל-`#products` → הסרגל חוזר כהה.
פירוט מלא: `REVIEW.md`, סעיף 27.7.

### 2. פריט M — כלי המדידה אבדו
`perf.mjs`, `shoot.mjs`, `audit.mjs`, `weigh.mjs` נכתבו בסקראצ'פד ואינם ב-`scripts/`.
אי-אפשר לשחזר אף מספר ב-`PERF.md` ואין איך לזהות הידרדרות בין סבבים.
לכתוב מחדש, `scripts/` + `package.json` עם `npm run perf` / `npm run shots`.

### 3. R2 🟡 — `rootMargin` מקובע לגובה חלון של זמן הטעינה
שני ה-IntersectionObserver-ים ב-`main.js` מחשבים `innerHeight` פעם אחת. סיבוב מסך
או קריסת שורת ה-URL במובייל מזיזים את פס התצפית. ליצור מחדש ב-resize (debounce).

### 4. פריט H — למדוד ברשת אמיתית (Lighthouse + throttling; קל אחרי M)
### 5. פריט E — שומר הסף על ספארי; אם אין — לתעד ב-`DELIVERY.md` שזו הנחה לא בדוקה
### 6. T5 — מוניטור סחיפת תוכן. הפריט שהיה מונע את הטעות הגדולה — עדיין לא קיים.

## 🎨 חזות

התחל מ-**D1** (מעבר עיניים מלא — מיפוי, לא תיקון). שתי נקודות למפה:

1. **הסרגל הבהיר עדיין נקרא כרצועה** ב-`hero-3-light-1440` — פס אפרפר (~`#EEF0EE`)
   מעל התכלת. שיפור עצום מהנייבי, אבל עדיין רכיב. שקול backdrop-filter קל או ירושת
   גוון מהסקשן. לא דחוף — ליטוש.
2. תוך כדי D1: עצור בהגעה לאור והמשך לגלול — תראה את R1 בעצמך. אל תתקן (מכניקה),
   אבל תעד איך זה מרגיש כחוויה.

## כללים

- העבודה עדיין רצה כסוכן יחיד על `site-complete`; תיקיית `shift-website-tech` וענפי
  `site-design`/`site-tech` עוד לא קיימים. עד לפיצול — שתי הרשימות מופנות לאותו סוכן.
- `REQUESTS.md` ריק — אין בקשות פתוחות.
- אל תדחוף לאתר החי. פונטים וצבעים נעולים. 🔒 — לדלג.
- `delivery/before/` הוסיף 3.9MB לריפו — בפעם הבאה קישור להיסטוריה במקום העתק (R3 🟢).

---

**סקירה אחרונה: 27.07.2026 08:30 · קומיטים שנסקרו: site-complete `8948b3d` · main `8948b3d`**
