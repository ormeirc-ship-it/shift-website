# NEXT — תיבת הדואר מ-Cowork

**סקירה אחרונה: 27.07.2026 22:05 · קומיט: `e8d50e0` (13:35) · הסבב הבא שלי: ~00:00**

🚨 **סבב חמישי ברציפות בלי שום פעילות — ~8.5 שעות מאז 13:35.** מאז 16:10 השתנו
רק קבצי התיאום שלי (NEXT/REVIEW). ההנחיות מ-14:05 עומדות כלשונן. אם אתה קורא
את זה — התחל מפריט 0 (שחרור ה-lock), ואז פריט 0.5 (ה-🔴).

**ל-OC — זה כבר לא עניין של סוכן:** Claude Code לא רץ מאז 13:35. שני דברים
דורשים אותך: (א) להתניע את Claude Code ידנית; (ב) או למחוק בעצמך את
`.git/index.lock` בטרמינל: `rm ~/Desktop/SHIFT/shift-website/.git/index.lock`
(ניסיתי שוב ב-22:05 — `Operation not permitted` מה-sandbox).
הקוד עצמו יציב (אומת שוב 22:05: בדיקות 23/23, תוכן 21/21 מול המקור החי,
syntax נקי על כל js/ ו-scripts/, שבעת הסקשנים במקומם) — אבל ה-🔴 של
מופחת-תנועה יושב בקוד מאז הצהריים ואיש לא מתקן.

---

## פריט 0 — לפני הכל: שחרור הריפו

`.git/index.lock` (0 בייט, 14:03) נוצר ע"י ה-git שלי בסביבה המבודדת ואין לי
הרשאה למחוק אותו. ודא שאין תהליך git רץ, מחק את `.git/index.lock`, והמשך.

## פריט 0.5 — 🔴 תיקון רגרסיה מ-B3 (פירוט מלא ב-REVIEW.md ‏14:05)

במצב `prefers-reduced-motion`: הפריים המוצג הוא המואר + veil נייר בהיר, אבל
`html.ending-curtain` צובע את כותרת ההגעה בלבן ⇒ טקסט לבן על רקע בהיר.
תיקון מוצע: לעטוף את חוקי הצבע של `ending-curtain` ‏(style.css ‏1902–1912)
ב-`@media (prefers-reduced-motion: no-preference)`. לאמת גם את מסלול `!FULL`
(בלי GSAP) — אותה חשיפה. אחרי התיקון: `npm run motion` + צילום reduced-motion
ל-`delivery/`.

## ואז — התור של 13:30, מפריט 2 והלאה

הרשימה המלאה בגרסה הקודמת של הקובץ (`git show 69c6dc4:NEXT.md`). תקציר הסדר:

2. **DB** — השוואת רפרנסים (izanami-official.com · alethia.earth) דרך shots.mjs אם הפאנל תקוע.
3. **404.html** → 4. **robots+sitemap** → 5. **מטא ב-head** → 6. **JSON-LD** → 7. **SRI**.
8–12. רשת ביטחון: perf מתוארך · רגרסיה ויזואלית · `npm run check` מאוחד (כיום חסר בו a11y +‏ reduced-motion — לכלול!) · pre-commit hook · drift.
13–17. עמידות: בלי-JS · bfcache · מסך-מגע · feature-guards · טיפוגרפיה אחרי המבנה החדש.
18–20. ביצועים: subset גופנים · audit ‏preload אחרי 440vh · רשימת דיאטת נכסים (בלי מחיקה).
21–23. אירועים (D8) · אנליטיקס-מוכנות · README+RUNBOOK.
24–26. רקע: בדיקות נשימה/תפריט · doctor · מעבר שני על RHYTHM.

## 🔒 לא לגעת בהיעדר OC
og:url/og:image (דומיין) · פרטי קשר · תוכן אירועים אמיתי · מחיקת קבצים ·
פונטים (המשפחה) וצבעים (Assistant · ‎#18163B · ‎#5CBBF0 · ‎#F6F5F2) ·
דחיפה לאתר החי · מיזוג ל-main. **B3 בוצע — נשאר כמות-שהוא עד אישור OC בדיעבד; לא לבנות עליו עוד.**

## ממתין להכרעת OC בשובו
אישור B3-א בדיעבד (הפיך במחלקה אחת) · Lenis (הומלץ בנתונים: להשאיר) ·
דומיין · מייל/וואטסאפ · תוכן אירועים · אימות-ביד + אייפון (E).

## כללים
commit לכל פריט · `npm run check` בסוף סבב · HANDOFF.md כל 5 פריטים ·
נתקעת? דלג, רשום למה, המשך.

---

**סקירה אחרונה: 27.07.2026 22:05 · קומיט: `e8d50e0`**
