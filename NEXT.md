# NEXT — תיבת הדואר מ-Cowork

**סקירה אחרונה: 27.07.2026 12:06 · קומיט: site-complete `747f3a6`**

מצב: שלב הבסיס מתקדם בקצב טוב. T9 ✅ · B1 ✅ (שניהם נסקרו — עומק אבחון מצוין,
בדיקות רגרסיה נוספו). B2 היה באמצע commit בזמן הסקירה (index.lock מ-12:04;
בעץ העבודה COUNT=60 + 60 פריימים על הדיסק — תואם). ההנחיה של OC מ-11:00 בתוקף:
בסיס חזק קודם, D2–D7 מוקפאים.

---

## ל-Claude Code — לפי סדר

### 1. לסגור את B2 עד הסוף
- להשלים את הקומיט (אם עוד לא נסגר) ולוודא שתיקיית הביניים `d97-tmp` לא נשארה
  לא ב-git ולא על הדיסק (בזמן הסקירה: staged rename אליה + תיקייה ריקה).
- **מדידה לפני/אחרי ב-`PERF.md`** — חלקות scrub בהאטת CPU + `npm run net` ב-Slow 4G.
  זו ההצדקה של כל הדילול; בלי המספרים B2 לא סגור.
- אם עדיין לא חלק — `createImageBitmap` מראש, כמתוכנן.

### 2. T11 / R6 🟡 — שחרור overflow עיוור נשאר ב-`motion.js` (ממצא חדש)
T9 הסיר את השחרורים מה-HTML, אבל `dismissPreloader()` (~130) ומטפל-הקריסה (~940)
עדיין מוחקים `document.body.style.overflow` — אם התפריט פתוח ברגע החשיפה (רשת
איטית), הנעילה שלו נדרסת. תיקון של שתי שורות. פירוט: `REVIEW.md` 12:06.
מהיר — לסגור לפני B3.

### 3. B5 / T10 🟡 — `node_modules` עדיין לא ב-`.gitignore` (נכון ל-12:06)
ראיתי שנוטרל המוקש של `logo.png` — טוב שנתפס לפני זה. עכשיו אפשר לבצע בבטחה:
`node_modules/` ל-`.gitignore` + `git rm -r --cached node_modules`. בלי לגעת בהיסטוריה.

### 4. B3 🟡 — סיום הצלילה: שתי גרסאות ל-`delivery/`, לא ליישם בלי OC
כמפורט בתוכנית 11:00: ברוח מסך הפתיחה, ניגודיות 4.5:1 מדודה בכל גרסה.

### 5. אחר כך — B6 (ריצת חיסון: stress/reduced-motion/console/drift/H על המצב החדש)
ואז B7 — `LAUNCH.md` עם צ'קבוקסים לחסמי-עלייה שאינם קוד.

## ממתין ל-OC
- **פריט E** — לפתוח את האתר באייפון אמיתי: לוגו מצויר = שומר הסף עובד; מלבן שחור = לדווח.
- **B3** — הכרעה בין שתי הגרסאות כשיהיו ב-`delivery/`.

## בקשות פתוחות מ-`REQUESTS.md`
שתי בקשות עיצוב (הסרגל כרצועה בהגעה לאור · יחס 520vh) — מוקפאות עם D2–D7.
בקשת פריט N נסגרה: הדילול הוא ההחלטה, B2 מממש.

## כללים
סוכן יחיד על `site-complete` · לא דוחפים לאתר החי · פונטים וצבעים נעולים ·
B3 לא מיושם סופית בלי אישור OC · `npm run check` בסוף כל סבב (אין Chrome בסביבת
Cowork — האכיפה עליך).

---

**סקירה אחרונה: 27.07.2026 12:06 · קומיט: `747f3a6` · הסבב הבא של Cowork: ~14:00**
